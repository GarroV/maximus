<!-- managed by furca: формат строк ниже не менять — файл парсят fabrica и cursus -->
# Задачи

<!-- id: Tnnn (T001, T002, ...). статус: todo | in_progress | done | failed | blocked:Qnnn (Qnnn — id открытого вопроса из questions.md). "зависит от" — id через запятую или «—». блок: имя блока продукта из plan.md, либо chores для служебных задач, которые не относятся ни к одному блоку (добор исследования, выяснение доступов, ответы владельца) — их не отдают блок-агентам. -->

## Как читать этот граф

**Что здесь лежит.** Работа, которая берётся в стройку: задачи по модулям
эталона и дефекты продукта. У каждой строки в тексте назван **issue** (`#N`) и,
если задача рисует экран, **модуль дизайн-системы**. Мелкие находки, вопросы «а
как правильно» и техдолг, до которого руки не дошли, живут в
[Issues](https://github.com/GarroV/maximus/issues) и сюда не
переезжают: граф — план стройки, а не свалка.

**Источник истины о том, что должно быть на экране, — не эта таблица.** Это
папка `Дизайн-система MAXIMUS/` плюс issues (решение владельца 27.08.2026).
Спека FURCA описывает поведение зарплатного модуля и осталась про него; поэтому
задача, рисующая экран, сверяется с модулем эталона, а не со спекой. Что чему
соответствует и где дыры — [docs/product-map.md](docs/product-map.md).

**Перед тем как взять задачу — сверься.** Три места, все три обязательны:
модуль эталона, журнал блока в `docs/furca/blocks/`, решения в
`docs/furca/decisions.md`. Одной командой: `python tools/context.py <модуль>`.

**Почему граф однажды разошёлся с правдой.** С T161 работа переехала в issues, а
сюда её никто не переносил: 171 задача `done`, 11 `todo` — и почти все `todo`
либо отложены владельцем, либо давно сделаны. Диспетчер `fabrica` читает
**этот** файл, поэтому стройка агентами была невозможна не по технической
причине, а потому что раздавать было нечего. Приведено в правду 28.08.2026 по
прямому указанию владельца.

| id | блок | зависит от | статус | задача |
|---|---|---|---|---|
| T001 | db | T008 | done | Починить изоляцию регистров учёта: политики видимости сделать `as restrictive` и добавить `force row level security`. Дефект подтверждён на живой базе 2026-08-06, фикс написан в `0004_facts.sql` — перенести при переезде на миграции. Готово, когда тест ролью `app_user` показывает: роль с доступом только к `official` не получает строк других регистров |
| T002 | db | T008 | done | Починить видимость записей с `tenant_id is null`: системные роли и общий справочник статей не проходят политику изоляции и не видны никому. Готово, когда тест подтверждает видимость системных ролей и общих статей |
| T003 | db | T008 | done | Убрать привязку к Supabase: перевести функции контекста на `app_user_id()`, удалить шим `auth.uid()` и `db/platform/supabase.sql`. Готово, когда схема накатывается с нуля и `grep -ri supabase db/` пуст |
| T004 | db | T008 | done | Переименовать регистры учёта по D009: тип и колонки `layer` → `ledger`, значения `official`/`supplementary`/`internal` — в миграциях, движке, YAML-пресете и тестах. Готово, когда `grep -ri "white\|grey\|black"` не даёт попаданий по смыслу регистра и тесты зелёные |
| T005 | chores | T008 | todo | Отложено осознанно: дописать тесты SQL-слоя фактов по списку из `docs/backlog-facts.md`. Это модуль сборки P&L, он вне скоупа первой версии и ждёт переезда схемы на миграции Django |
| T006 | chores | — | done | Вычищены персональные данные из истории git: таблица удалена из всех коммитов, имя сотрудника заменено, `.gitignore` пускает только обезличенный файл, оригинал вынесен под `PAYROLL_FIXTURE`. Проверено: `git log -p` не содержит ни одного ФИО из таблицы |
| T007 | chores | T006 | done | Обезличенная фикстура собрана (`tools/make_fixture.py`, 8 листов, 32 строки, все четыре схемы). Регрессия гоняется на ней всегда: 23 зелёных без настоящей таблицы, 31 зелёный с ней |
| T008 | db | — | done | Завести каркас Django 6.1 на Python 3.13 и перенести схему `0001`–`0003` в миграции Django, политики и функции — через `RunSQL`. Готово, когда схема накатывается на чистую базу одной командой и старые SQL-файлы удалены |
| T009 | db | T008 | done | Завести роль `app_user` без `bypassrls` и гранты для неё; тесты доступа гонять только ею. Готово, когда тест изоляции тенантов падает при попытке выполнить его владельцем таблиц — то есть проверяет то, что должен |
| T010 | db | T008 | done | Версионирование правил по датам: непересечение периодов действия через `btree_gist` и `EXCLUDE`. Готово, когда попытка вставить пересекающееся правило отклоняется базой, а тест это показывает |
| T011 | core | T008 | done | Перенести правила из YAML в таблицы пресетов и переопределений; реализовать `load_preset_at(tenant, country, on_date)` со слоями страна → партнёр → группа → сотрудник. Готово, когда есть тест на каждый уровень переопределения |
| T012 | core | T011 | done | Сверка переноса: расчёт на правилах из базы даёт то же, что расчёт из YAML, по всем 30 сотрудникам и четырём схемам. Готово, когда тест сравнивает два источника и оба сходятся с эталоном |
| T013 | core | T011 | done | След расчёта `explain()`: какие правила сработали, с какими входами, какой версией ставки. Готово, когда тест проверяет, что сумма шагов равна итогу строки |
| T014 | auth | T009 | done | Вход, роли, членства; middleware выставляет контекст пользователя через `set_config(..., true)` внутри транзакции запроса при `ATOMIC_REQUESTS`. Готово, когда четыре роли из спеки работают и представления не фильтруют по тенанту вручную |
| T015 | auth | T014 | done | Тесты утечки контекста: два запроса разных пользователей на одном соединении пула; фоновая задача без контекста. Готово, когда оба случая дают пустоту, а не чужие данные |
| T016 | web | T014 | done | Каркас интерфейса: базовый шаблон, навигация, компоненты таблицы с итогами, форм и диалогов; htmx 2.x зафиксированной версии в `static/`. Готово, когда экраны собираются из компонентов, а не из копипасты |
| T017 | web | T016 | done | Локализация на русский, английский и сербский; демо всегда на английском. Готово, когда переключение языка работает и непереведённых строк на экранах нет |
| T018 | web | T016 | done | Админка справочников: сотрудники, группы, точки, юрлица, производственный календарь. Готово, когда справочники ведутся без обращения к базе руками |
| T019 | timesheets | T041 | done | Сетка ввода часов «сотрудник × тип часа» с сохранением ячейки без перезагрузки и клавиатурной навигацией. Готово, когда табель на 30 человек заполняется с клавиатуры и не теряется при уходе со страницы |
| T020 | timesheets | T019 | done | Импорт таблицы партнёра поверх существующего импортёра, идемпотентный по ключу. Готово, когда загрузка эталонной таблицы даёт корректный табель, а повторная загрузка ничего не меняет |
| T021 | timesheets | T020 | done | Отчёт импорта и проверки ввода: неизвестные листы и колонки, часов больше нормы, сотрудник без часов, часы у уволенного. Готово, когда испорченный файл не молчит, а показывает, что именно не разобрано |
| T022 | timesheets | T019 | done | Закрытие часов по точке отдельно от других точек; управляющий видит и правит только свою. Готово, когда есть тест на попытку залезть в чужую точку |
| T023 | payrun | T011, T019 | done | Модель периода и переходы статусов: черновик → посчитан → утверждён, один черновик на период; запись в утверждённый период отклоняется базой, а не только интерфейсом. Готово, когда есть тест на каждый переход, включая запрещённые |
| T024 | payrun | T023 | done | Расчёт периода фоновой задачей с прогрессом; задача сама выставляет контекст тенанта. Готово, когда расчёт 30 сотрудников проходит фоном и повторный запуск не плодит ведомости |
| T025 | payrun | T023 | done | Утверждение и откат: откат требует причины, причина и автор попадают в историю периода. Готово, когда откат без причины невозможен, а история показывает, кто и зачем открывал |
| T026 | payrun | T025 | done | Правки задним числом: по умолчанию помеченная дельта в текущий период со ссылкой на исходный и в тот же регистр; переключение на пересчёт — только через явное переоткрытие, настройкой тенанта. Готово, когда тест показывает, что закрытый период остался байт в байт прежним |
| T027 | payrun | T023 | done | Блокировка построчная и по точкам: спорный сотрудник не держит закрытие остальных. Готово, когда тест закрывает точку при незакрытой строке в другой точке |
| T028 | reports | T024 | done | Ведомость: строка — сотрудник, колонки — компоненты, регистры одной таблицей с меткой и переключателем разреза; итоги считаются по видимому срезу. Готово, когда тест показывает: итоги сходятся с видимыми строками для роли с ограниченным доступом |
| T029 | reports | T013, T028 | done | Экран следа расчёта: от суммы до входных часов и версии правила. Готово, когда по следу можно воспроизвести сумму руками |
| T030 | reports | T028 | done | Отчёт расхождений «период против прошлого» с порогами на каждый компонент. Готово, когда тест на намеренно изменённых данных показывает подброшенное отклонение |
| T031 | reports | T028 | done | Сверка с таблицей бухгалтера: загрузка его файла и построчное сравнение с расчётом. Готово, когда сверка эталонной таблицы июня 2026 показывает совпадение и объясняет известные расхождения |
| T032 | reports | T028 | done | Три выгрузки: ведомость к выплате, строки для P&L с раздельными налогами, выгрузка в привычном бухгалтеру виде. Готово, когда файлы открываются в Excel и не содержат невидимых пользователю регистров |
| T033 | demo | T024 | done | Генератор демо-данных: два юрлица, три точки, тридцать сотрудников, все четыре схемы и все три регистра, два закрытых периода и один открытый. Готово, когда в демо работают и отчёт расхождений, и сверка |
| T034 | demo | T033 | done | Демо-база отдельным экземпляром, вход без регистрации, сброс пересозданием из шаблона по расписанию. Готово, когда сброс проходит без ручных действий и запись в демо не попадает в боевую базу |
| T035 | deploy | T008 | done | Локальный запуск: compose, `.env.example`, README с поднятием с нуля. Готово, когда чистая машина поднимает продукт по инструкции без вопросов |
| T036 | deploy | T035, T016 | done | Развернуть на MUSPELHEIM своим compose-проектом на портах 8030 и 5440, база с меткой для ночного бэкапа. Готово, когда смоук проходит **на сервере**, соседние контейнеры живы, а свежий дамп появился |
| T037 | deploy | T035 | todo | CI на каждый pull request: линт, тесты, сборка образа. Готово, когда падающий тест блокирует слияние **Отложено до сентября решением владельца (D050): CI не чиним, денег в него не несём, проверки гоняются локально перед слиянием.** |
| T038 | db | T008 | done | Сид тестовых данных для разработки: 32 выдуманных сотрудника из обезличенной фикстуры, точки и юрлица, один открытый период. До готового MVP настоящая таблица партнёра не загружается (D028). Готово, когда `manage.py seed_dev` наполняет чистую базу и расчёт проходит на ней |
| T039 | chores | T032 | todo | Приёмка MVP на настоящих данных: загрузить таблицу бухгалтерии июня 2026 в поднятый продукт и сверить с её же расчётом. Делается один раз, в конце, а не по ходу. Готово, когда ведомость в продукте совпадает с эталоном |
| T040 | chores | T028 | done | Заложить MCP-обёртку поверх HTTP API как отдельную задачу на будущее: в первую версию не входит (D030), но эндпоинты проектируются так, чтобы обёртка не требовала переписывания. Готово, когда в спеке API зафиксировано, какие вызовы MCP будет использовать |
| T041 | web | T008, T038 | done | Минимальный веб-каркас первой очереди: проект Django поднимается, dev-вход без регистрации, список периодов, страница периода. Без локализации, ролей и оформления — они в следующих очередях. Готово, когда страница периода открывается в браузере на данных сида |
| T042 | payrun | T041 | done | Расчёт периода из интерфейса: кнопка запускает движок на данных сида (правила пока из YAML, перенос в базу — вторая очередь) и показывает ведомость: строка на сотрудника, колонки на компоненты, итоги. Готово, когда месяц считается в браузере и суммы совпадают с прямым вызовом движка |
| T043 | chores | T042 | done | Сверить построенное первой очередью со спекой свежим взглядом, итерация 1: агент, который не строил, проходит сценарии как пользователь и проверяет критерии готовности. Каждое расхождение — отдельной задачей |
| T044 | web | T014 | done | Управляющий точки видит все точки: интерфейс обещает «точка NS1», а ведомость показывает все три — фильтра по `unit_ids` нет нигде. Либо фильтровать, либо не обещать. Найдено сверкой 2026-08-07 |
| T045 | db | — | done | Внутренний регистр не представлен в сиде ни одной строкой, поэтому сценарий его скрытия сид сам по себе не проверяет — при сверке пришлось вставлять строку руками. Добавить в `seed_dev` данные всех трёх регистров |
| T046 | deploy | — | done | Привести документы деплоя в соответствие: таблица переменных в `deploy.md` неполна (нет `DEV_LOGIN`, `DJANGO_DEBUG`, `DJANGO_ALLOWED_HOSTS`, `DODO_TEST_ADMIN_DSN`, `HEALTHCHECK_*`), в README нет таблицы переменных, которую требует конституция, а `healthcheck.py` и `deploy.md` расходятся в том, что отдаёт корень |
| T047 | db | T010 | done | `db.md` заявляет действующей гарантией непересечение периодов действия через `EXCLUDE`, а в схеме нет ни одного такого ограничения — гарантия описана, но не выполняется. Убрать из контракта до реализации либо сделать вместе с T010 |
| T048 | chores | — | done | `db/platform/postgres.sql` ссылается на удалённые `0001–0003.sql`, а «Порядок работ» в `CLAUDE.md` разъехался с `tasks.md` — два параллельных плана, один устарел. Свести к одному источнику |
| T049 | web | — | done | Шапка показывает бейдж «dev-вход» и ссылку на страницу входа даже при `DEV_LOGIN=0`, ссылка ведёт в 404: шаблон не смотрит на флаг |
| T050 | db | T004 | done | У `payslips` нет политики видимости регистров: поля `net`, `gross`, `tax`, `to_cash` посчитаны по всем регистрам сразу, и скрытый регистр восстанавливается вычитанием из видимых компонентов (issue #42). Сейчас обойдено дисциплиной в коде — ведомость собирается только из компонентов. Решить в базе: суммы в разрезе регистра либо отказ от суммарных полей. Готово, когда тест ролью `app_user` показывает: бухгалтер не может получить из `payslips` число больше суммы видимых ему компонентов |
| T051 | db | T008 | done | В `timesheets` негде хранить выплату наличными и ручную корректировку — движок принимает `cash_payout` и `manual_correction`, в схеме колонок нет, поэтому `to_cash` всегда 0, а сид теряет значения (issue #43). Корректировка со следом: кто и почему поправил (D025). Готово, когда расчёт периода с заданной корректировкой даёт компонент и пометку в `notes`, а выплата наличными попадает в `to_cash` |
| T052 | db | — | done | Функции контекста `app_tenant_ids()` и `app_visible_ledgers()` работают только потому, что их владелец обходит RLS: с обычным владельцем политика зовёт функцию, которой нужна та же политика, и Postgres падает с `stack depth limit exceeded` (issue #44, воспроизведено фактически). Сейчас условие выполняется случайно — в образе роль из `POSTGRES_USER` суперпользователь. Готово, когда требование к роли-владельцу проверяется тестом схемы или стартом, а не выясняется на площадке |
| T053 | core | T011 | done | Схема расчёта `none` у группы `couriers` движком не реализована: сид обходит её переопределением (issue #45). Либо реализовать в движке, либо убрать из пресета. Готово, когда сид не нуждается в обходе, а тест покрывает выбранный вариант |
| T054 | chores | — | done | `docs/furca/blocks/payrun.md` остался на старых именах регистров после переименования по D009 (issue #46) — документ описывает то, чего в коде уже нет |
| T055 | chores | — | done | `docs/furca/blocks/web.md` описывает `devauth.py`, которого больше нет: блок auth заменил переключатель пользователей настоящим входом. Найдено блоком auth при сдаче |
| T056 | web | T014 | done | След расчёта пересобирается на лету, а не хранится (issue #48): у закрытого периода объяснить сумму будет нечем, если правило с тех пор изменилось. Решать вместе с T023 (модель периода) — пока правила не менялись, расхождения не видно |
| T057 | db | T044 | done | Справочник сотрудников не отфильтрован по точке: управляющий NS1 видит всех 35 человек, включая чужие точки, — проверено диспетчером ролью `app_user` при приёмке волны 3. Спека требует «видит только своих людей» (spec.md, сценарий управляющего). Политики видимости точек поставлены на `units`, `employment_terms`, `timesheets`, `payslips`, `pay_components`, но не на `employees`. Готово, когда управляющий получает только сотрудников своей точки, а тест это показывает |
| T058 | deploy | — | done | Контейнер приложения накатывает миграции на старте, а роль приложения не имеет права на DDL (issue #50): пока новых миграций нет — стартует, первая новая уронит контейнер на площадке. Найдено блоком db при T052. Готово, когда миграции накатываются ролью, которая на это уполномочена, и это проверено запуском с новой миграцией |
| T059 | chores | — | done | Канонический список функций контекста в `docs/furca/blocks/db.md` не знает про `app_unit_ids()` и `app_unit_is_visible()`, добавленные блоком web в T044. Два блока правили одно место в один заход и разошлись |
| T060 | chores | — | done | Сверить построенное второй очередью со спекой свежим взглядом, итерация 2: агент, который не строил, проходит сценарии как пользователь и проверяет критерии готовности. Каждое расхождение — отдельной задачей |
| T061 | timesheets | — | done | Ввод часов не трогает базу взносов, а движок считает по ней взносы и бруто: ввёл 8,5 праздничных — «Итого» стало 96,50, «База взносов» осталась 88,00, и расчёт прошёл без предупреждения. В сиде равенство соблюдалось, после любой правки через интерфейс расходится. Правдоподобное неверное число без падения — нарушение принципа 1 конституции. Готово, когда база взносов либо пересчитывается, либо расхождение явно помечено и не даёт посчитать период молча |
| T062 | timesheets | — | done | Экран табеля падает 500-й, когда в базе нет правил на период: `PayrunRefused` вылетает наружу из `timesheets.views.grid`. На странице периода тот же случай обработан внятной плашкой с командой загрузки. При `DJANGO_DEBUG=0` человек увидит голое «Server Error». Готово, когда табель показывает то же объяснение, а не 500 |
| T063 | web | — | done | «Норма часов» на странице периода берётся как норма произвольного сотрудника (`values_list(...).first()` без `order_by`): бухгалтер видит 176,00, управляющий на той же странице того же периода — 88,00. Норма должна быть свойством производственного календаря страны (spec.md). Готово, когда число одинаково для всех ролей и совпадает с нормой месяца |
| T064 | auth | — | done | Права ролей (`permissions`) загружаются в принципала и больше нигде не проверяются: администратор сети, которому спека запрещает править данные расчёта, отредактировал ячейку табеля. Отказ на «Посчитать» он получает не по праву, а случайно — по набору регистров. Готово, когда роль без `payrun.calculate` получает отказ по праву при любом наборе регистров, а роль без `timesheet.edit` не может записать ячейку |
| T065 | db | — | done | Бухгалтер читает `payslips.ledgers` и поимённо узнаёт, у кого есть выплаты в закрытых от неё регистрах: суммы защищены, факт — нет. D023 требует «ни строк, ни следа». Готово, когда под бухгалтером `select ledgers from payslips` не содержит значений вне её набора |
| T066 | timesheets | — | done | Отказ при вводе мусора виден плохо: объяснение рисуется внизу документа (при 35 строках почти всегда за экраном), подсветка ячейки едва отличима, а в поле остаётся введённый мусор вместо прежнего значения — вопреки обещанию CHANGELOG. Готово, когда объяснение видно без прокрутки рядом с ячейкой и значение либо возвращается, либо явно помечено непринятым |
| T067 | deploy | — | done | `SEED_USER_PASSWORD` не передаётся контейнеру в `docker-compose.yml`: значение из `.env` молча игнорируется, приложение падает на умолчание `dodo-dev`. Тот же класс ошибки, что уже чинили для `DEV_LOGIN`. Готово, когда изменённый в `.env` пароль действительно меняет вход |
| T068 | chores | — | done | README расходится с продуктом: число тестов (77 против 253), в списке экранов нет табеля — главной фичи второй очереди, раздел «Что уже работает» описывает правила в YAML вопреки переезду в базу и не упоминает `manage.py load_presets`, сид описан как 32 сотрудника и три роли вместо 35 и четырёх |
| T069 | chores | — | done | Две записи CHANGELOG за 2026-08-07 предъявляют опровергнутые числа как действующие: «управляющий 58 строк на 1 830 846,13» (после T044 стало 24 и 891 373,32) и «для наличных и ручной правки теперь есть место в табеле» (место есть в схеме, ввода в интерфейсе нет). У других устаревших мест пометки проставлены, у этих — нет |
| T070 | chores | — | done | Сверить построенное со спекой свежим взглядом, итерация 3: проверить, что двенадцать расхождений итерации 2 действительно закрыты, и поискать то, что появилось вместе с починками |
| T071 | db | — | done | Бухгалтер по-прежнему получает поимённый список тех, у кого есть выплаты в закрытых от неё регистрах — теперь через `payslips left join payslip_totals ... where t.payslip_id is null`: на сиде 27 фамилий из 35. Дыра структурная: строка ведомости видна всем в тенанте, а итог виден только при доступе ко всем её регистрам, поэтому отсутствие итога само по себе выдаёт факт. Закрытие колонки `ledgers` (T065) убрало лишь названный вектор. Нарушает D023 «ни строк, ни следа». Готово, когда под бухгалтером такой запрос отдаёт либо все строки, либо ни одной, и это закреплено тестом — существующие тесты этот вектор не ловят |
| T072 | web | — | done | `permissions.has()` не вызывается ниоткуда: интерфейс предлагает действия, которые сам же отвергнет. Админ видит редактируемую сетку 35×6 и кнопку «Посчитать», хотя оба действия дадут 403; у управляющего то же с расчётом. Оба контура отказа работают правильно — это про то, что о запрете узнаёшь только после действия. Готово, когда роль без права не видит кнопку и получает сетку на чтение |
| T073 | timesheets | — | done | В `timesheets.views.cell` не обработан `PayrunRefused`, в отличие от `grid` (T062): если правила перестанут действовать между загрузкой страницы и досылкой значения, POST ячейки даст 500 вместо объяснения. Найдено чтением кода рядом с починкой, смоуком не подтверждено — сценарий узкий |
| T074 | chores | — | done | README снова разошёлся по числу тестов: написано 253, фактически 296. Правился в 17:35, а блоки починок в 18:06 и 19:15 добавили тесты и README не тронули — тот же инвентарь протух дважды за один прогон |
| T075 | timesheets | T020 | done | Курьеры: поддержать оба способа мерить работу — часы и сдельную (число доставок либо фиксированная сумма), выбор настройкой группы (D032, ответ владельца на Q011). Сейчас у группы `couriers` часы условны, а схема расчёта даёт бруто равным нето. Готово, когда в табель по курьерской группе вводится сдельная величина, расчёт по обоим способам даёт разные и объяснимые числа, и переключение способа не ломает закрытый период |
| T076 | auth | T022 | done | Право `unit.close` есть только у управляющего: закрыть часы точки сегодня не может ни директор, ни бухгалтер (D033). Управляющий в отпуске — период не закрыть никем. Найдено при приёмке T022. Готово, когда директор закрывает и открывает часы любой точки, у управляющего остаётся только своя, и на попытку закрыть чужую есть тест ролью `app_user` |
| T077 | web | T016 | done | Онбординг: человек, впервые открывший продукт, должен понимать, что делать дальше, без объяснений из чата. Пустые состояния экранов объясняют следующий шаг, а не показывают пустую таблицу; на первом входе видно, с чего начинается месяц (часы → расчёт → утверждение). Замечание владельца 2026-08-08 при первом живом просмотре. Готово, когда человек, не участвовавший в стройке, проходит месяц от входа до ведомости, ни разу не спросив, куда нажимать |
| T078 | deploy | T037 | todo | Включить защиту ветки `master`: слияние только через pull request с обязательной зелёной проверкой CI. Без этого пункт готовности T037 «падающий тест блокирует слияние» не достигнут — сам файл рецепта ничего не блокирует. Делается ОДНОЙ командой в конце стройки, а не сейчас: включённая защита остановит слияния диспетчера, которыми стройка и живёт. Команда записана в журнале блока `deploy`. Готово, когда pull request с намеренно сломанным тестом слить нельзя **Отложено до сентября решением владельца (D050): CI не чиним, денег в него не несём, проверки гоняются локально перед слиянием.** |
| T079 | timesheets | T076 | done | Смоук `tools/smoke_closing.mjs` протух: 25 проверок из 29. Он утверждает, что у директора нет кнопки закрытия часов, а право `unit.close` директор получил задачей T076 по решению D033 — то есть смоук проверяет отменённое правило. Передан агентом локализации, чужая территория, сам не чинил. Готово, когда смоук снова 29 из 29 и проверяет действующее правило: директор закрывает любую точку, управляющий только свою. Смоук, который падает, обесценивает набор так же, как плавающий тест |
| T080 | chores | — | done | `test_import_does_not_move_the_calculation` краснеет на определённом сочетании файлов в прогоне, причём воспроизводится и на коммите `3a42718`, то есть дефект старше локализации. Полный прогон обычно зелёный — значит тест зависит от порядка, как уже было с `test_the_report_of_each_role_matches_what_that_role_sees` (issue #65, причина была в выборке без `order_by`). Готово, когда причина названа и устранена, а не обойдена перестановкой тестов |
| T081 | web | T075 | done | След сдельного начисления называет цену единицы «ставкой за час», а при фиксированной выплате печатает «rate per hour None» (issue #72). Подпись живёт в `src/web/views.py`, то есть в чужом для блока timesheets месте — поэтому агент курьеров её не правил и честно передал. Готово, когда след сдельной строки называет величины своими именами: цена единицы, количество, а при фиксированной выплате ставки нет вовсе, а не «None» |
| T082 | payrun | T075 | done | Наблюдение при порче тестов: увеличение нормы часов всем сотрудникам меняет строку табеля, но НЕ двигает итог расчёта ни на копейку. Возможно, так и должно быть (у затронутых людей доплата до минимума задана ручной правкой и перекрывает эффект), но это не проверено, а выглядит как молчаливо игнорируемый вход. Готово, когда поведение объяснено тестом: либо норма действительно не влияет в этих схемах и это закреплено, либо найден дефект |
| T083 | chores | — | done | Сверить построенное со спекой свежим взглядом, итерация 4. Первые три итерации прошли внутри второй и третьей очередей; четвёртая и пятая очереди (отчёты, каркас, языки, онбординг, демо, админка, раскатка) не сверялись со спекой ни разу. Делает агент, который ничего из этого не строил: он щупает продукт как пользователь, а не читает код. Готово, когда каждое расхождение заведено задачей со ссылкой на пункт спеки, а итерация без расхождений записана в журнал явной строкой |
| T084 | web | T018 | done | Смоук `tools/smoke_permissions.mjs` протух: 34 проверки из 35 (issue #73). Ждёт фразы, которой при фоновом расчёте не бывает — расчёт стал асинхронным задачей T024, а смоук остался на синхронном. Тот же класс, что T079. Готово, когда смоук снова 35 из 35 и проверяет действующее поведение, а не отменённое |
| T085 | payrun | — | done | **ПЯТАЯ УТЕЧКА РЕГИСТРОВ, она же ложное число.** Блок «Данные этого месяца изменились после утверждения» показывает бухгалтеру 27 человек поимённо с суммами и названиями закрытых от неё регистров, и утверждает «расхождение 1487053.72», хотя не изменилось НИЧЕГО: директор, которому видны все регистры, блока не видит вовсе. Механизм в `src/payrun/retro.py`: `_stored()` читает компоненты через RLS (срез роли), `_fresh()` пересчитывает месяц движком целиком мимо видимости — разница равна ровно невидимым строкам. Нарушает D023 «ни строк, ни следа» и запрет конституции на правдоподобно-неверное молча. **Воспроизводится на демо-стенде**, титульная страница которого дословно обещает «the hidden part cannot be recovered by subtracting». Готово, когда роль видит расхождение только по своему срезу, а при отсутствии расхождений в её срезе блока нет вовсе, и это закреплено тестом ролью `app_user` |
| T086 | payrun | — | done | Отказы называют скрытые регистры по именам: «Расчёт не выполнен… недоступные вашей роли: internal, supplementary» (`check_ledgers` кладёт `refusal.ledgers`, шаблон печатает). Второй экземпляр — отказ переноса разницы: «Внутренний, Дополнительный». Названо не только имя регистра, но и факт, что у партнёра в нём есть данные (D023). Заодно коды не переведены и не подписаны. Готово, когда отказ объясняет, что делать, не называя недоступных регистров, и есть тест |
| T087 | auth | — | done | **Ответ владельца получен (Q012 → D036): бухгалтер и оперативный директор равны.** Бухгалтеру открываются все три регистра — тогда расчёт периода у него действительно проходит, а не отвергается базой. Механизм видимости не разбирается: он остаётся и сужается позже, там где надо. Тронуть надо и сид, и пресет ролей, и conftest, и проверки, которые ждали отказа бухгалтера ИМЕННО из-за неполного набора регистров — их надо переписать под роль, у которой набор неполный (управляющий), а не удалить: иначе защита останется без проверки. Управляющего точки решение не касается (D031). Готово, когда бухгалтер на стенде считает и утверждает период, а тесты ролью `app_user` по-прежнему ловят утечку на роли с неполным набором |
| T088 | reports | — | done | Выгрузка «Строки для P&L» у бухгалтера молча без налогов и взносов: у директора 34 строки, из них 6 налоговых на 951333.90, у бухгалтера 8 строк, все «Начисление», ни одной «Налог» или «Взносы». Причина законная (`payslip_totals` видны только роли со всеми регистрами), но человек соберёт P&L без налогов на зарплату и не узнает об этом — ни на странице, ни в файле нет ни слова. Спека обещает «раздельными начислениями и налогами — must». Готово, когда файл либо содержит налоговую часть, либо прямо говорит, что она не вошла и почему |
| T089 | web | T018 | done | Админка показывает 8 сотрудников из 35 и 3 группы из 6: у администратора `visible_ledgers = {official}`, а `directory.manage` есть ТОЛЬКО у него — значит ни один пользователь продукта не может открыть карточку курьера или кухни, поменять ставку, закрыть группу. Тот же тупик, что закрывали решением D033 для точек. Число «8» подано без оговорок: администратор не может узнать, что есть ещё 27 карточек. Готово, когда справочники ведутся полностью — либо правкой прав по образцу D033, либо решением владельца о том, кто ведёт справочники |
| T090 | web | T018 | done | Экрана правил расчёта нет вовсе, хотя спека даёт его как must: «Как администратор, хочу видеть и править правила расчёта как настройки с датами действия, чтобы не лезть в YAML». Права `rules.manage` и `roles.manage` не спрашиваются нигде в коде — только в сиде, миграциях и тесте. Задачи на это в графе тоже не было: пропущено при планировании. Готово, когда правила и их версии правятся с экрана, а `rules.manage` наконец имеет потребителя |
| T091 | timesheets | T075 | done | Курьеры: второй способ оплаты из продукта не включается. Форма группы в админке содержит `code`, `title`, `scheme`, `ledger` — поля способа работы нет; способ живёт переопределением правила `groups.couriers.work_measure`, а экрана правил не существует (T090). В табеле сида колонки способа нет вообще — ни ячейки, ни прочерка, хотя CHANGELOG обещает «у почасовых групп стоит прочерк». D032 выполнен в движке и не выполнен в продукте. Готово, когда способ переключается из интерфейса и табель показывает то, что обещано |
| T092 | web | T017 | done | Колонки ведомости и табеля остаются РУССКИМИ на английском и сербском: «Zaposleni, Objekat, Knjiga, Spor, Отработанные, Праздничные, Больничный…». Это главная таблица продукта. Обещано обратное: DoD MVP «непереведённых строк на экранах нет», CHANGELOG «интерфейс переведён на английский и сербский целиком». Механизм есть — в демо те же колонки английские через переопределения правил, — но к сиду продукта не применён. Готово, когда подписи видов часов и компонентов переводятся вместе с остальным интерфейсом |
| T093 | demo | T034 | done | Демо на английском содержит русское «и» в колонке порогов («5 % и 2,000.00»), смешивает разделители в одной строке (проценты с запятой «-9,1 %», деньги с точкой «84,840.00») и печатает фразу без глагола: «This partner the difference is carried over to the current period.». Демо — витрина для заказчика и портфолио. Готово, когда на английском экране нет русских слов, разделитель один на всю страницу и фразы собраны целиком |
| T094 | payrun | T025 | done | Список месяцев никогда не становится «Закрыт»: утверждение ставит `payruns.status = approved`, а `periods.status` остаётся `open`, и действия, закрывающего период, в продукте нет. На странице после утверждения — «Состояние: Открыт / Расчёт: Утверждён», в списке «Июнь 2026 · Открыт». В демо сид проставляет `closed` руками, то есть демо показывает состояние, до которого продукт своими силами не доходит, а онбординг обещает завершение цикла. Готово, когда цикл замыкается сам и список месяцев показывает правду |
| T095 | reports | — | done | Сверка говорит управляющему «в расчёте этого периода такой строки нет» про 16 строк, которые есть — просто на других точках. Выдаёт «не видно» за «не существует», то же правдоподобно-неверное, только словами. Готово, когда формулировка честна: строка не входит в ваш срез, а не отсутствует |
| T096 | web | — | done | Мелочи сверки, каждая проверяема: страница входа при `DEV_LOGIN=1` перечисляет анониму все три регистра в панели быстрого входа (на площадке выключено, но названия утекают до входа); в демо-отчёте расхождений строка «Total variances · 4 employees — 0.00» — алгебраическая сумма взаимно погасившихся отклонений, читается как «всё сошлось» над четырьмя расхождениями выше порога; след с подобранным чужим `?ledger=` отдаёт официальный срез, но подпись «разрез: …» исчезает, хотя на своих разрезах она есть |
| T097 | chores | — | done | Сверить построенное со спекой свежим взглядом, итерация 5. Четвёртая нашла 12 расхождений, одиннадцать закрыты (T085-T086, T089-T096), одно ждёт ответа владельца (T087/Q012). Пятая проверяет починки и ищет новое — особенно на поверхностях, появившихся после четвёртой: экран правил расчёта, переводимые подписи колонок, закрытие месяца, хранимый след. Готово, когда каждое расхождение заведено задачей со ссылкой на пункт спеки, а итерация без расхождений записана в журнал явной строкой |
| T098 | chores | — | done | Гигиена проверочной обвязки, четыре issue разом: #76 смоуки не изолированы (правка ячейки в одном красит следующие по контрольным числам), #79 тесты сверки и расхождений падают при запуске файла отдельно (Django настраивается побочно фикстурой), #80 упавший на полпути смоук прав оставляет базу без правил расчёта, #81 смоуки требуют высокого окна браузера и нигде это не сказано. Общая причина у всех — проверки зависят от состояния, которое сами же и портят. Готово, когда любой смоук и любой тестовый файл проходят в одиночку и не оставляют следов |
| T099 | deploy | — | done | `DJANGO_DEBUG=1` по умолчанию: на 404 человек видит техническую страницу Django с текстом исключения (issue #82). На площадке стоит 0, но умолчание в `.env.example` — то, с чем поднимут продукт локально и покажут коллеге. Готово, когда несуществующий адрес отдаёт человеческую страницу на всех трёх языках, а не трассировку |
| T100 | reports | — | done | **ШЕСТАЯ УТЕЧКА РЕГИСТРОВ.** Экран сверки, блок «Есть в расчёте, нет в таблице», выдаёт бухгалтеру полный поимённый состав скрытого от неё регистра — не вычитанием, а списком: на демо четыре человека, все `{internal}`, у управляющего свой. Формулировка «**в расчёте** этого периода» вдобавок утверждает о базе то, чего роли не показывали. Это тот же класс, из-за которого владелец сказал «демо никому не показываем» (D035), и виден он именно на демо. Смежная половина — табель называет тех же людей всем ролям (issue #57, открыт с 07.08 как осознанная позиция «табель по регистрам не режется»); решить надо обе, либо признать позицию и закрыть сверку. Готово, когда роль не получает имён из невидимого ей регистра ни на одном экране, и это закреплено тестом ролью `app_user` |
| T101 | db | — | done | **Заморозка работает по строке, которой роль не видит.** Бухгалтер (только `official`) морозит и размораживает строку внутреннего регистра — 302, запись проходит. Пара «302 против 404 на случайном id» — рабочий оракул существования: перебором выясняется, какие строки лежат в скрытых регистрах. Плюс официальная роль может молча пометить внутренние строки спорными, а спорная строка исключается из переноса разницы. Причина: у `payslips` политики `ledger_visibility` НЕТ вовсе (есть у компонентов, итогов и шагов следа), а политика заморозки смотрит только на точку — срез по регистру держит приложение, и на этом маршруте его забыли. Нарушает D014 («забытый фильтр даёт пустой результат, а не утечку»). Готово, когда запись и чтение по невидимой строке неотличимы от несуществующей, и это закреплено тестом ролью `app_user` |
| T102 | web | — | done | **Каталог переводов перепутан: инверсия смысла на главном экране цикла.** «Посчитан» → `not calculated yet`, «Утверждён» → `3. Approval`, «Открыт заново» → `Reopen`. На демо — а оно всегда английское, то есть это видит заказчик — август со статусом «посчитан» показывает `not calculated yet` и под этим полную ведомость на 47 строк. Один перевод отдан разным оригиналам 29 раз в `en` и 31 в `sr`; хуже всего досталось следу расчёта: «ставка налога» → `unit not found`, шесть разных строк про часы → одна `Standard hours`. **Источник — коммит bf598e5 от 09.08:** `makemessages` пометил записи `fuzzy`, подставив перевод от похожей старой строки, а пометку потом сняли, не исправив текст. Готово, когда каждый перевод соответствует своему оригиналу и есть проверка, что один перевод не отдан двум разным строкам |
| T103 | web | — | done | Мелочи следа и отказов, найденные сверкой: на экране следа печатается сырой объект Python — `часов {'sick': Decimal('20')}` вместо числа (в базе хранится нормально, не форматируется вложенное значение); отказ по регистру и схеме группы называет несуществующую дату `0001-01-01` и советует «возьмите дату позже», хотя **поля даты в этой форме нет** — совет невыполним; формат даты жёстко русский на нерусских страницах (`13.08.2026 15:10` рядом с `Aug. 13, 2026`); подписи `pnl_line` в пресете страны только по-русски; деньги в xlsx уходят двоичным float (`80756.32000000001`) |
| T104 | demo | — | done | Демо-файл таблицы бухгалтера отдаёт 404 бухгалтеру и управляющему (issue #88): ссылка на титульной работает анониму и директору, а ролям, ради которых экран сверки и существует, отвечает «страница не найдена» — да ещё текстом «demo period is not calculated» про посчитанный и утверждённый период. Причина: файл собирается из `collect_run`, а тот пропускает строки без `totals`, которые роли с неполным набором регистров не отдаются вовсе. Решить надо осознанно: файл по замыслу сделан ВНЕ продукта и от среза скачивающего зависеть не должен, но собрать его под полным контекстом — значит выдать официальной роли числа скрытых регистров (в демо люди выдуманы, и в этом смысл сценария, но это решение записывается, а не случается). Готово, когда все три роли получают файл, сверка на нём проходит целиком, а выбор зафиксирован решением |
| T105 | deploy | — | done | Статика не отдавалась при `DJANGO_DEBUG=0` (issue #68), и на раскатанной площадке это было живой поломкой: `grid.css`, `grid.js` и `htmx` отдавали 404, то есть экран табеля открывался, сетка рисовалась, а ячейка на сервер не уходила — человек вводил часы и уходил, ничего не сохранив. Закрыто: файлы отдаёт whitenoise самим приложением (без отдельного веб-сервера — файлов три), `collectstatic` уходит в сборку образа, а `WHITENOISE_USE_FINDERS` страхует от забытой сборки. Хешей в именах нет намеренно: манифест менял бы молчаливую поломку одной ячейки на громкую поломку всех страниц. Проверено фактически на стенде с `DJANGO_DEBUG=0` — три файла по 200, смоук табеля 17 из 17; тест краснеет при снятой прослойке |
| T106 | chores | T087 | done | Сверить построенное со спекой свежим взглядом, итерация 6. Поводов два, и оба весомее «положено по процессу»: (1) D036 поменял модель доступа для целой роли уже ПОСЛЕ пятой итерации — бухгалтер теперь видит и может всё, что директор, и это ломает предположения, на которых стояли предыдущие проверки; (2) пятая итерация нашла две утечки и инверсию смысла в каталоге, то есть источник находок ещё не иссяк. Проверять в первую очередь: не потерялся ли срез у роли, у которой он остался (управляющий), считает ли бухгалтер и утверждает ли месяц целиком, выгрузки, демо-сверку с файлом, статику при DJANGO_DEBUG=0. Готово, когда каждое расхождение заведено задачей со ссылкой на пункт спеки, а итерация без расхождений записана в журнал явной строкой |
| T107 | facts | — | done | Перенести схему фактов на миграции Django из `db/migrations/0004_facts.sql` (1159 строк готового и проверенного на живой базе SQL: таблицы `facts`, `source_documents`, `fact_batches`, типы, представления `pnl_lines`/`pnl_by_unit`/`pnl_by_network`/`facts_unallocated`, функции разнесения). Переносить, а не переписывать: схема уже проверена на накате с нуля и на разнесении до копейки. Политики RLS обязаны встать по образцу зарплатных — тенант, точки роли, регистры — и быть проверены ролью `app_user`, а не владельцем таблиц. Готово, когда схема накатывается на чистую базу одной командой в общем ряду миграций, лист остаётся один, а тесты доступа ловят порчу каждой политики |
| T108 | facts | T107 | done | Справочник статей расходов: код, название на трёх языках, строка P&L, действует с даты. Ведёт администратор сети (право `directory.manage`), остальные только читают. Пустой по умолчанию — наполняется с файла бухгалтера, а не выдумывается (Q015). Готово, когда статья заводится и правится с экрана, задним числом в закрытый месяц не правится, а расход без статьи не сохраняется |
| T109 | facts | T107, T108 | done | Внесение расхода из кассы веб-формой: дата, сумма, статья, комментарий, канал `cash`. Управляющему точка подставляется своя и **не подменяется запросом** — проверить именно подменой, а не глазами на форме. Бухгалтер и директор вносят по любой точке. За закрытый месяц запись идёт заменой (`revision`, `superseded_at`), а не правкой. Готово, когда расход вносится всеми тремя ролями по своим правилам, а попытка управляющего внести на чужую точку отвергается базой, а не только формой |
| T110 | facts | T109 | done | Список расходов: свои за месяц у управляющего с итогом, все с фильтрами по точке, статье и датам у бухгалтера. Правка и удаление до закрытия месяца, после — заменой. Готово, когда список показывает ровно свой срез каждой роли (проверено ролью `app_user`), а удалённое видно как заменённое, а не исчезает бесследно |
| T111 | facts | T107 | done | Разнесение расхода без точки по правилам: поровну, пропорционально выручке, на фиксированную точку; правило версионируется по датам. Факт без правила остаётся нераспределённым и **виден в отдельном списке**, а не пропадает. Готово, когда сумма детей сходится с родителем до копейки на неровных суммах (100.01 на три точки), а пересчёт на неизменившихся правилах не переписывает факты вслепую |
| T112 | facts | T109 | done | HTTP-эндпоинты внесения и чтения расходов по условиям секции «API и будущая MCP-обёртка» в `spec.md`: роль и тенант приезжают контекстом базы, срез по регистру — параметром, запись только POST и с причиной, отказ по невидимому неотличим от несуществующего. Готово, когда те же сценарии, что проходят с экрана, проходят запросом, и ни один не даёт роли больше, чем даёт экран |
| T113 | facts | T110, T111 | done | Расходы в выгрузке и в демо: строки расходов уходят в выгрузку «Строки для P&L» рядом с зарплатными и в тех же статьях; демо наполняется расходами на английском и показывает нераспределённый факт. Готово, когда выгрузка бухгалтера содержит обе части, а демо-сценарий проходит смоуком целиком |
| T114 | web | — | done | **ВОСЬМАЯ УТЕЧКА, и самая заметная: на главном экране месяца.** Отказ расчёта печатает управляющему точки поимённый список людей ЧУЖОЙ точки и СКРЫТОГО регистра — `dev-courier-1, dev-courier-2` (внутренний регистр, след и заморозка по ним честно дают 404) или `UROS ANDRIC` с чужой точки BG1. То же отдаёт `GET /periods/<id>/calculate/status/` любой роли: проверки доступа там нет намеренно (`src/web/views.py:517-526`, довод «задание чужого партнёра не видно политикам» верен про партнёра и неверен про точку и регистр), а `payrun_jobs.details` собирается по всему периоду. В `details` лежит `external_id` — на боевых данных это ЈМБГ. Плюс счётчик: управляющему статус пишет `total: 35`, его же страница — 17. Готово, когда ни отказ, ни статус, ни счётчик не выдают роли ничего сверх её среза, и это закреплено тестом ролью `app_user` |
| T115 | auth | — | done | Бухгалтер не может откатить утверждение периода и закрыть часы точки: в `roles.permissions` у неё нет `period.reopen` и `unit.close`, страница пишет «Откат периода не входит в права вашей роли», POST даёт 403. Противоречит и must-стори спеки дословно («хочу откатить утверждение с указанием причины»), и D036 («доступ равен оперативному директору»). Готово, когда бухгалтер откатывает утверждение с причиной и закрывает часы точки на стенде, а миграция открывает права и в уже живущей базе, не только в сиде |
| T116 | reports | — | done | След расчёта печатает числа, по которым сумма НЕ воспроизводится: `152,00 × 421,08 = 64 004,16`, а показано 64 004,92 (в базе ставка 421.085); `371,00 × 1,14 ≠ 421,08` (коэффициент 1.1350); «множитель нето → бруто 0,70» при 0.701 — тот самый 0,701, ради которого след и написан. Причина: `input_value` форматирует любой Decimal как деньги, до двух знаков. Там же ставка взносов 0.4505 подписана «ставка за час» (issue #72, #75). Готово, когда человек может сложить показанные числа и получить показанный итог, а подпись величины соответствует её смыслу |
| T117 | demo | — | done | Титульная демо противоречит D036 через пятнадцать строк после себя же: сверху «Enter as Accountant — sees official, supplementary, internal», ниже «an accountant sees only the official ledger» (зашито в `src/demo/templates/demo/landing.html:118`). Это первое, что читает смотрящий. Готово, когда на титульной нет ни одного утверждения о доступе, расходящегося с фактическим поведением демо |
| T118 | timesheets | — | done | Подсказка о подозрительных числах есть только на импорте, а на сетке — главном пути ввода — её нет вовсе: 400 часов при норме 88 сетка принимает молча, ни цвета, ни подписи. Спека даёт это must-стори («хочу видеть подсказку о подозрительных числах, чтобы ловить опечатки до расчёта»), а `_check_data` живёт только в импортёре. Готово, когда те же проверки работают на сетке, подсказка не мешает сохранить (это подсказка, а не запрет), и правило одно на оба пути ввода, а не две копии |
| T119 | reports | — | done | Собственная выгрузка «Вид бухгалтера» не читается собственной сверкой: загружаешь только что выгруженный файл — «лист не входит в формат PLATA», 0 сошлось, и 35 фамилий помечены «в загруженной таблице такой строки нет», хотя они в файле. Это ломает главный сценарий переезда (спека: «загружает свою же таблицу, система считает параллельно и показывает построчное сравнение»). Готово, когда файл, выгруженный продуктом, читается им же и сходится сам с собой построчно |
| T120 | reports | — | done | Сверка объясняет ноль сравнений неправдой про доступ: бухгалтеру со всеми тремя регистрами пишет «итоги расчёта вам не отданы», хотя после D036 отданы. Формулировка осталась от T100, где была верной. Готово, когда объяснение соответствует настоящей причине, а сама сверка у бухгалтера сравнивает суммы, а не пропускает их |
| T121 | rules | — | done | Правка задним числом по умолчанию (D020: закрытый период не переписывается, разница ложится в текущий) из продукта недостижима: правило расчёта, условия найма и календарь с датой внутри закрытого месяца отклоняются, а отказ советует переоткрыть период — то есть альтернативу, а не умолчание. Перенос дельты доказан только тестами, которые правят модель мимо интерфейса. Готово, когда правка с датой в закрытом месяце проходит с экрана и порождает помеченную строку в текущем периоде со ссылкой на исходный, а закрытый месяц не сдвигается |
| T122 | chores | — | done | Мелочи сверки: отчёт импорта относит строки к «Не сопоставлено» и тут же называет их поимённо (то есть сопоставил); README обещает «655 зелёных» при фактических 1005 passed, 8 skipped. Готово, когда обе цифры и обе формулировки соответствуют факту |
| T123 | timesheets | — | done | Две одновременные досылки одной ячейки роняют запись табеля 500-й: `duplicate key timesheet_days_uniq` (issue #61, поймано смоуком на обычном пути ввода). Человек видит белую страницу ошибки там, где просто быстро ввёл два числа подряд. Готово, когда повторная досылка той же ячейки не падает, а обновляет значение (upsert по ключу), и на это есть тест с настоящей гонкой, а не с последовательными вызовами |
| T124 | deploy | — | done | Рабочий процесс очереди не может ходить ролью продукта: `permission denied for table django_q_ormq` (issue #66), а миграции накатывает контейнер приложения, что на площадке без суперпользователя не пройдёт (issue #50). Две половины одной конфигурации: владелец схемы отдельно, приложение и очередь — логин-ролью без DDL. Сейчас на площадке обе роли совпадают, то есть разделение объявлено, но не проверено. Готово, когда стенд поднимается с РАЗНЫМИ ролями (миграции одной, приложение и очередь другой) и все смоуки на нём проходят |
| T125 | db | — | done | Схемная проверка политик слепа к пустым таблицам: политику `tenant_isolation` на `payrun_jobs` можно ослабить до `using (true)` — и ни один тест не покраснеет (issue #63). Проверка перебирает таблицы и молча пропускает те, где нет строк, то есть самые новые. Это дыра в самой обвязке, из-за которой утечку ловят люди, а не тесты. Готово, когда пустая таблица считается непроверенной и валит прогон с внятным текстом, а не пропускается |
| T126 | core | — | done | `seed_dev` не наливает базу, где расчёт остался утверждённым: удаление данных отклоняет заморозка периода (issue #62). Человек, который поднял стенд, посчитал и утвердил месяц, больше не может вернуть стенд к сиду — а именно этого требует договор смоуков (T098). Готово, когда сид приводит базу к эталону при любом состоянии периода, не ослабляя саму защиту закрытого периода |
| T127 | reports | — | done | Выгрузка «Строки для P&L» ставила всем начислениям служебную статью «Без статьи» (issue #95): статья искалась ОТОБРАЖАЕМЫМ именем строки («ФАМИЛИЯ ИМЯ»), а справочник собран по ключу сотрудника — совпасть они не могли в принципе. Налоговые строки спрашивали правильно, поэтому дефект выглядел как «статья есть, но не у всех», а не как поломка. P&L — цель всего продукта, и файл, где все начисления под одной служебной статьёй, для сборки бесполезен и молчит об этом. Закрыто: строка ведомости несёт ключ сотрудника рядом с именем, выгрузка спрашивает ключом. Проверено на стенде — 32 строки из 32 со своей статьёй, было 26 без |
| T128 | chores | T113 | done | Сверить третью очередь (ручной ввод и наличные) со спекой `docs/furca/spec-cash.md` свежим взглядом, итерация 7. Очередь построена за ночь пятью блоками и ни разу не проверялась чужими глазами: справочник статей, внесение расхода, список с фильтрами и карточкой, разнесение по правилам, нераспределённое, HTTP-эндпоинты, выгрузка вместе с зарплатой, демо. Отдельно проверить то, на чём этот продукт спотыкался восемь раз: не выдаёт ли роль с неполным набором регистров чужих строк, имён, сумм или счётчиков — ни на экране, ни запросом, ни в выгрузке, ни подобранным руками параметром. Готово, когда каждое расхождение заведено задачей со ссылкой на пункт спеки, а итерация без расхождений записана в журнал явной строкой |
| T129 | facts | — | done | **Замечание владельца по экрану, закрыто сразу:** под ведомостью стояли два ряда ОДИНАКОВЫХ кнопок — верхний менял вид на экране, нижний молча скачивал xlsx, а одна кнопка в нём открывала другой экран. Три поведения под одним видом; о скачивании человек узнавал по файлу в загрузках. Закрыто: у выгрузок стрелка вниз, пунктирная рамка и атрибут `download`, у сверки стрелка вправо, переключатель остался прежним. Тест проверяет не наличие класса, а РАЗНИЦУ между тремя видами — иначе он зеленел бы и в день, когда пометку повесят на всё сразу |
| T130 | facts | — | done | **УТЕЧКА И ЗАПИСЬ ЧЕРЕЗ СЕТЕВЫЕ РАСХОДЫ (сверка 7, находка 1).** Управляющий точки видит, ПРАВИТ и УДАЛЯЕТ расходы «на всю сеть», внесённые директором: на демо в его списке `Marketing campaign 150 000` и `Office rent 90 000`, итог «для сверки с кассой» 254 850 вместо 14 850. POST на карточку чужого сетевого расхода прошёл (999,99 → 1,00), удаление прошло, внесение расхода на всю сеть на 500 000 прошло. Причина: `p_unit is null — строка без точки: ничья, видна всем` в `app_unit_is_visible`, написано до появления сетевых расходов. Противоречит DoD спеки «управляющий видит только свою точку». Готово, когда сетевой расход не виден и не правится ролью с урезанным набором точек, и это закреплено тестом ролью `app_user` |
| T131 | facts | T130 | done | **Удаление сетевого расхода рвёт разнесение молча (сверка 7, находка 2).** `supersede_fact` снимает детей выборкой под политиками вызвавшего: управляющий удалил родителя 100,01 — снялся только его ребёнок 33,33, а 33,34 и 33,34 остались живыми в `pnl_lines`. Итог: 66,68 в P&L, чей источник помечен удалённым, и найти их нельзя ни на одном экране. Директор то же удаление делает целиком. Готово, когда снятие родителя снимает ВСЕХ детей независимо от того, кто снимает, либо отказывает целиком — половинчатого исхода быть не должно |
| T132 | facts | — | done | **Два правила разнесения из четырёх не работают, и продукт врёт про причину (сверка 7, находка 3).** Статья с правилом «Пропорционально выручке» или «Спрашивать каждый раз» → расход отвечает дословно «Правила разнесения у статьи нет», хотя правило есть и видно в карточке; кнопка «Разнести по правилам» при висящих 1 490,00 говорит «менять было нечего». Причина: выручки в продукте нет вовсе (придёт с коннектором Dodo IS, этап 6), а `ask` требует ручного выбора, которого нет. Готово, когда продукт называет настоящую причину («выручка появится с коннектором», «этот расход ждёт вашего решения»), а не отрицает существующее правило — либо правило нельзя выбрать до тех пор, пока за ним нечего исполнять |
| T133 | facts | — | done | Экран и запрос дают разное на один `?ledger=` (сверка 7, находка 4): `/expenses/?ledger=official` — 2 строки, `/api/expenses/?ledger=official` — 1, потому что экран этот параметр не читает вовсе. Спека требует обратного: «те же сценарии, что проходят с экрана, проходят запросом, и результат совпадает». Готово, когда срез по регистру на экране и в запросе один и тот же |
| T134 | facts | — | done | Мелочи третьей очереди, найденные сверкой: название статьи на `/expenses/unallocated/` не переводится («Аренда» на английском экране, где соседние зовут её Rent — берётся снимок `facts_unallocated.title`, а не название статьи); подсказка «Месяц уже закрыт» стоит в форме ВСЕГДА, даже когда не закрыт ни один месяц, а английский перевод при этом условный («If the month is already closed») — два языка говорят разное; кривое `?unit=`/`?item=` молча игнорируется и отдаёт полный список, хотя кривая дата в той же функции отказывает. Готово, когда каждая из трёх мелочей проверена глазами на английской странице |
| T135 | facts | — | done | Расход сегодняшней датой не попадает ни в одну выгрузку (сверка 7, находка 8): он ложится в период 2026-08, строки периода за август в продукте нет, а завести месяц нечем — на демо не видно, потому что там сид заводит три месяца. Готово, когда период заводится из продукта (или заводится сам при первой записи в него), и расход текущего месяца виден в выгрузке |
| T136 | facts | — | done | Нарушенное ограничение базы отдаёт человеку 500 вместо отказа формы (issue #109, того же класса #98). Повторный код статьи расходов — белая страница вместо «такой код уже есть», хотя код это то, «по чему статья сходится с файлом бухгалтера», то есть совпадения будут регулярными и именно у того, кто наполняет справочник с её файла. Соседние проверки той же формы отвечают понятным текстом и 400. Готово, когда уникальные ключи и внешние ссылки, нарушенные ВВОДОМ человека, превращаются в отказ формы на всех справочниках, а не поштучно на том экране, где заметили |
| T137 | reports | — | done | Разрез по регистру в выгрузке знает только регистры ВЕДОМОСТИ (issue #108): словарь разрезов собирается из того, что встретилось в зарплате, и расход в регистре, где зарплаты нет, разрезом не сужается — то есть в файл одного регистра приезжают чужие траты. Найдено при T113. Готово, когда список разрезов собирается из всего, что есть в периоде (зарплата и расходы), и файл каждого разреза содержит ровно свой регистр по обеим частям |
| T138 | deploy | — | done | Три дефекта обвязки, каждый молчит по-своему: `HEALTHCHECK_PORT` читается как порт на хосте, и стенд семь часов висел `unhealthy` при работающем продукте (issue #107); `docker-compose.yml` задаёт `name: maximus`, поэтому параллельные рабочие копии сносят друг другу базу (issue #51) — за прогон это чуть не случилось трижды; `test_import_does_not_move_the_calculation` зависит от порядка файлов в прогоне (issue #69). Готово, когда проверка здоровья краснеет только по делу, имя compose-проекта нельзя забыть, а тест проходит при любом порядке (проверить `-p no:randomly` и обратным порядком) |
| T139 | rules | — | done | Две правки, о последствиях которых продукт молчит: версия правила с датой в СЕРЕДИНЕ месяца на этот месяц не влияет вовсе, а у условий найма — влияет, и человеку об этом нигде не сказано (issue #99); правка производственного календаря за закрытый месяц задевает ВСЕХ партнёров страны, и ни правящий, ни соседи об этом не узнают (issue #100). Обе — про то, что человек уходит с экрана с неверной картиной. Готово, когда каждая правка говорит, на что она подействует и на что нет, теми же словами, что уже приняты для закрытого месяца |
| T140 | web | — | done | Мелочи текста и данных, накопившиеся в беклоге: находки разбора таблицы партнёра не переведены и на английской сверке выходят по-русски (issue #96); каталоги переводов держат записи для строк, которых в коде уже нет (issue #94); отказ по закрытому месяцу начинается со строчной буквы (issue #78); сид демо и сид разработки разошлись по регистрам администратора сети, а комментарий утверждает обратное (issue #91). Готово, когда каждая проверена глазами на нужной странице, а расхождение сидов закрыто одним источником, а не правкой обоих |
| T141 | reports | — | done | Выгрузка «Строки для P&L» у роли с неполным набором регистров молча без налогов (issue #90): у директора и бухгалтера 34 строки и 6 налоговых на 982 484,80, у управляющего 13 строк и НИ ОДНОЙ налоговой. Причина законная — итоги расчёта его роли не отданы, — но человек соберёт P&L без налогов на зарплату и не узнает об этом: ни на экране, ни в файле нет ни слова. Тот же класс, что закрывали в T088 для бухгалтера, только теперь про управляющего. Готово, когда файл и экран прямо говорят, что налоговая часть не вошла и почему, не называя при этом ни сумм, ни людей скрытого регистра |
| T142 | web | T136 | done | Приём «отказ базы словами формы» доделать до конца (issue #111, #112): экран правил расчёта его не получил вовсе, а формы точек, юрлиц и сотрудников отвечают на разбор ввода 200 вместо 400 — то есть человек видит сообщение об ошибке на странице, а всё, что смотрит на код ответа (смоук, обёртка, будущий бот), считает запись удавшейся. Менять надо вместе с существующей проверкой, которая этого 200 ждёт. Готово, когда отказ на всех формах справочников выглядит одинаково и снаружи, и человеку |
| T143 | timesheets | — | done | У правки часов в табеле нет автора (issue #52): кто поставил 176, система не ответит. Табель — первичный документ, по которому потом считаются деньги, и «кто это внёс» спрашивают ровно тогда, когда числа разошлись. Рядом issue #54: база для взносов, законно отличная от часов, не снимается из интерфейса — то есть законный случай приходится править мимо продукта. Готово, когда у каждой правки ячейки есть автор и время, это видно человеку, а база взносов задаётся с экрана |
| T144 | chores | — | done | Дока и мелочи, которые врут читателю: README описывает переменную `DJANGO_LANGUAGE`, которой в коде нет — язык читает `UI_LANGUAGE` (issue #110); журналы блоков db и auth утверждают, что `period.approve` и `period.reopen` ничего не решают, хотя с T025 это неправда (issue #59); панель точек в табеле не оформлена вовсе — ни одного правила стиля (issue #71). Готово, когда каждая правка сверена с кодом, а не с памятью, и панель точек выглядит частью продукта |
| T145 | facts | T109 | done | **Касса как справочник, и регистр расхода следует из неё** (D039, ответ на Q013). У точки бывает несколько касс: официальная и «чёрная»; человек не выбирает регистр, он берёт деньги из одной коробки или из другой. Значит касса — справочник со своим регистром и своей точкой, а у расхода поле «из какой кассы», из которого регистр приезжает умолчанием. Ручная правка регистра остаётся, но она перестаёт быть главным способом. Готово, когда касса заводится с экрана, расход берёт регистр из неё, а роль видит только кассы своих точек — проверено ролью `app_user` |
| T146 | facts | T145 | done | **НДС у расхода** (D042, ответ на Q016). Хранить сумму документа и ставку налога отдельно — в схеме фактов поля для этого есть; в P&L по умолчанию показывать сумму БЕЗ НДС. «Без НДС» — умолчание, а не единственный вид: партнёр, который налог не зачитывает, должен получить полную сумму. Готово, когда расход вносится с НДС, файл P&L показывает сумму без него, а переключение вида не требует пересчёта задним числом |
| T147 | facts | T108 | done | **Загрузка справочника статей файлом бухгалтера** (D041, ответ на Q015). Правка с экрана уже есть; недостаёт первичного наполнения: её список берётся файлом, дальше правится и дополняется в продукте. Готово, когда файл со списком статей загружается, повторная загрузка того же файла не плодит дублей (идемпотентность по коду), а строки, которых в файле нет, не удаляются молча |
| T148 | web | — | done | **Критический разбор вида и юзабилити по указанию владельца «лаконично и понятно сразу».** Диагноз: продукт объяснял вместо того, чтобы показывать — подзаголовок-абзац на каждом экране, три карточки-инструкции над содержимым, подсказка под каждым полем длиннее самого поля (форма из шести полей растягивалась на два экрана), фильтры столбиком на четверть экрана, шапка в две строки из-за трёх полных названий языков, главное действие ссылкой. Сделано: шапка в одну строку (языки кодами, полное название подсказкой), объяснение шага только у текущего, подсказка поля показывается при фокусе, форма в две колонки, фильтры в строку, главное действие кнопкой у заголовка, пояснение к расчёту под кнопками, ключ и значение рядом. Проверено снимками экранов до и после и смоуками |
| T149 | chores | T148 | done | Сверка свежим взглядом, итерация 8. После седьмой в продукт приехало много: кассы как справочник и регистр расхода из кассы (T145), НДС с показом без налога в P&L (T146), загрузка справочника статей файлом (T147), общий перевод отказов базы в отказ формы (T136), автор правки часов и база взносов с экрана (T143), и переделка вида всех главных экранов (T148). Ни одно из этого чужими глазами не проверялось. Проверять как пользователь: деньги (НДС, разнесение, выгрузки), доступ (кассы и точки у роли с урезанным набором), и отдельно — не сломала ли переделка вида что-то фактически: скрытая до фокуса подсказка поля, свёрнутая история, молчащая до наведения колонка спора. Готово, когда каждое расхождение заведено задачей, а итерация без расхождений записана в журнал явной строкой |
| T150 | suppliers | T107 | done | Контрагенты справочником: название, идентификатор у партнёра, действует с даты; ведёт администратор сети, остальные читают. Траты одного поставщика обязаны складываться, а не рассыпаться по разным написаниям названия — поэтому у строки трата ссылается на контрагента, а не несёт его имя текстом. Готово, когда контрагент заводится и правится с экрана, роль видит контрагентов своего партнёра и только их (проверено ролью `app_user`), а повторный идентификатор даёт отказ формы, а не 500 (приём T136) |
| T151 | suppliers | T150 | done | Счёт и платёж — разные события с разными датами: дата документа, дата денег и период учёта отделены друг от друга (в схеме фактов поля есть). Источник денег — справочником, как касса (D039, вопрос Q019): регистр приезжает оттуда. Оплата без счёта возможна — мелкая покупка не должна требовать бумаги. Готово, когда счёт вносится, оплата отмечается отдельно, строка попадает в P&L месяца учёта, а не месяца платежа, и это закреплено тестом на трёх разных датах |
| T152 | suppliers | T151 | done | Инбокс классификации: строки без статьи стоят одним списком с суммой и разбираются прямо в нём, не открывая по странице на строку. Пока строка стоит — её сумма есть по сети и нет ни на одной точке, и об этом сказано словами. Готово, когда инбокс показывает число и сумму, статья и точка назначаются из списка, а разобранная строка уходит в P&L |
| T153 | suppliers | T152 | done | Траты поставщикам в выгрузке, HTTP-эндпоинты по условиям спеки API и демо: контрагенты, неоплаченный счёт и непустой инбокс. Готово, когда выгрузка бухгалтера содержит три части (зарплата, наличные, поставщики), те же сценарии проходят запросом, а демо-сценарий проходит смоуком целиком |
| T154 | facts | — | done | **ДЕНЬГИ ОСТАЮТСЯ ПОСЛЕ УДАЛЕНИЯ (сверка 8, Н3).** Удаление расхода закрытого месяца ПОСЛЕ его правки — молчаливая пустышка: 302, ни слова, ничего не изменилось, строка на 700,00 живёт в августе, итоги не двигаются. Причина: `remove_expense` зовёт `storno_expense` с тем же ключом, который уже создала правка, `upsert_fact` видит ту же строку и ничего не делает, а исправленную строку никто не снимает. Идемпотентность, задуманная против двойного клика, сработала на другом действии. Готово, когда удаление после правки снимает ОБЕ строки (сторно и исправленную), а если снять нельзя — отказывает словами; молчаливого «ничего не произошло» на кнопке удаления быть не должно |
| T155 | web | — | done | Два молчаливых затирания (сверка 8, Н5 и Н1). «Завести месяц» в производственном календаре молча переписывает уже заведённый (176/22 → 99/9, 302, без единого слова) — единственный справочник, где занятый ключ не отказ, и календарь при этом ОБЩИЙ на страну: промахнувшийся администратор одного партнёра переписывает норму всем. Деньги при этом не поехали (норму держат табель и пресет), но узнать о правке нельзя. И загрузка справочника статей молча подменяет дату «Действует с» из файла умолчанием формы (01.06.2026 → 2020-01-01), хотя соседние негодные строки называет поимённо. Готово, когда занятый месяц календаря даёт отказ формы как у остальных справочников, а негодная дата в файле называется, а не проглатывается |
| T156 | timesheets | — | done | Табель приписывает базу взносов человеку, который её не ставил (сверка 8, Н6): правишь соседнюю ячейку — подсказка говорит «поставил Бухгалтер». Автор строки записывается на любой правке строки, а не только на правке самой базы. Это тот же класс, что T143 закрывал: «кто поставил 176» спрашивают, когда числа разошлись, и неверный ответ хуже отсутствующего. Готово, когда автор базы взносов меняется только тогда, когда меняется сама база |
| T157 | demo | — | done | Демо и тексты (сверка 8, Н7, Н4, Н8): демо не показывает ни кассу, ни НДС на том экране, куда приводит посетителя, и там же строка «Fuel paid from the till» с ПУСТОЙ колонкой Till — витрина противоречит сама себе; на карточке закрытого месяца пояснение к удалению описывает поведение открытого; инструкция к загрузке справочника переведена, а названия колонок в ней оставлены русскими — сербскому бухгалтеру она советует неверное. Готово, когда демо показывает кассу и НДС на первом же экране, а каждый текст соответствует тому, что произойдёт |
| T158 | web | T148 | done | **Своя регрессия из T148 (сверка 8, Н2).** Подсказки полей спрятали через `display: none` — а он выкидывает узел из дерева доступности целиком: для незрячего подсказки не существовало вовсе, и даже появившись по фокусу, она вставала отдельным текстом рядом, а не описанием поля. Плюс появление двигало форму на 22,6 px, и при обходе табом кнопка сохранения прыгала на каждом поле. Закрыто: подсказка всегда в дереве и связана с полем через `aria-describedby`, прячется прозрачностью, место под неё зарезервировано. Проверено в живом браузере — сдвиг кнопки 0, поле ссылается на подсказку, при фокусе она проявляется; тест краснеет при возврате `display: none` |
| T159 | web | T153 | done | **Гайд по продукту — внутри самого продукта, а не только в репозитории.** Сходу непонятно ничего: человек, открывший систему впервые, видит экраны и не понимает, что это, кем входить и с чего начать. Нужна страница «Как это работает», доступная с любого экрана: что за продукт и какую работу он снимает; роли и что каждая видит; сквозной сценарий месяца (табель → расчёт → ведомость → закрытие); наличные, расходы и поставщики; регистры учёта простыми словами; правка задним числом сторно; где живут правила и кто их меняет; выгрузка. Тексты на обоих языках. Готово, когда посетитель демо, впервые открывший продукт, доходит от титульной до закрытого месяца, ни разу не спросив человека |
| T160 | web | T159 | done | **Первый экран объясняет себя.** Пустой список и форма входа сегодня ничего не говорят новичку: на форме входа демо-стенда нет пути в демо (issue #116), пустые списки не подсказывают первый шаг. Готово, когда с формы входа демо-стенда есть вход в демо, а каждый пустой список говорит, что здесь появится и с чего начать |
| T170 | web | — | done | **Один человек — несколько ролей (D047).** Сейчас у пользователя ровно одна роль, и бухгалтер, который у партнёра ведёт весь проект, не может быть ещё и администратором: справочники ему запрещены, а выписать себе право нечем — экрана ролей нет (issue #77). Права должны складываться из всех ролей пользователя, и это должна проверять база, а не только форма. Готово, когда бухгалтеру выдана вторая роль администратора, он ведёт справочники и по-прежнему считает период, а тест ролью `app_user` подтверждает и запись строк всех трёх регистров, и доступ к справочникам |
| T171 | web | T170 | done | **Экран ролей — issue #77, право `roles.manage` без потребителя.** Нужен список ролей с их регистрами, точкой и правами и выдача/снятие права тем, у кого `roles.manage`; изменение проверяется базой, а не только формой; в следе видно, кто и когда менял. Готово, когда роль без права на расчёт получает его с экрана и сразу может считать, а роль без `roles.manage` этого экрана не видит вовсе (D023) |
| T172 | web | T171 | done | **Отказ ведёт к действию, а не в стену.** Сейчас страница говорит «Попросите того, у кого это право есть» — тому, у кого `roles.manage`, она должна предлагать открыть роли и выдать право. Готово, когда с экрана отказа администратор доходит до нужного права в два клика |
| T173 | web | — | done | **Управляющий точки проверяет своих сотрудников (D047).** Ему нужны имена, должности и ставки людей своей точки — на чтение. Ставок в Dodo IS нет вовсе (это условия найма, наши данные), поэтому проверить их можно только у нас. Готово, когда управляющий видит карточки сотрудников своей точки и не видит чужих, что подтверждено тестом ролью `app_user` |
| T174 | suppliers | T153 | done | **Управляющий скидывает накладные и чеки (D047).** Его роль в сборе первички — не считать, а донести бумагу: накладная поставщика и чек с точки. Входит в четвёртую очередь как отдельный сценарий: внесение с точки, разбор и проверка бухгалтером, а не прямая запись в P&L. Готово, когда управляющий вносит накладную и чек своей точки, они попадают в инбокс классификации, и до подтверждения бухгалтером в P&L их нет |
| T161 | deploy | — | done | **Публичный вход на стенд через Tailscale Funnel — временное решение, делается ВПЕРЁД текущей очереди (D045).** Стенд на MUSPELHEIM живёт с T036, но доступен только внутри Tailscale: показать продукт некому, а обкатка сейчас первоочередная. Funnel даёт постоянный публичный HTTPS-адрес `<машина>.<tailnet>.ts.net` — без покупки домена, без открытия портов роутера, без доступа к ноуту владельца. Что нужно: атрибут `funnel` в ACL сети и включённые HTTPS-сертификаты; публикация с одного из разрешённых Funnel портов (443/8443/10000) на 8030; адрес в `DJANGO_ALLOWED_HOSTS` и `CSRF_TRUSTED_ORIGINS`; `DJANGO_DEBUG=0` и `DEV_LOGIN=0` — на публичном адресе вход без пароля отдаёт продукт кому угодно; `noindex`, чтобы стенд не попал в поиск; служба поднимается сама после `shutdown /g`. **Пароли учёток сменить ДО включения Funnel:** пока стенд за Tailscale, на нём стоит `admin`/`admin` (поставлено по просьбе владельца 2026-08-18 для обкатки) — за публичным адресом такая пара находится сканерами за часы, а у роли `admin` права на справочники, правила и роли. Готово, когда адрес открывается с телефона по мобильному интернету (вне Tailscale): вход работает, статика отдаётся, dev-вход недоступен, ни одна учётка не имеет пароля из словаря, а после перезагрузки сервера адрес живёт без ручных действий **Сделано 22–27.08.2026** (`c713187`, `feef7f4`): наружу опубликовано ДЕМО на 8443, продукт (8030) не публикуется вовсе — демо изолировано своей базой и тестовыми данными, продукт нет. Подтверждено фактом, а не памятью: `python tools/stand.py` показывает, что `:8443` отдаёт наше демо, а корень `:443` занят чужим проектом и не трогается. Требование про пароли из словаря снято тем же решением: на публичном адресе стоит демо со своим входом (`demodemo`), а не продукт с учётками партнёра |
| T162 | chores | — | todo | **Онбординг владельца в продукт: живая экскурсия по системе.** Владелец 2026-08-18: «я сейчас не понимаю как она работает, где какие функции, соответственно партнеру мы явно не сможем передать то что есть». Пройти вместе весь месяц на стенде: кем входить, где справочники и правила, табель → расчёт → ведомость → закрытие, наличные и расходы, регистры, выгрузки. Задача не про код: её результат — понимание владельца и уточнённый список расхождений «что ожидал / что есть», каждое из которых заводится отдельной задачей. Делается на публичном стенде (T161). Готово, когда владелец сам проводит месяц от табеля до закрытого периода без подсказок, а расхождения из экскурсии заведены задачами |
| T163 | stands | — | done | **Демо приводит посетителя ролью, которой не видно половины продукта.** Кнопка «Enter the demo →» входит директором (`DEFAULT_ROLE` в `src/demo/views.py`), а `directory.manage` и `rules.manage` есть только у `admin` (`src/core/roles.py`) — поэтому владелец, открыв демо, не увидел «ни групп, ни точек, ничего»: пункты «Справочники» и «Правила» в шапке просто не появляются. Права разведены по T089, но у партнёра бухгалтер и директор равны (D036), а «запрещать меньшее тому, кому разрешено большее» уже признано тупиком (D033). Решить: дать ведение справочников и правил тем же ролям, что ведут месяц, либо на демо объяснять, что раздел ведёт другая роль, и давать переключиться. Готово, когда посетитель демо с первого экрана попадает туда, где видны сотрудники, группы и точки, и ни один раздел не пропадает молча |
| T164 | web | — | done | **Сотрудника нельзя завести из интерфейса, а схему расчёта выбирают текстом.** Три дыры одного узла, все подтверждены ревизией 2026-08-18: (1) заведения сотрудника нет намеренно (D029 — карточки приезжают загрузкой таблицы партнёра), но передавать продукт партнёру так нельзя: человек выходит на работу в середине месяца, и до следующей загрузки его в системе не существует; (2) `scheme` в карточке — свободное текстовое поле (`directory_views.py`), надо знать ключ из YAML (`standard`, `half_time`, `half_time_min_base`), списка и валидации нет; (3) «часовая или окладная» — это `work_measure`, он настраивается только на группу и только правом `rules.manage`, персонально сотруднику не задать. Пересмотреть D029. Готово, когда сотрудника можно завести, уволить и задать ему схему расчёта выбором из списка, ни разу не открыв YAML |
| T165 | rules | — | done | **Правила настраиваются не на тех уровнях, которые обещаны.** Экран `/rules/` правит только слой партнёра: `SCOPE = "tenant"` зашит в `src/web/rules.py`, хотя `RuleOverride` умеет `group` и `employee`, а сборка пресета читает все четыре уровня — значит коэффициент ночных часов конкретной группе или сотруднику с экрана не задать. Плюс тело пресета страны (ночные часы, больничные, ставки взносов — база для всех партнёров страны) правится только YAML и `manage.py load_presets`: владелец ищет эти правила в продукте и не находит. Готово, когда переопределение ставится на любом из четырёх уровней с экрана, а страновые правила видны и правятся тем, кто вправе, без доступа к серверу |
| T166 | reports | — | done | **Карточка сотрудника не показывает его денег.** Сегодня карточка — только история версий условий найма; сумм по месяцам нет ни на одном экране, `Payslip` нигде не фильтруется по человеку. Владелец 2026-08-18: «нужен будет функционал чтобы можно было посмотреть статистику по отдельному человеку». Нужна история выплат по периодам: компоненты, часы, начислено / на руки / налоги, динамика — с соблюдением видимости регистров смотрящего. Готово, когда по человеку видно все его месяцы, а роль с урезанными регистрами не получает в этих итогах скрытых сумм |
| T167 | reports | T166 | todo | **Аналитика по кадрам.** Владелец 2026-08-18: «также нужен будет функционал… и в целом аналитику по кадрам». Численность, движение (приход и уход), ФОТ и его доля по точкам и группам в динамике по месяцам. Новая продуктовая область: до сих пор каждый отчёт был срезом одного периода, сквозного по времени нет ни одного. Требует проработки до реализации — что считать текучестью, от чего берётся доля ФОТ, какие разрезы нужны. Готово, когда есть экран с динамикой по месяцам и разрезами по точке и группе, а числа сходятся с ведомостями тех же периодов |
| T168 | visual | — | done | **Интерфейс вразнобой и нечитаем — нужна визуальная система.** Владелец 2026-08-18: «также нам явно надо проработать визуал. сейчас весь интерфейс вразнобой, нечитаемый». Экраны собирались очередь за очередью, и каждая несла своё оформление: CSS живёт и в `base.html` (370 строк), и в отдельных шаблонах, единых токенов, типографики и сетки нет. Нужны токены (цвет, шрифт, отбивки, радиусы), типографская шкала, единые таблица / форма / кнопка / плашка, читаемая плотность чисел, состояния фокуса и наведения. Продукт показывают заказчику — «вразнобой» стоит доверия. Готово, когда на всех экранах одна сетка и одна типографика, числа выровнены по разряду, ни один экран не несёт собственных цветов, а обход табом виден **Сделано блоком `visual` (T176–T179 и далее, 20+ коммитов).** Из задачи выросла дизайн-система как источник истины — эталон пришёл в проект и стал главнее спеки (решение владельца 27.08.2026). Держится тремя сторожами, а не словами: `tests/test_visual_tokens.py` (ни одного цвета мимо токенов), `tests/test_no_unstyled_classes.py` (класс в разметке обязан быть где-то описан), `tests/test_appbar.py` (25 проверок шапки и фокуса на настоящих страницах). Остаток по этому направлению живёт отдельными задачами: тёмная тема, словарь классов, ведомость |
| T169 | stands | — | done | **Форма ролей из кода не доезжает до поднятых стендов, и молча.** Роли — данные: их создаёт `seed_dev` один раз при пересоздании тенанта, поэтому правка `ROLE_SHAPES` в `src/core/roles.py` не меняет уже работающую базу ничем — ни миграцией, ни при старте, ни предупреждением (issue #126). Стоимость уже заплачена: T089 закрыл «админ видит 8 сотрудников из 35», а на стенде роль `admin` осталась с одним официальным регистром, и владелец, открыв продукт 2026-08-18, принял дефект за устройство системы — отсюда и «в демо не вижу ни групп, ни точек», и «у директора нет доступа к настройкам». Роли на стенде приведены к коду вручную (проверено смоуком: 35 сотрудников, 6 групп, 3 точки), механизма нет. Готово, когда изменение `ROLE_SHAPES` доезжает до поднятого стенда одной идемпотентной командой с дифом, а расхождение роли в базе с кодом обнаруживается проверкой, а не глазами владельца |
| T175 | chores | — | todo | **Доступ к Dodo IS API: завести приложение и получить `Client Id`/`Secret` (issue #132).** Организационный шаг, зависит от владельца и учётки партнёра, а не от кода; доступ ничем не заблокирован (D043). В форме кабинета выбор из двух потоков — `Authorization Code Flow` и `Device Flow`, сервисного токена там нет (проверено на живой форме 2026-08-21); рекомендация — Device Flow, потому что адреса возврата в форме нет, а наш публичный адрес временный (D045). Разбор и все проверенные факты — `docs/dodo-is-api.md`. Готово, когда часы за реальный период приезжают живым вызовом коллекцией Postman — до всякого кода, вместе с ответом про лимиты запросов и глубину истории |
| T176 | visual | — | done | **Токены дизайн-системы как статика продукта.** Перенести `tokens.css` эталона (165 переменных) в статику и подключить в `base.html`; значения взять как есть — эталон для этого и делался. Тёмную тему завести вторым набором значений, переключатель не делать. Готово, когда файл отдаётся при `DEBUG=False` и число переменных в продукте совпадает с эталоном. |
| T177 | visual | T176 | done | **Стили из `base.html` переезжают в файл и на токены.** Сейчас 336 строк CSS лежат инлайном в шаблоне (строки 29–365), отдельный CSS-файл в продукте один. Вынести в файл, значения переписать на токены. Готово, когда в `base.html` нет `<style>`, все тесты зелёные, и в шаблонах не осталось литеральных цветов (проверять поиском `#[0-9a-f]{3,6}` и `rgb(`). |
| T178 | visual | T177 | done | **Каркас и шапка по эталону** (раздел 08 «Этап 2»): имя продукта, тенант, роль, навигация. Оговорка: компонент эталона нарисован под несколько наборов прав рядом, а владелец 21.08 решил — у человека **одна** роль; шапка показывает одну. Расхождение разбирается в issue #130, самому не решать. Готово, когда каркас совпадает с эталоном по сетке и типографике, обход табом виден, на 768 не рассыпается. |
| T179 | visual | T177 | done | **Числа читаются столбиком** — первый принцип эталона: правый край, моноширинное, табличные цифры; ноль это цифра, отсутствие значения — прочерк, и путать их нельзя. Применить к ведомости, P&L, расходам, счетам. Готово, когда суммы выровнены по разряду, ноль и прочерк различимы, и это закреплено тестом на разметку хотя бы одной таблицы. |
| T180 | stands | — | done | **Стенд в полном простое жжёт CPU ноутбука (issue #190).** Замер `docker stats` 2026-08-27 при пустой очереди: рабочий процесс очереди 6,08 % CPU и база 10,15 %, при том что веб и демо, которые реально отвечают на запросы, стоят на 1 %. Причина в `Q_CLUSTER` (`src/config/settings.py:247`): `poll` не задан, а умолчание `django-q2` — 0,2 с (`django_q/conf.py:92`), брокер — сама база. Это 5 запросов в секунду круглосуточно, ~432 тыс. в день и 5,76 ГБ трафика базы за неделю аптайма — на ноутбуке с закрытой крышкой, где рядом живут 14 контейнеров чужих проектов с 0,00 % в простое. Запас есть: `PAYRUN_QUEUE_STALE_SECONDS` = 10 с, то есть опрос можно замедлить до 1–2 с, ничего не сломав. Заодно посмотреть интервал проверок здоровья: 15 с у трёх контейнеров означает 12 запусков Python в минуту без нужды. Готово, когда CPU в простое замерен до и после **числом**, а фоновый расчёт периода по-прежнему подхватывается рабочим процессом (проверено постановкой задачи, не рассуждением) |
| T181 | stands | — | done | **Продукт и публичное демо работают на сервере разработки (issue #191).** `app` и `demo` запускаются `runserver` (`docker-compose.yml:161` и `:307`), а демо при этом опубликовано в интернет через Tailscale Funnel. Django о `runserver` говорит прямо: не для эксплуатации. Практическая цена: автоперезагрузчик непрерывно опрашивает файловую систему, хотя код на площадке между деплоями не меняется (часть расхода из T180); нет управления рабочими процессами, тайм-аута запроса и graceful reload; упавший поток не восстанавливается. Не дыра: `DJANGO_DEBUG=0` и `whitenoise` проверены фактом, трассировок наружу нет. Локальный запуск обязан остаться на `runserver` — автоперезагрузка нужна при разработке, значит разница между машиной и площадкой живёт в `.env`, как уже сделано с портами (D006). Перед реализацией — ресерч: gunicorn против uvicorn+ASGI для Django 6.1, число воркеров под ноутбук, поведение с `ATOMIC_REQUESTS=True`. Готово, когда смоук проходит **на площадке** (вход, период, расчёт, статика и форма на публичном адресе — CSRF за прокси самое вероятное место поломки), CPU в простое упал числом, README и таблица переменных обновлены тем же изменением |
| T182 | suppliers | — | done | **Накладная разносится по точкам руками (issue #174, модуль эталона 15 «Чья накладная»).** Правило отвечает на вопрос «этот поставщик всегда делится так-то», а конкретную накладную привозят на две пиццерии в долях, которых ни одно правило не знает. Сделано: экран «Чья накладная» с долями по точкам, заготовки «Поровну» и «По выручке», отказ словами, когда доли не дают ровно 100% (приём взят у ERPNext `cost_center_allocation`), функция базы `split_fact_by_hand` — копейки раскладываются тем же способом, что в `allocation_plan`, поэтому дети всегда складываются в родителя. Проверено вживую в браузере: 50+30 отвергнуто фразой «не разнесено 20%», 60/40 дало −60,01 и −40,00. **Развилка Q028 рядом, но не блокирует:** эталон говорит «до разнесения накладная в P&L не попадает», у нас она входит служебной строкой и видна числом. Остаток модуля вынесен в T204 и T205 |
| T183 | payrun | — | done | **Список периодов отвечает, что за месяц, не открывая его (issue #176, модуль эталона 4).** Было три колонки — месяц, состояние, партнёр. Стало: часы, ФОТ, доля от выручки, «что можно делать» и метка «переоткрывали». Часы берутся из `timesheets.hours`, а не из дней табеля: дни заполняются только при вводе по датам, а итог месяца есть у каждого табеля — по дням картина была бы пуста ровно там, где данные пришли импортом (проверено на стенде: 35 табелей, ноль дней). Доля от выручки — прочерк, пока выручки нет вовсе: ноль читался бы как посчитанный ответ. Метка переоткрытия живёт у расчёта (`payruns.status`), а не у периода, и остаётся навсегда — требование эталона дословно. 6 новых тестов, проверено вживую: июнь показывает 4562 часа |
| T184 | money | — | done | **Наличные показывают разрыв «касса / P&L» и чек к расходу (issue #177, модуль эталона 6).** Сделано блоком `money`: три числа наверху реестра — сколько ушло из кассы, сколько принято в P&L за тот же месяц и разрыв между ними с названной причиной; фотография чека цепляется к расходу тем же движением, а строка без бумаги помечена и посчитана. Без чека расход всё равно записывается: деньги уже отданы, и терять запись из-за неудачного снимка нельзя. Таблица `cash_receipts` заведена отдельно от файлов документов — у чека другая жизнь. Агент оборвался на лимите до финальной проверки; диспетчер довёл хвосты (переводы, значение для сторожа схемы) и принял по прогону |
| T185 | facts | — | done | **Экран P&L: отчёт, раскрытие строки, честная неполнота (issue #183, модуль эталона 5).** До этого P&L существовал только выгрузками строк: числа были, отчёта не было. Сделано: отчёт за месяц рядом с прошлым, раскрытие любой строки до первичных фактов (тот же приём, что след расчёта — D025), плашка «отчёт неполный» с суммой неразобранного и путём в инбокс. Считает база (`pnl_lines`, `security_invoker`), поэтому срез роли — та же RLS: управляющий видит свою точку, и это проверено числом, а не наличием фильтра. Итог — сложение показанных строк, второй выборки нет. Проверено вживую: расход 12 000 против 9 000 в мае, разница −3 000, раскрытие показывает сам факт. 10 новых тестов. **Консолидация в EUR вынесена в T209** (нужны курсы), **перенос ведомости в факты — в T208** (issue #201): без него в отчёте нет зарплаты |
| T186 | reports | — | done | **Ведомость по эталону: ярус групп над колонками (issue #163, модуль 9).** Липкая колонка имени и строка итогов были перенесены раньше блоком `visual` — оставался третий пункт. Сделано: шапка в два яруса, группы приходят из `payrun/sheet.py` готовыми (что чем считается — устройство ведомости, а не решение разметки), имя и «Итого» проходят через оба яруса. **Группы у нас другие, чем в макете, и это не отступление:** эталон рисует «Начисления» и «Удержания», а удержаний в расчёте нет ни одного компонента — налог и взносы производные и колонками не приходят. Пустая группа обещала бы колонки, которых нет; третья появится вместе с первым удержанием. Проверено фактически: «Оплата часов» накрывает 4 колонки, «Надбавки и корректировки» — 3. 4 новых теста |
| T187 | reports | T186 | done | **Печатные формы: платёжная ведомость и расчётный листок (issue #184).** Сделано блоком `reports`: отдельный `print.css`, суммы прописью (`src/reports/words.py`), листок объясняет, что бруто — не то, что человек получает на руки. 73 теста. Живой смоук печати агент начать не успел (оборвался на лимите) — проверка «печатается в А4 без обрезки» остаётся за следующим заходом, задача T212 |
| T188 | roles | — | done | **Админка: человека не пригласить, роль выдаётся молча и навсегда (issue #178).** Модуль эталона 11 «Админка»: приглашение по почте, срок действия доступа, история выдач и снятий. Сейчас человек заводится только в базе, а выданная роль не оставляет следа. Готово, когда партнёр приглашает сотрудника из интерфейса, роль снимается тем же экраном, а история «кто кому что выдал и когда» видна и не переписывается |
| T189 | roles | — | done | **Точку нельзя перенести в другое юрлицо (issue #179).** Модуль эталона 11: связь точки с юрлицом одна на всю историю, а у партнёра пиццерия переходит между юрлицами, и закрытые месяцы должны остаться как были. Готово, когда связь версионируется по датам (D020), перенос не меняет ни одного закрытого месяца, а отчёт за прошлый период показывает точку под тем юрлицом, где она тогда была |
| T190 | core | — | done | **Платформенной админки нет: партнёр заводится только руками в базе (issue #193, D065).** Эталон этого не рисует — поверхность платформенная, а не продуктовая, и отдельная от админки партнёра (модуль 11): там администратор партнёра ведёт своих людей, здесь владелец платформы ведёт сами пространства. Устройство иерархией: список пространств → внутрь пространства → его сотрудники → выдача ролей, плюс место под статистику по пространствам (сотрудники, заходы). Предусловие вынесено в issue #221: создать пространство сегодня нельзя ниоткуда, кроме как владельцем схемы — на `tenants` политика только на чтение, `insert` на `users` роли приложения не выдан, и путь записи надо спроектировать. Готово, когда новый партнёр заводится из интерфейса платформенным администратором и сразу может войти, роли его сотрудникам выдаются там же, а данные соседнего партнёра ему не видны (проверено ролью `app_user`). **Сделано 31.08–02.09.2026:** доступ в базе (`0261`, узкая дверь на четыре таблицы), `core/spaces.py` — заведение пространства одной транзакцией вместе с ролями и первым человеком, экраны `/platform/` и `/platform/<id>/`, статистика по пространствам (люди, действующие, последний вход), 32 проверки в двух файлах — базы и экрана. Приглашение человека по почте вынесено отдельной задачей: сейчас роль выдаётся по идентификатору |
| T191 | money | — | done | **У статьи расхода появилось «где выбирается» (issue #180, модуль эталона 14).** У каждой статьи сказано, где её предлагают: наличные расходы, разнесение накладных, разбор выписки. На телефон управляющего попадают только помеченные «наличные расходы» — сорок строк никто не читает. Право видеть статью это не ограничивает: в справочнике видны все |
| T192 | rules | — | done | **Правила страны без рамки (issue #182).** Модуль эталона 17 «Правила страны»: значение уровня партнёра или юрлица не может быть мягче страны, и продукт это показывает и отвергает. Сейчас юрлицо ставит что угодно, и никто не останавливает. Готово, когда рамка страны задана в пресете, попытка выйти за неё отвергается словами (а не молча принимается), а на экране правил видно и рамку, и текущее значение |
| T193 | visual | — | done | **Тёмная тема включается человеком (issue #164, страница эталона «Тёмная тема»).** Токены `[data-theme="dark"]` были перенесены вместе с дизайн-системой, ручки не было. Сделано: переключатель в шапке рядом с языком, три состояния — светлая, тёмная, «как в системе» (умолчание: пока не попросили, продукт не спорит с настройкой машины). Выбор хранится в cookie и ставится **сервером** в атрибут `<html>`: выбранная скриптом тема применяется после загрузки, и человек видит вспышку светлого перед тёмным. Значение проверяется списком — оно уезжает прямо в разметку. Проверено вживую: фон #EDEFF2 → #12161C, текст инвертирован, на четырёх экранах ни одного светлого пятна. 6 новых тестов |
| T194 | web | — | done | **Разделы названы словарём эталона и живут в одном месте (issue #162, модуль 10).** Было «Периоды · Расходы · Счета · Сотрудники» — слова про механику продукта; стало «Табель · Наличные расходы · Инбокс документов · Справочники · Правила расчёта · Роли и права» — слова про работу человека. Словарь вынесен в `web/navigation.py` и сверяется тестом со словами **из самого эталона**, а не из копии в тесте. Раздел первички ведёт в инбокс, а не в список счетов: работа с бумагами начинается с очереди разбора. Пунктов, которых в продукте нет (P&L, выписка, аналитика, выплаты), в шапке нет: пункт в никуда хуже отсутствующего. Проверено вживую тремя ролями. Группы эталона со счётчиками вынесены в T207 |
| T195 | guide | — | done | **Гайд ведёт по месяцу (issue #187).** Сделано блоком `guide`: путь первого месяца шагами, инструменты съёмки доведены до одной команды (`guide_prepare.mjs`, `guide_browser.mjs`). Агент оборвался на переписывании текста — снимки не пересняты, и артефакт не переопубликован; остаток в T213 |
| T196 | hr | — | done | **Именованные надбавки человека (issue #189, модуль эталона 16).** Был один множитель `coefficient`, и всё, чем отличается оплата — наставничество, старшинство, надбавка за точку, — пряталось в него: через полгода никто не мог сказать, почему 1,15. Сделано: таблица `employee_allowances` (код, название, величина, способ начисления, **свой регистр**, даты), движок начисляет их отдельными строками (`allowance.<код>`), карточка человека показывает их таблицей вместе с закончившимися. **Регистр у надбавки свой**, а не берётся у человека: в эталоне наставничество лежит в дополнительном, а ночные в официальном у одного и того же человека. Процентные считаются последними — от уже начисленного, иначе результат зависел бы от порядка строк в списке. Надбавку можно закончить датой, не заводя новую версию условий найма — ради этого задача и делалась. Проверено на живой карточке: «Наставничество · 30,00 · За отработанный час · Дополнительный · 01.01.2026». 9 новых тестов |
| T197 | hr | T208 | done | **Затраты человека делятся по его точкам, а не падают на одну (D055, issue #194).** Заведена связь «сотрудник → точки» (`employee_units`), версионированная по датам: перевод человека в сентябре не меняет июньский P&L. Перенос ФОТ в факты (T208) делит сумму между точками — поровну по умолчанию, доля у строки переопределяет; точек нет вовсе — это офис, и деньги уходят на разнесение общим правилом («офис на всех работает, вне зависимости» — владелец дословно). Копейки раскладываются накопленной суммой, тем же приёмом, что в `allocation_plan`, поэтому сумма по точкам равна полной стоимости до копейки. 4 новых теста. **Экран привязки к точкам — в T210:** сейчас связь заводится в базе, и это половина задачи |
| T198 | facts | T185 | todo | **Банковская выписка не загружается (модуль эталона 7).** Шестая очередь продуктовой карты: загрузка выписки и сопоставление строк с уже внесёнными счетами и платежами. Задачи в трекере пока нет — работа плановая, а не найденная. Готово, когда выписка грузится файлом, строки сопоставляются с существующими фактами по сумме и дате, несопоставленные встают в инбокс, а повторная загрузка того же файла не удваивает ничего |
| T199 | hygiene | — | done | **Имя папки эталона в git было записано в NFD (issue #197).** Из-за этого `test_product_map` не находил дизайн-систему на Linux, и прогон в CI был красным двое суток при зелёном локальном. Починено в источнике: папка переименована в индексе git в NFC (`d0f52cf`), сторож смотрит в индекс, а не на диск — на macOS проверка диска проходит при любой форме и ничего не доказывает |
| T200 | hygiene | — | done | **Красный прогон никем не замечается (issue #196).** Двое суток и 25 запусков подряд CI был красным, и об этом узнали случайно. Проверка, о результате которой никто не узнаёт, не проверка. Нужен сигнал: красный master должен доходить до человека (канал уведомлений уже есть — `~/.claude/forge/channel.env`). Готово, когда падение прогона на master приходит сообщением в тот же день, а не обнаруживается через двое суток |
| T201 | hygiene | — | done | **`apply_translations` молча портит запись с множественными формами (issue #195).** Скрипт вписывает одиночный `msgstr` в запись с `msgid_plural`, и получается каталог, невалидный для gettext; формы при этом остаются чужие, угаданные `makemessages`. Сторож каталога этого не ловит. Готово, когда скрипт отказывается класть перевод в такую запись, формат для форм описан, а тест каталога краснеет на записи, где есть и `msgstr`, и `msgid_plural` |
| T202 | auth | — | todo | **Вход и выбор партнёра (issue #186).** Модуль эталона 10 «Вход и каркас». Ждёт ответа владельца по Q026: пароль или одноразовый код на почту, и появляется ли в шапке выбор партнёра. Готово, когда выбранный способ входа работает на всех трёх языках, а человек с несколькими членствами (если выбран этот вариант) переключается между партнёрами без выхода из системы |
| T203 | roles | — | done | **Модель доступа выбрана и одна (issues #130, #131).** Страница эталона «Роли и права». **Q027 отвечен 28.08.2026 (D060):** выбирать одну картину не нужно — доступы делаются настраиваемыми, конкретные наборы обсуждаются с партнёром на месте. Значит строится механизм настройки прав (матрица как конфигурация, #129), а не спор о наборах. Готово, когда модель доступа записана решением, эталон, код и матрица говорят одно, а роль «менеджер смены» либо заведена, либо явно отложена с причиной |
| T204 | suppliers | T182 | done | **Накладная делится на позиции с разными статьями (issue #174, вторая половина).** Модуль эталона 3, вкладка «Разбор документа». Сделано: блок «Позиции документа» на карточке счёта, добавление позиции своим адресом (`invoices/<id>/positions/`), сверка суммы позиций с суммой на бумаге числом — «не сходится с позициями на N». Позиция берёт регистр и знак у документа: одна бумага не ложится половиной в официальный, половиной в дополнительный. Чужой счёт отвечает 404, а не отказом поля — иначе по ответу видно, что он существует (D023). Проверено вживую: позиция на 2 500 к бумаге на 10 000 дала «Не сходится с позициями на 2 500,00». 8 новых тестов |
| T205 | suppliers | T182 | done | **«Не наша»: бумага чужого юрлица уходит из очереди со следом (issue #174, третья часть).** Модуль эталона 15. Сделано: кнопка с обязательной причиной на карточке счёта, сторно всех строк документа (D020 — не удаление: июнь сегодня и июнь через полгода обязаны давать одно число), пометка `not_ours_at/by/why` в шапке документа и уход из списка счетов. Документ при этом жив: стёртый принесут второй раз и разберут как новый, а объяснить, почему его нет, будет нечем. Причину требует схема, а не только форма. Проверено вживую: без причины — отказ словами, с причиной — «в P&L его денег нет», из очереди ушла. 6 новых тестов. **Разделение на две руки (директор разносит — бухгалтер сверяет) вынесено в T206:** это модель доступа, а она ждёт Q027 |
| T206 | roles | T203 | todo | **Разбор первички в две руки (issue #174, «Путь накладной»).** Эталон: операционный директор говорит, чья накладная и за что; бухгалтер видит уже разнесённую и сверяет сумму и НДС; в раскрытии P&L видно, кто разнёс и кто подтвердил. Сейчас разбор — одно действие одного человека. Q027 отвечен (D060): доступы настраиваемые. Оперативный директор в продукте определён и с 28.08.2026 ведёт справочники (D059), так что делить работу между руками теперь есть чем. Готово, когда очередь показывает, чьего решения ждёт каждая бумага, а в раскрытии строки P&L видны оба имени |
| T207 | web | T194 | done | **Навигация областями учёта со счётчиками (issue #162, модуль эталона 10).** Сделано блоком `web`: «Сбор данных», «Расчёт», «Отчёты», «Справочники», «Настройки» — и число ждущей работы у пункта, причём ноль не показывается. Счётчик берётся из того же места, что и сам экран |
| T208 | payrun | T185 | done | **Утверждённая ведомость попадает в P&L фактами (issue #201).** Было: 35 ведомостей и ноль фактов — цепочка «данные → отчёт» разорвана там, где всё посчитано. Сделано: перенос при утверждении, двумя строками справочника — «Зарплата» (по компонентам, каждый со своим регистром) и «Налоги с зарплаты» (разница между полной стоимостью и тем, что дошло до людей, в официальном регистре: налог с неофициальной части не платится по определению). Точка — от ведомости; у офиса её нет, и строка уходит на разнесение правилом. Ключ выводится из **периода**, а не из номера расчёта — иначе пересчёт после сноса расчёта давал второй набор строк (поймано прогоном: ФОТ удвоился). Запись идёт ДО смены статуса: закрытый период база пополнять не даёт. Проверено вживую: в отчёте зарплата −1 951 806,13 и налоги −982 484,76, итог сходится с ведомостью до копейки. 6 новых тестов |
| T209 | facts | T185 | todo | **Консолидация сети в единой валюте (модуль эталона 5, нижняя часть).** Эталон: «четыре партнёра, три страны, единая валюта EUR по курсу на последний день периода». У факта уже есть `amount_report` и `report_currency` — не хватает курсов и самого экрана. Готово, когда отчёт по нескольким партнёрам собирается в одной валюте, курс виден рядом с числом и зафиксирован датой, а пересчёт закрытого месяца даёт то же число, что и в день закрытия |
| T210 | hr | T197 | done | **Точки человека заводятся с экрана, а не в базе (D055).** Связь `employee_units` работает и делит ФОТ, но завести её можно только SQL-ом — то есть партнёр без разработчика управляющего на две пиццерии не оформит. Нужен блок в карточке сотрудника: список точек с датами и долями, умолчание «поровну», предупреждение о том, что правка задевает закрытые месяцы (тот же отказ, что у условий найма). Готово, когда управляющего на две точки заводят из интерфейса, а расчёт следующего месяца делит его ФОТ по ним |
| T211 | polish | — | done | **Накопившиеся дефекты интерфейса, найденные другими блоками (issues #149, #151, #141, #146).** Несуществующий токен `--muted` на четырёх экранах, навигация, вылезающая за шапку на 1440, пустая рамка на странице 404, русские множественные числа в двух формах вместо трёх. Все четыре закрыты; журнал работы — `docs/furca/blocks/polish.md`. Задача заведена задним числом: работа шла через трекер мимо графа, и проверка целостности справедливо ругалась на блок без задач |
| T212 | reports | T187 | done | **Печать проверена фактически (остаток T187).** Смоук `tools/smoke_print_forms.mjs` прогнан на живом браузере: 17 из 17. Что доказано числами, а не словами: у ведомости 2 листа на экране и **2 страницы в напечатанном PDF**, у листка 1 и 1; каждый лист ровно 210×297 мм (793,7×1122,5 px); ни на одном листе содержимое не выходит за края; подписи «Лист N из M» идут по порядку и знают общее число; разрез (`?ledger=`) отвечает отказом, а не документом всего расчёта молча; **лист печатается белым** — тёмная тема в печати отключена не на словах, а замером цвета; консоль чистая. **Проверки не декоративны:** на старом образе они честно краснели («листов 0», «в PDF 1 вместо 0») — то есть падать умеют, и это была не гипотеза, а наблюдение. Попутно: образ приложения был собран до слияния печати, и адрес формы отвечал 404 при зелёных 73 тестах — ровно то, ради чего смоук и существует |
| T213 | guide | T195 | done | **Снимки гайда пересняты и артефакт переиздан (остаток T195).** Все 21 экран сняты заново с нынешнего продукта одной командой (`tools/guide_shots.mjs`), страница собрана (`guide_build.py`, 15,2 МБ) и опубликована по прежней ссылке — она у людей на руках и меняться не должна. Список экранов вырос с 15 до 21: в гайд вошли P&L и раскрытие строки, инбокс, счета, роли. Снимки пересняты, а не оставлены с поправленной подписью: картинка врёт убедительнее текста |
| T214 | chores | — | done | **У проекта нет объявленной команды проверок.** `docs/furca/plan.md` не содержит раздела «Проверки качества», хотя приёмка блоков обязана звать именно объявленную команду, а не собранную на месте: собранная расходится с тем, что гоняют хук и CI, и «зелено у меня» перестаёт что-либо значить. Фактически проверки живут в `.github/workflows/ci.yml` (`ruff check .`, `pytest -rs --junitxml=...`, `.github/scripts/check_skips.py`, `docker build`, `compose config`) — их надо перенести в техплан как канон, вместе с форматом машинного отчёта. Готово, когда раздел есть, команды в нём совпадают с CI, и приёмка может сверять состав прогона, а не только код возврата |
| T215 | chores | — | done | **Блок `facts` нельзя раздать: раздел «API-контракт» в `docs/furca/blocks/facts.md` пуст** (issue #208). Проверка пакета (`forge/test/package.test.sh`) красная именно на нём, и по пустому контракту агент будет строить по догадке. Из-за этого T198 и T209 в волну не берутся. Готово, когда контракт описан по факту кода (что блок отдаёт и потребляет), проверка пакета зелёная, и блок можно раздавать |
| T216 | chores | — | done | **Готовая работа по D064 не влита в master и потеряна из виду.** Ветка `feat/demo-all-screens` (коммит 792015c от 31.08, 30 файлов, +1087) делает то, что владелец потребовал дословно: демо показывает все экраны эталона, ненаписанный открывает страницу с описанием замысла, состояние экрана — один список на шапку и на страницу. В журнале стройки об этой работе нет ни строки, задачи в графе не было, а issue #219 уже ссылается на неё как на свершившийся факт — то есть снимки гайда (T213) снимались с продукта БЕЗ неё. Готово, когда ветка слита (master ушёл вперёд, конфликты вероятны), полный прогон зелёный, а в журнале записано, откуда работа взялась |
| T217 | chores | — | done | **Восемь файлов блоков не имеют раздела «API-контракт», и проверка пакета красная не на одном блоке, а на всех них** (`guide`, `hr`, `hygiene`, `money`, `polish`, `roles`, `stands`, `visual`). Вскрылось при починке T215: у `facts` контракт был написан, но назван «Контракт» — тест ищет ровно «API-контракт», как у одиннадцати остальных блоков. У этих восьми файл велся журналом работ, и раздела нет вовсе. Практическая цена — не формальность: блок без контракта не раздаётся, потому что агент строит по догадке. `hygiene`, `roles` и `stands` правятся **после приёмки волны** — сейчас в этих файлах пишут их агенты, и правка дала бы конфликт. **`hygiene` дописан 02.09 сразу после сдачи блока**; остаются `roles` и `stands`. Готово, когда у каждого блока есть раздел, описывающий по факту кода, что блок отдаёт и потребляет, и `forge/test/package.test.sh` зелёный |
| T218 | guide | T216 | done | **Снимки гайда устарели второй раз за день, и на этот раз по нашей вине** (issue #219). T213 переснял 21 снимок в 16:31 с продукта, в котором ещё не было шапки по D066 — она приехала слиянием `87ec088` через час. То есть в гайде показана шапка без областей «Расчёт» и «Отчёты», без «Банковской выписки», «Выплат», «Аналитики по людям» и «Интеграции с Dodo IS», а в продукте они есть. Картинка врёт убедительнее текста: человек ищет пункт, которого на снимке нет, и решает, что сломался он. Готово, когда снимки пересняты с продукта **после** слияния, гайд собран и переиздан по той же ссылке, а в задаче записано, чем проверено, что снимки соответствуют нынешнему продукту |
| T219 | rules | T192 | todo | **Сверить экран правил страны с эталоном модуля 17 (issue #250).** Приёмка T192 прошла живым прогоном (рамка, отказ словами, журнал попыток), но сверка с картинкой эталона не делалась: окно подписки было выбрано на 96%, а сверку делает роль, читающая скриншоты. Готово, когда `norma` сравнила карточку правила с модулем 17 и расхождения либо исправлены, либо заведены задачами |
| T220 | payrun | — | in_progress | **Сторож нормы окладника (issue #236, решение D077).** Полная выплата по контракту держится на том, что в табеле стоят все часы месяца. Не проставили тип часов — окладник молча получает меньше контракта, и расчёт выглядит успешным. Готово, когда `readiness.check` даёт находку «часов меньше нормы» с суммой разрыва против контракта, находка видна на экране закрытия месяца на трёх языках, отложить её можно с причиной, а пропустить молча нельзя; проверено на июне 2026 настоящим расчётом |
| T221 | hr | — | in_progress | **Затраты человека разносятся по точкам, а не падают на одну (issue #194, решение D055).** Офисный сотрудник числится на случайной пиццерии, и вся его зарплата ложится в её затраты — P&L точки врёт. Готово, когда в условиях найма у человека набор точек вместо одной, пустой набор означает сеть, затраты делятся правилом (умолчание — поровну), табель и ведомость показывают человека с несколькими точками, а управляющий видит своего человека, даже если тот привязан и к чужой точке |
