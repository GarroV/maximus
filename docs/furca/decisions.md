<!-- managed by furca: формат строк ниже не менять — файл парсят fabrica и cursus -->
# Решения

<!-- id: Dnnn (D001, D002, ...). кто: owner (решил владелец) | auto (решено автономно диспетчером). -->

| id | дата | решение | почему | кто |
|---|---|---|---|---|
| D001 | 2026-08-06 | Продолжаем в существующем коде, движок не переписываем | Движок сходится с эталонным расчётом бухгалтерии за июнь 2026 и покрыт 23 тестами; переписывать нечего, риск потерять точность выше любой выгоды | owner |
| D002 | 2026-08-06 | Репозиторий — личный GarroV, открытый | Решение владельца: внутренняя механика учёта интереса для посторонних не представляет, а открытость упрощает работу и показ. Принято после того, как риск был назван | owner |
| D003 | 2026-08-06 | Данные партнёров в репозиторий не попадают никогда; реальная фикстура вычищается из истории git до первого push | В `tests/fixtures/plata-2026-06.xlsx` реальные ФИО, ставки и суммы 30 сотрудников — персональные данные третьих лиц. Место таких данных в закрытой базе, а не в git, независимо от видимости репозитория | owner |
| D004 | 2026-08-06 | Регрессия гоняется на сгенерированной фикстуре с той же структурой и схемами; сверка с настоящей таблицей — отдельный тест, пропускаемый при отсутствии файла | Тест не должен быть заложником секретных данных, но и терять эталон нельзя: настоящая таблица остаётся у владельца локально и подключается переменной окружения | auto |
| D005 | 2026-08-06 | Мультиязычность с первого дня: базовый язык русский, дополнительно английский и сербский; демо-режим всегда английский | Продукт мультистрановый по замыслу, добавлять языки задним числом дороже; демо на английском — стандарт владельца для показа | owner |
| D006 | 2026-08-06 | Разворачиваем свой стек из `C:\projects\_template` со своим Postgres и своими портами (API 8030, БД 5440), а не в существующий Supabase на сервере | Контейнеры `supabase-*` на MUSPELHEIM принадлежат приватному staging Swarm Brain: в `public` 18 чужих таблиц, порты 8020/8043/5433/6543 заняты. Заливка в чужую базу перемешала бы данные двух продуктов | owner |
| D007 | 2026-08-06 | Продукт размещается на домашнем сервере MUSPELHEIM, а не в облаке | Решение владельца, принятое после того, как был назван риск: хранятся персональные данные сотрудников партнёра, включая национальные идентификаторы. Своя площадка внутри Tailscale считается достаточной защитой на этом этапе | owner |
| D008 | 2026-08-06 | Работа через ветки и pull request, CI на каждый PR | Код будут трогать не только владелец, но и коллеги из Dodo; прямые коммиты в основную ветку при нескольких участниках приводят к потере изменений | owner |
| D009 | 2026-08-06 | Регистры учёта переименовываются: `white/grey/black` → `official/supplementary/internal`, поле `layer` → `ledger` | Сленг «чёрная бухгалтерия» не должен торчать в открытом репозитории и в интерфейсе. Нейтральная терминология не теряет смысла для своих. Переименование делается сейчас, пока код маленький: затрагивает движок, YAML-пресет, схему БД и RLS-политику | owner |
| D010 | 2026-08-06 | Веб и API на Django 6.1, с переходом на 6.2 LTS в апреле 2027. Движок расчёта остаётся отдельным модулем на чистом Python | Админка справочников, мультиязычность и вход с ролями — три из шести наших требований — у Django готовы, у FastAPI и Litestar не реализовано ни одно. `ATOMIC_REQUESTS` даёт транзакцию на запрос, без которой контекст тенанта в RLS не выставить безопасно. Django единственный из троих с публичным LTS-календарём | owner |
| D011 | 2026-08-06 | Ввод часов — сетка «сотрудник × тип часа» одним числом за период, плюс импорт xlsx равноправным путём. Хранение при этом подневное | Партнёр сегодня ведёт итог за месяц: требовать подневной ввод сразу — верный способ не получить данных вовсе. Но подневная модель хранения нужна с первого дня, иначе разнесение по точкам при переводе сотрудника посреди месяца потребует миграции данных | owner |
| D012 | 2026-08-06 | Инструмент доверия к расчёту: загрузка таблицы бухгалтера и построчная сверка на первый месяц + постоянный отчёт расхождений «период против прошлого» с порогами на каждый компонент | Отрасль не показывает бухгалтеру формулу, она показывает сравнение с прошлым периодом. Сверка с его собственной таблицей окупается доверием при переезде и потом не нужна; отчёт расхождений — режим на каждый цикл. Пороги на компонент, а не общие: 5% нормально для сверхурочных и ненормально для оклада | owner |
| D013 | 2026-08-06 | Аутентификация своя, на Django auth; внешний сервер идентичности не ставим. Реализация прячется за узким интерфейсом получения текущего пользователя | Пользователей десятки, продукт за периметром. authentik требует 2 ядра и 2 ГБ плюс свои Postgres и Redis, Keycloak — JVM со своей БД: больше, чем весь наш продукт, на машине, которую нельзя надолго выключать. Права (точки × регистры) всё равно живут у нас, потому что от них зависят политики БД. Узкий интерфейс оставляет возможность подменить вход на корпоративный позже | owner |
| D014 | 2026-08-06 | Разграничение доступа целиком в RLS: роль разворачивается в ключи контекста, видимость регистров — ограничивающими (`restrictive`) политиками. Приложение ходит отдельной ролью `app_user` без `BYPASSRLS`, на таблицах `force row level security` | Движок политик в коде приложения дал бы два источника истины о доступе, которые обязательно разъедутся; при RLS забытый фильтр в новом отчёте даёт пустой результат, а не утечку. Отдельная роль обязательна: владелец таблиц и суперпользователь обходят RLS по умолчанию, а в образе Postgres всё ходит ролью `postgres` — без этого политики пишутся и не работают, а тесты проходят | owner |
| D015 | 2026-08-06 | Остаёмся на Postgres 17, непересечение периодов действия правил — через `btree_gist` и `EXCLUDE`. Обновление до 18 закладываем плановым | Стандартные темпоральные ограничения и внешние ключи с `PERIOD` появились только в 18. `EXCLUDE` даёт тот же инвариант и не блокирует стройку, но темпоральные внешние ключи руками не делаются — они понадобятся, когда правило начнёт ссылаться на другое версионированное правило | owner |
| D016 | 2026-08-06 | Демо-стенд — отдельная база, сброс пересозданием из шаблона по расписанию | Сброс атомарен и физически не может задеть боевые данные, что при живых персональных данных решает. Сид в той же базе такой гарантии не даёт | owner |
| D017 | 2026-08-06 | Интерфейс — серверные шаблоны Django + htmx 2.x, один изолированный островок на экране табеля. htmx и грид пинить по версии и класть в `static/`, не тянуть с CDN | 95% экранов — формы и ведомости; SPA удваивает кодовую базу и обесценивает главную причину выбора Django. htmx 4.0 уже в бете: CDN без пина однажды сломает продукт молча. Из гридов бесплатно всё нужное (буфер обмена, диапазоны, отмена, валидация) даёт только Tabulator под MIT — Handsontable платный, у AG Grid это enterprise-часть | owner |
| D018 | 2026-08-06 | Python 3.13 | Ветки 3.11 и 3.12 уже security-only, багфиксов нет. Нынешнее `requires-python = ">=3.11"` в любом случае перестаёт быть правдой: Django 6.1 требует 3.12 и выше | auto |
| D019 | 2026-08-06 | Схему ведём миграциями Django, переезжаем с голых SQL-файлов сразу. Фоновые задачи — очередь на самом Postgres, брокер не заводим | Ни одна из трёх SQL-миграций нигде не применена, истории применений нет, а `0003_rls.sql` всё равно переписывается — то есть переезд стоит одного переписывания и нулевого риска, а через полгода будет стоить кратно дороже. Свой раннер миграций — инфраструктурный код вместо продуктового. Фоновых задач единицы в день; очередь в самой БД даёт транзакционность даром, а отдельный брокер — ещё один контур обновлений на домашнем сервере | auto |
| D020 | 2026-08-06 | Правки задним числом: по умолчанию разница ложится помеченной дельтой в текущий период со ссылкой на исходный. Поведение настраивается на уровне тенанта, но альтернатива — не тихий пересчёт закрытого периода, а пересчёт через явное переоткрытие с причиной (D021) | Владелец попросил сделать поведение настраиваемым — сделано, но развилка переформулирована: тихий пересчёт означал бы, что июнь сегодня и июнь через полгода дают разные числа, при том что ведомость уже на руках у сотрудника, а P&L сдан. Тогда сверка перестаёт быть воспроизводимой. Через переоткрытие партнёр получает ту же гибкость, но след сохраняется. Дельта ложится в тот же регистр, что исходная строка, и помечается визуально — иначе бухгалтер получает «не сходится» без причины | owner |
| D021 | 2026-08-06 | Утверждённый период неизменяем, но обратим отдельным действием с обязательной причиной; откат виден в истории периода | Необратимость — главная жалоба на системы этого класса: одна опечатка в день утверждения превращается в разбирательство на месяц. Свободный откат без причины (как у Zoho) через полгода не объяснить | owner |
| D022 | 2026-08-06 | Блокировка на уровне строки сотрудника и на уровне точки: управляющий закрывает свою точку, бухгалтер — период целиком | Один спорный сотрудник не должен держать закрытие месяца по всей сети. Модель NetSuite: табель остаётся редактируемым, но утверждённые записи внутри заморожены | owner |
| D023 | 2026-08-06 | Три регистра показываются одной ведомостью с меткой регистра и переключателем разреза, а не тремя отдельными и не цветной заливкой строк | Заливка искажает чтение чисел и конфликтует с подсветкой отклонений; отдельные ведомости множат работу, потому что один человек может иметь строки в двух регистрах. Ключевое требование: при отсутствии доступа к регистру не видно ни строк, ни следов в итогах — итоги считаются по видимому срезу, а не маскируются на выводе | owner |
| D024 | 2026-08-06 | Выгрузка ведомости, загрузка табеля и выгрузка в привычном бухгалтеру виде — с первого дня | Самая частая претензия к системам этого класса: ввести легко, достать нельзя. Пока бухгалтер не может выгрузить и перепроверить в Excel, он не перенесёт расчёт — это условие переезда, а не уступка | owner |
| D025 | 2026-08-06 | К каждой сумме доступен след расчёта: какие правила пресета сработали, с какими входами и какой версией ставки | Продукта, который так делает, в исследовании не нашлось, а у нас правила уже лежат в конфиге с версионированием по датам — то есть след почти бесплатен. Это прямой ответ на «через полгода никто не вспомнит, откуда взялось 0,701» | owner |
| D026 | 2026-08-06 | Согласование одноуровневое в интерфейсе, но многоуровневое в модели данных | У партнёра сейчас один бухгалтер. Таблица шагов согласования с единственной строкой стоит дёшево, а переделка из одного поля «кем утверждено» — дорого | owner |
| D027 | 2026-08-07 | Стройка идёт очередями вертикальным срезом, а не блоками целиком: после каждой очереди продукт работает и наступает пауза | Блоками работающий продукт появился бы только в конце. Вертикальный срез даёт щупаемый результат после первой очереди, а паузы позволяют расходовать лимиты сессий порциями и оставлять их на обычную работу | owner |
| D028 | 2026-08-07 | До готового MVP работаем только на тестовых данных: настоящая таблица партнёра не загружается вообще. Сид — те же 32 выдуманных человека из обезличенной фикстуры | Решение владельца. Оригинальные данные не трогаем, пока продукт не готов: нечего терять и нечего утекать. Загрузка настоящей таблицы становится приёмочным шагом MVP, а не рабочим инструментом по ходу | owner |
| D029 | 2026-08-07 | Справочник сотрудников в первой версии не заводится экранами: карточки появляются из импорта табеля, админка нужна только для правки | Импортёр уже вытаскивает из таблицы имя, коэффициент, ставку, а лист даёт схему и группу. Экраны создания сотрудников отложили бы первый рабочий расчёт и продублировали бы то, что и так приходит с данными | owner |
| D030 | 2026-08-07 | MCP-сервер в первую версию не входит; HTTP API проектируется так, чтобы MCP позже стал тонкой обёрткой | Пока в базе пусто, спрашивать нечего. Плюс MCP даёт доступ к персональным данным и суммам, то есть требует того же разграничения по ролям и регистрам, что и веб — это отдельная работа, а не приставка | owner |
| D031 | 2026-08-07 | Управляющий точки видит два регистра — официальный и дополнительный, а не один официальный. Спека приведена к этому, сид уже был таким | Ответ владельца на Q010. Управляющий отвечает за свою точку и должен видеть надбавки своей смены: с одним официальным регистром он не может проверить то, за что отвечает. Числа приёмки (24 строки, 891373.32) сняты именно на этом варианте, менять их не пришлось | owner |
| D032 | 2026-08-07 | Работа курьера меряется двумя способами сразу: и часами, и сдельно (доставки или фиксированная сумма). Поддерживаем оба, выбор — настройкой группы | Ответ владельца на Q011. Это ровно тот случай, ради которого в принципах записано «закладываем больше вариантов, чем известно сейчас»: чем платят курьерам в Сербии, бухгалтер ещё не ответил, а гадать дороже, чем поддержать оба пути и выбрать сходящийся с таблицей | owner |
| D033 | 2026-08-08 | Право закрывать часы точки получает и оперативный директор, а не только управляющий этой точки. У управляющего остаётся только своя точка | Найдено приёмкой T022: закрыть точку сегодня может исключительно её управляющий, и если он в отпуске или уволился, месяц не закрыть никем — при том что директор вправе утвердить весь период целиком. Запрещать меньшее тому, кому разрешено большее, — не разграничение доступа, а тупик в работе. Вопросом владельцу не делалось: разумный дефолт очевиден, а обратное решение (директор не закрывает) требовало бы объяснить, кто закрывает вместо отсутствующего | auto |
| D034 | 2026-08-10 | Раскатываем на MUSPELHEIM недостроенный продукт: 69 задач из 80 закрыты, 11 нет (админка справочников и курьеры начаты и лежат в ветках, долги проверочной обвязки, хранение следа, защита ветки, приёмка на настоящих данных) | Прямое указание владельца после того, как список незакрытого был показан ему целиком. Причина названа им же: нужна ссылка, чтобы потыкать продукт на тестовых данных до похода к заказчику. Риска для чужих данных нет — на площадке продукта ещё не существует, настоящих данных партнёра там нет и по D028 не будет до приёмки MVP | owner |
| D035 | 2026-08-13 | Демо-стенд никому не показывается, пока не закрыты все расхождения сверки. Продукт и демо остаются на MUSPELHEIM в сети Tailscale, наружу не открываются | Указание владельца 2026-08-13 в ответ на алерт о пятой утечке регистров: «нужно всё править, демо никому не показываем». Утечка была видна именно на демо — там бухгалтеру показывали поимённый список людей со скрытыми регистрами, при том что титульная страница демо обещает обратное | owner |
| D036 | 2026-08-13 | Бухгалтер и оперативный директор имеют РАВНЫЙ доступ: бухгалтеру открыты все три регистра, как директору. Механизм видимости регистров (RLS, срез по роли) остаётся на месте и не разбирается — сужать доступы будем позже, там где надо. Управляющего точки это не касается: у него по-прежнему свои точки и два регистра (D031) | Ответ владельца на Q012 дословно: «И бухгалтер и операционный директор имеют равный доступ на данный момент. поэтому давай спроектируем систему чтобы она просто работала. нам ее еще обкатывать придется и будет еще миллион правок. А потом уже будет доступы закрывать там где надо». Разграничение строилось на догадке, что официальная роль не должна видеть остальные регистры; у партнёра сейчас это не так. Из этого же следуют сами собой: бухгалтер может посчитать период (T087), её выгрузка P&L перестаёт быть молча без налогов (T088), демо-файл перестаёт отдавать ей 404 (T104) | owner |
| D037 | 2026-08-13 | Демо-файл таблицы бухгалтера собирается ПОЛНЫМ срезом и одинаков для всех, кто его скачал, независимо от роли | Файл изображает артефакт, живущий вне продукта: таблицу бухгалтер ведёт у себя и рассылает остальным. Артефакт со стороны не может зависеть от того, кто его открыл — иначе один файл давал бы разным ролям разные расхождения, и экран сверки показывал бы не сверку, а собственный срез смотрящего. Отвергнуто: оставить 404 (экран существует ради тех самых ролей) и собирать срезом скачивающего. Ограничено демо-стендом: маршрут есть только при DEMO_MODE=1, база отдельная и с выдуманными людьми (D016); подменяется только app.user_id внутри транзакции запроса, роль подключения остаётся app_user — политики продолжают действовать | auto |
| D038 | 2026-08-13 | Раскатка на MUSPELHEIM идёт без отдельного подтверждения на каждый раз, пока владелец не скажет иначе | Владелец дословно: «делай все что можешь сделать… не жди от меня разрешений… С утра хочу уже видеть результат». Площадка не боевая: живых пользователей и настоящих данных партнёра на ней нет (D028), доступ только по Tailscale, наружу ничего не публикуется. Перед каждой раскаткой всё равно делается дамп базы, а сама раскатка — только закоммиченным и запушенным master с зелёным CI. Отменяется одним словом владельца | owner |
| D039 | 2026-08-14 | Регистр расхода следует из КАССЫ, из которой платили, а не выбирается к каждой трате: официальная касса → официальный регистр, чёрная → внутренний. Значит у точки может быть несколько касс, и касса — справочник со своим регистром | Ответ владельца на Q013 дословно: «Из кассы берем только официально. Но есть чёрная касса, где по дефолту идёт в чёрную. Не понимаю вопроса». Вопрос был задан про свойство операции, а владелец думает про источник денег — и он прав: человек на точке не выбирает «регистр», он берёт деньги из одной коробки или из другой. Ручной выбор регистра при этом остаётся возможным (правка), но умолчание приезжает из кассы | owner |
| D040 | 2026-08-14 | Остаток по кассе не ведём — ни сейчас, ни в следующих очередях. Экран расходов показывает список и итог трат, а не «сколько должно лежать» | Ответ владельца на Q014: «Кассу не трогаем нигде же? У нас сервис по сборке ПНЛ, нам тут касса не важна». Остаток требовал бы прихода наличными, то есть выручки из Dodo IS, и превращал бы продукт в кассовую книгу — это другой продукт | owner |
| D041 | 2026-08-14 | Справочник статей расходов наполняется из файла бухгалтера, и его можно править и дополнять с экрана | Ответ владельца на Q015: «Берём за основу её справочник, но должна быть возможность редактировать и дополнять». Правка с экрана уже есть (T108); недостаёт загрузки её списка файлом | owner |
| D042 | 2026-08-14 | НДС у расхода хранится отдельным полем (сумма документа и ставка), а в P&L по умолчанию показывается сумма БЕЗ НДС | Ответ владельца на Q016: «НДС нужен, да. Это вообще важная вещь в Сербии и вообще. Хотя вообще в итоговом ПНЛ мы обычно не показываем НДС». Хранить обязательно — иначе сумму без налога потом не восстановить; показывать по умолчанию без него | owner |
| D043 | 2026-08-17 | Доступ к Dodo IS API для нас не требует публикации в маркетплейсе: заводим приложение в личном кабинете разработчика, получаем Client Id и Secret, ходим под учёткой Dodo IS партнёра. Публикация — отдельная история про распространение другим сетям, к шестой очереди отношения не имеет | Проверено по документации: раздел «Начало работы» на docs.dodois.io описывает ровно четыре шага без маркетплейса, а инструкция по публикации начинается словами «у вас уже есть приложение, которое использует API». Фраза «API access is granted after the app is published…» на страницах методов вводит в заблуждение — я на неё вчера и купился | auto |
| D044 | 2026-08-17 | Порядок забора данных из Dodo IS: сначала ЧАСЫ, потом сырьё и списания, потом выручка с разбивкой. Ничего из этого не стопорит текущие очереди | Указание владельца дословно: «нам и надо от додо ис на текущем уровне только часы, потом сырье и списания, и потом выручка с разбивкой. нашей работы не стопорит никак». Порядок совпадает с ценой ручной работы у партнёра: часы вбиваются в табель каждый месяц руками, это самая дорогая рутина | owner |
| D045 | 2026-08-18 | Стенд на MUSPELHEIM открывается наружу через Tailscale Funnel — постоянный публичный HTTPS-адрес без домена. Этим отменяется часть D035 «наружу не открываются» и снимается условие «не показывать, пока не закрыты все расхождения сверки»: обкатка теперь важнее. Ноут владельца площадкой не рассматривается вообще. Настоящая таблица партнёра на публично доступный стенд не грузится — приёмка T039 идёт при выключенном Funnel либо на закрытом стенде | Владелец дословно: «мне надо будет этот софт показывать. значит надо вывести его в интернет… это временное решение, но сейчас первоочередная задача это обкатка. а давать доступ к своему ноуту я не могу». Funnel выбран против покупки домена и против Cloudflare Tunnel: адрес постоянный (значит `ALLOWED_HOSTS` правится один раз, а не при каждом перезапуске, как у `trycloudflare.com`), Tailscale на сервере уже стоит, платить не надо. Оговорка про настоящие данные — не отмена решения владельца, а его же D028: в базе стенда выдуманные люди, и до приёмки MVP это так и остаётся; ФИО и национальные идентификаторы 30 живых сотрудников за публичным адресом — другой класс риска, чем за Tailscale | owner |
| D046 | 2026-08-18 | Продуктовый скоуп расширяется за пределы изначального дизайна: жизненный цикл сотрудника из интерфейса, настройка правил на всех уровнях включая страновой, статистика по отдельному человеку, аналитика по кадрам и единая визуальная система. Онбординг владельца в продукт идёт первым — он же уточнит остальное | Владелец дословно: «я сейчас не понимаю как она работает, где какие функции, соответственно партнеру мы явно не сможем передать то что есть… Я знаю что это все отходит от изначального дизайна. но это то что нам надо будет». Ревизия интерфейса 2026-08-18 подтвердила фактически: сотрудника из интерфейса не завести (D029), схема расчёта — свободное текстовое поле, переопределения правил ставятся только на уровне партнёра, страновые правила живут лишь в YAML, карточка человека без сумм, аналитики по кадрам нет вовсе. То есть жалоба владельца — не про навигацию, а про отсутствующую функциональность | owner |
| D047 | 2026-08-19 | **Права — набор на пользователе, а не одна жёсткая роль:** одному человеку можно дать несколько ролей сразу (бухгалтер + администратор), и есть экран, где это делается. Отменяет мою первую формулировку того же дня — «администратор получает полный цикл месяца» | Владелец поправил разбор: людей не один и ролей не четыре штук на весь мир. Оперативный директор отвечает за сборку P&L целиком. Бухгалтер отвечает за часть операций, а у части партнёров — за весь проект, и тогда он **по сути администратор**. Управляющий на месте скидывает накладные и чеки и должен проверять данные сотрудников своей точки (имена, ставки, должности). Значит правильный механизм — не «выдать администратору все права по умолчанию», а возможность дать одному человеку несколько ролей сразу. Первая формулировка чинила симптом и ломала разделение обязанностей там, где партнёр его как раз держит. Задачи: T170, T171, T172, T173 | owner |
| D048 | 2026-08-20 | **Репозиторий переведён в приватные.** Отменяет часть D002 «репозиторий открытый»: код и беклог остаются личными, наружу не публикуются | Общее решение владельца от 20.08.2026: репозиторий проекта по умолчанию приватный — задачи ведутся внутри, но не наружу. Публичным делается только осознанно и по причине. Проверено фактом: `gh repo view` отдаёт `visibility: PRIVATE`. Следствие, которое пришлось узнать дорого: у приватного репозитория на бесплатном тарифе нет защиты ветки и ограничены минуты Actions — CI встал по биллингу (Q021, issue #137) | owner |
| D049 | 2026-08-21 | **D029 в прежнем виде больше не действует: сотрудника можно завести с экрана.** Карточки по-прежнему приезжают загрузкой таблицы партнёра, но это перестало быть единственным путём — появился экран заведения, и человек создаётся сразу с первой версией условий найма | D029 запрещал заведение сотрудника из интерфейса нарочно: источник карточек — таблица партнёра, и второй путь означал бы два источника правды. Довод перестал работать на живом сценарии: человек выходит на работу в середине месяца, и до следующей загрузки его в системе НЕ СУЩЕСТВУЕТ — ни в табеле, ни в ведомости. Передавать продукт партнёру так нельзя, и T164 это прямо потребовала пересмотреть. Двух источников правды при этом не возникло: загрузка и экран пишут одни и те же таблицы одним путём, а не спорят. Пометки «намеренно» в коде не снесены, а переписаны со ссылкой на T164 | owner |
| D050 | 2026-08-22 | **CI до сентября не чиним и денег в него не несём.** Actions остаются выключенными по биллингу, защиты ветки нет; проверки гоняются локально полным прогоном перед слиянием. К вопросу тарифа возвращаемся в сентябре | Владелец: «терпим до сентября и там всё будет окей» — решение принято, обсуждение закрыто. Записано после того, как вопрос всплыл повторно в отчёте: он был задан устно и нигде не зафиксирован, поэтому выглядел открытым | owner |
| D051 | 2026-08-26 | **У демо есть учётка осмотра `admin` / `admin` со всеми правами сразу.** Права набираются двумя ролями на одном человеке (директор + администратор сети), а не новой ролью «может всё». Учётка роли «администратор сети» переехала на логин `netadmin`, потому что логин `admin` отдан осмотру | Владелец дословно: «сделай учетку админ/админ, который будет с максимальными правами. это для теста и отсмотра». Кнопки титульной показывают роли по одной — это и есть демонстрация разграничения; тому, кто обходит продукт целиком, четыре переключения мешают. Пятая роль «может всё» отвергнута: такой роли в продукте нет, и демо, показывающее её, врало бы про то, ради чего его показывают. Набор ролей на одном человеке — ровно продуктовый механизм (D047), и права складывает сама база | owner |
| D052 | 2026-08-26 | **Роль «администратор сети» может ВСЁ**: весь цикл месяца плюс справочники, правила и роли. Отменяет часть D047, где администратору намеренно не давали данные расчёта, а недостающее предлагалось добирать второй ролью | Владелец дословно: «У нас есть роль АДМИН, у которой есть вообще все». Прежняя форма держалась на доводе «разделение обязанностей», но он был непрочен: `roles.manage` означает, что администратор правит сами роли, то есть в любой момент вправе выписать себе недостающее — запрет, снимаемый одним экраном самим запрещённым, это лишний шаг, а не разграничение (D033). Механизм нескольких ролей на человеке (D047) остаётся живым: им собирают промежуточные наборы вроде «бухгалтер + администратор». Цена решения — два десятка проверок разграничения, которые брали администратора как готовую «роль без права»: они переведены на явное сужение роли в тесте (`narrowed_permissions`), как это делает партнёр экраном ролей | owner |
| D053 | 2026-08-26 | **Следующая очередь — часы и оплата труда целиком, а не банковская выписка.** Модуль включает: оклад как форму оплаты наравне с почасовой и сдельной, именованные надбавки в условиях найма, должности как шаблон с вилкой ставки, гарантию часов по договору, массовые изменения условий и предпросчёт начислений на карточке. Очереди 5–10 прежнего порядка сдвигаются на одну позицию | Владелец дословно: «в первую очередь мне необходим модуль учета часов сотрудников. система окладов, коэффициентов и прочее». Продукт сегодня умеет считать только от почасовой ставки с одним множителем (`base_rate × coefficient`) плюс сдельные меры — месячного оклада нет как формы оплаты вовсе, а надбавки (ночные, наставничество) либо зашиты в коэффициент, либо считаются типом часа (Q006 висит открытым). Партнёру, у которого часть людей на окладе, продукт в таком виде передать нельзя | owner |
| D054 | 2026-08-26 | **Надбавка = коэффициент к ТИПУ ЧАСА** (вечерние, ночные, выходные и прочее), а не множитель у человека и не отдельная ставка. Оплата человека — **либо часы, либо оклад суммой**: две формы, а не пропорция одной | Владелец дословно, двумя ответами: «либо часы либо сумма оклада» и «надбавки это коэффициенты за вечерние часы, ночные часы, выходные и прочее». Этим закрывается Q006, висевший с начала стройки. Следствия: набор типов часов и их коэффициенты становятся настройкой партнёра (сегодня они зашиты в пресет страны, и «вечерних» с «выходными» в нём нет вовсе), а `coefficient` в условиях найма перестаёт быть местом, куда прячут надбавки | owner |
| D055 | 2026-08-27 | **Затраты человека разносятся по ТОЧКАМ, к которым он привязан, а способ разнесения — настройка.** У человека может быть одна точка (пиццерийный персонал), несколько (управляющий на две-три пиццерии, территориальный управляющий по городу) или ни одной — тогда это сеть целиком (офис). Способ деления настраивается; умолчание — поровну. Группа сотрудников при этом остаётся: она отвечает не за «кого нанимаем», а за то, куда относятся деньги — строка P&L, регистр, схема расчёта | Владелец 27.08.2026 дословно: «чет кажется тут должно настраивать… в теории мне кажется что равномерно должно, потому что офис на всех работает, вне зависимости» и «управляющий может быть на несколько пиццерий один… чтобы условный человек в нови саде делился только по пиццериям нови сада, а в белграде только на белград». Найдено при разборе его же вопроса про группы и должности: механизм разнесения на точки в продукте есть и работает для расходов (поровну / по выручке), а зарплата жёстко привязана к одной точке — офисный сотрудник целиком ложится в затраты той пиццерии, где он числится. Решение принимается ДО проводки зарплаты в факты (T113): иначе она закрепит нынешнее поведение | owner |
| D056 | 2026-08-27 | **Источник истины о продукте — дизайн-система плюс задачи в трекере; документы Forge идут для поддержки.** Спека описывает, как продукт себя ведёт и почему сделано так, но что должно быть на экране, решает эталон. Отсюда обязательная сверка перед взятием модуля или задачи: модуль эталона, журнал блока, решения | Владелец дословно: «сейчас эталон это дизайн система + ищус. доки форджа для поддержки идут». Разрыв, из которого это выросло, стоил полутора дней: дизайн-система пришла после того, как стройка по спеке закончилась, слияние прошло наполовину, и ревизия через неделю нашла 63 расхождения — две трети открытых задач были порождены им. Записано с опозданием на день: решение прозвучало 27.08, а в журнал попало 28.08 — сама по себе иллюстрация к правилу «решение записывается в тот же момент» | owner |
| D057 | 2026-08-28 | **Граф задач ведётся в правде: работа, взятая в стройку, живёт в `tasks.md`, issues остаются беклогом.** У задачи графа в тексте назван issue и, если она рисует экран, модуль эталона; карта продукта читается «issue → задача графа». Мелкие находки и вопросы в граф не переезжают | Владелец: «приводи граф в правду. не забудь про дизайн систему» — после разбора, почему проект не идёт на многоагентных рельсах Forge. Причина оказалась не технической: диспетчер читает `tasks.md`, а работа с T161 переехала в issues и обратно не возвращалась — 171 `done` и 11 `todo`, из которых почти все отложены или давно сделаны, при семидесяти четырёх открытых issue. Раздавать было нечего | owner |
| D058 | 2026-08-28 | **Ничего платного не покупаем; репозитории публичные, CI работает и бесплатен.** Отменяет часть D050 «CI не чиним до сентября»: чинить нечего — прогоны идут. Находка по безопасности в публичном репозитории идёт приватным advisory, а не issue | Владелец дословно, на третий раз: «мы ничего не будем покупать, репозитории и так уже публичные, работаем без СИ до сентября, там лимитов будет хватать. Проверь это уже и запиши себе где-то на лбу глобально». Проверено фактом в тот же час: оба репозитория PUBLIC, у публичных минуты Actions бесплатны, за сутки четыре зелёных прогона. Issue #137 «CI мёртв по биллингу» закрыт как устаревший | owner |
| D059 | 2026-08-28 | **Оперативный директор и бухгалтер ведут справочники** — право `directory.manage` добавляется обеим ролям | Владелец на вопрос о правах: «Операционный директор мне кажется может вести справочники, почему нет?». Раньше справочники вёл только администратор сети, и это упиралось в то, что у партнёра оперативный директор — второй человек после бухгалтера, а не гость. Бухгалтер получает право вместе с ним не по расширению ответа, а по уже записанному D036: у партнёра эти две роли равны по доступу, и дать право одному значило бы молча отменить прежнее решение владельца | owner |
| D060 | 2026-08-28 | **Модель доступа делается настраиваемой, а не фиксированной; конкретные наборы прав обсуждаются с партнёром на месте.** Ответ на Q027: выбирать одну из трёх картин сейчас не нужно — нужна гибкость | Владелец дословно: «надо будет обсуждать с партнером на месте. сейчас я не смогу решить, но кажется надо на будущее предусмотреть настройку… чисто в теории может быть ситуация когда человек просит уточнить что-то и управляющий может сам в своем кабинете это посмотреть. поэтому я и говорил что система управления доступами должна быть гибкая, модульная». Это снимает блокировку с задач по доступам: строим механизм настройки, а не спорим о наборах | owner |
| D061 | 2026-08-30 | **Из Dodo IS берём первичные данные, а не расчёты: отработанные часы (главное), километраж курьеров, выручку с разбивкой по точкам и метрики, идущие прямо в строки P&L. Зарплату не берём ни в каком виде — ни начисления, ни премии, ни сверку.** Метод `staff/incentives-by-members` отдаёт готовые суммы по сменам (дневные, ночные, за заказы, за километраж, за стаж, итог) — не используем | Владелец дословно: «ЗАРПЛАТЫ МЫ НЕ БЕРЕМ ИЗ ДОДО ИС, мы ради этого и строим систему». Сказано в ответ на разбор API, где готовый зарплатный метод был подан как полезная находка для сверки — это противоречило замыслу продукта. Схемы Сербии, налоги и условия найма живут у нас, и второй источник истины по зарплате обесценивает движок: при расхождении непонятно, какая цифра правильная. Прежняя строка в `docs/dodo-is-api.md` про «сверку их премий против нашего расчёта» снята | owner |
| D062 | 2026-08-30 | **Из Dodo IS берём ТОЛЬКО отработанные часы.** Ни выручки, ни зарплаты, ни себестоимости, ни «полезных метрик» — ничего сверх часов. Отменяет объём D044 (часы → сырьё и списания → выручка) и сужает шестую очередь до одного метода `GET /staff/shifts` | Владелец дословно: «МЫ СТРОИМ СИСТЕМУ КОТОРАЯ БУДЕТ ПОМОГАТЬ СЧИТАТЬ ОКЛАДЫ И КОЭФФИЦИЕНТЫ… ОТ ДОДО ИС НУЖНЫ ТОЛЬКО ЧАСЫ». Сказано на третий раз, после того как разбор API дважды разворачивался в каталог возможностей. Причина в замысле продукта: всё, что Dodo IS уже посчитал, не нужно по определению — второй источник истины обесценивает движок, ради которого проект и существует. Часы — единственное, чего у нас нет и что вбивают руками каждый месяц | owner |
| D063 | 2026-08-31 | **Коннекторы устроены как выключатели: каждый поток данных включается отдельно и по умолчанию выключен.** Три состояния вместо флага: `off` (не тянем), `trial` (тянем и показываем рядом, но в расчёт и в P&L НЕ берём), `on` (берём в работу). Состояние живёт у партнёра, а не в настройках инстанса. Часы из невзятого источника не пропускаются молча — расчёт отказывает и называет людей | Владелец: «мы можем как-то построить, чтобы коннекторы были, но в работу мы их брали опционально? я не уверен что та же себестоимость считается нормально в додо ис». Развилка настоящая: проверить чужую цифру можно только на живом месяце, но если она уже попала в расчёт — проверка опоздала. `trial` разрывает этот круг. Состояние у партнёра, потому что одному себестоимость Dodo IS подходит, другому нет, и настройка на инстанс означала бы либо чужой риск всем, либо отказ всем. Механизм не отменяет D062 (из Dodo IS берём только часы) — он делает так, чтобы расширение объёма не требовало переписывания | owner |
| D064 | 2026-08-31 | **Партнёр не заводится входом. Пространство и первого пользователя создаём мы, вне продукта; вход через Dodo IS только привязывается к уже существующему пространству.** Человек, вошедший учёткой Dodo IS, точки которой не сопоставлены ни с одним партнёром, получает отказ, а не новый тенант | Владелец дословно: «для первого входа мы через админку создаем пространство и первого пользователя». Ответ на границу, найденную при разборе входа через Dodo IS: провайдер общий на всю сеть, и самозапись означала бы, что любой сотрудник Dodo заводит себе партнёра в нашем продукте. Решение совпадает с тем, как база уже устроена: на `tenants` стоит политика только на чтение, у роли приложения нет `insert` ни на тенантов, ни на пользователей (`0004_rls`, `0010_users`) — то есть завести партнёра из продукта нельзя по конструкции, ровно как первого администратора платформы (`platform_admin`, T165) | owner |
| D065 | 2026-08-31 | **Платформенная админка — отдельная полноценная поверхность, а не команда и не переключатель в админке партнёра.** Устроена иерархией: список пространств → внутрь пространства → его сотрудники → выдача ролей. В будущем — статистика по пространствам (сколько сотрудников, заходы, прочее). Заведение пользователей через разработчика остаётся только на первых партнёрах; появилась админка — переходим на неё | Владелец дословно: «админка нужна отдельная, для менеджмента пространств и добавления пользователей + назначения ролей», и на попытку свести её с админкой партнёра: «я хочу чтобы была полноценная админка где я вижу пространства, углубившись в пространства я могу видеть сотрудников и давать им роли… в админке также в будущем нужна будет статистика по пространствам». Я предлагал сначала команду по образцу `platform_admin`, потом — ту же админку с переключателем пространства, раз набор кнопок дублируется. Оба раза мимо: дублируются кнопки, а не инструмент. Обзор над всеми пространствами и статистика по ним у партнёрской админки отсутствуют по определению — она заперта в одном тенанте. Django admin при этом не подключаем: он либо ходит ролью `app_user` и не может ничего, либо получает права владельца схемы, и рядом с нашей моделью доступа появляется вторая, обходящая RLS | owner |
| D066 | 2026-08-31 | **Демо показывает ВСЕ экраны продукта, включая ненаписанные, и честно называет состояние каждого. Два состояния и две пометки: «Доработка» — экран есть и дорабатывается; «Разработка в процессе» — экрана нет, стоит заглушка с описанием, что здесь будет. Слово «беднее» из лексикона убирается везде, включая `docs/product-map.md`** | Владелец дословно: «в демо нужны вообще все экраны , те что сейчас в дработке - так и помечаем что В ДОРАБОТКЕ», и следом, уточняя: «формулировку БЕДНЕЕ выкидываем. там где что-т оесть и мы дорабатываем - там доработка. там где ничего нет - разработка в процессе». Сказано после того, как он открыл демо и решил, что половины функционала нет: P&L, сверка и выгрузки были построены и отвечали `200`, но в шапку не попали (#218) — демо скрывало готовое наравне с неготовым. Прятать неполноту дороже, чем признавать: скрытый экран читается как отсутствующий, помеченный — как запланированный. Две пометки вместо одной потому, что «дорабатываем начатое» и «ещё не начинали» — разные обещания смотрящему **Номер:** решение принято 31.08 в соседнем окне и записано там как D064, но тот номер в этот же день занят другим решением (заведение партнёра). Коммит 792015c и issue #219 ссылаются на «D064», имея в виду именно это решение — читать как D066 | owner |
| D067 | 2026-09-02 | **Незамеченная красная проверка — не разовый случай, а то, от чего нужен механизм на будущее. Готовую работу, лежащую невлитой веткой, вливать, а не оставлять ждать** | Владелец дословно, ответом на сводку о старте волны: «1. надо предусмотреть на будущее 2. надо вливать». Первый пункт — про прогон, который был красным с 30.08 по 02.09 и о котором узнали только потому, что стройка прогнала линтер руками; значит T200 строит не уведомление про этот случай, а защиту от класса: проверка, о результате которой никто не узнаёт, проверкой не является. Второй пункт закрывает вопрос про ветку `feat/demo-all-screens` (демо со всеми экранами, D066): она влита коммитом `87ec088`, и то же правило распространяется на любую следующую находку такого рода — готовая работа не лежит невлитой в ожидании повода | owner |
| D068 | 2026-09-02 | **Раскатка этого проекта не ждёт ночного окна: у него нет живых пользователей.** Обновление стенда и демо на площадке делается сразу по «да» владельца, в любое время суток. Правило ночного окна остаётся в силе для продуктов, которыми уже кто-то пользуется, и вернётся сюда в тот день, когда у партнёра появятся живые люди | Владелец дословно: «раскатывай», и следом, на мой вопрос про окно: «не жди окна, ебаш сразу. этим проектом никто не пользуется». Совпадает с триггером самого правила (`deploy-window`): оно написано про продукт, которым пользуются, и защищает людей, работающих в момент раскатки, — здесь таких людей нет, демо изолировано своей базой и тестовыми данными. Разрешение при этом остаётся явным и разовым: «да» на эту раскатку не переносится на следующую | owner |
| D069 | 2026-09-03 | **Репозиторий остаётся публичным, история git не переписывается. Раскрытие дефекта доступа, попавшее в публичную историю, признаётся допустимым риском до пилота; сами дефекты закрываются до того, как в продукте появятся живые люди и данные.** Правило про приватный advisory для новых находок остаётся в силе: следующая незакрытая дыра пишется черновиком advisory, а не открытым текстом | Владелец дословно: «проект пока без пользователей, и когда все пойдет в пилот, то мы уже поправим и закроем проблемы». Сказано после того, как риск был назван прямо: механика дефекта выдачи прав провисела в публичном `progress.md` сутки и осталась в истории коммита `90f8c88`, который переписыванием файла не убирается. Цена альтернатив измерена: перевод в приватные упирается в квоту Actions (замер 03.09 — 30 прогонов по 13,8 мин, около 2480 мин/мес против 3000 у Pro, то есть без запаса), переписывание истории ломает живые ветки блоков, растущие от master. Совпадает по логике с D068: у продукта нет пользователей, красть из него нечего, а правила, защищающие живых людей, включатся к пилоту. Условие возврата к вопросу — issue-носитель перед пилотом | owner |
| D070 | 2026-09-03 | **Оклад: базово полная выплата по контракту, но на неё влияют больничный, отпуск и прочие основания. Это ответ на Q025, и он совпадает с нынешним умолчанием `proration: worked_hours` — в правилах менять нечего.** Ветку `proration: none` под этот ответ включать нельзя | Владелец дословно: «базово идет полная выплата по контракту. но возможны больничные отпуска и прочее, т.е. друге средства влияния». Очевидное прочтение («полная выплата» = сумма целиком) ведёт к `none`, и это была бы ошибка: проверено по коду — `none` уводит окладника в `accrue_flat_salary`, где часы не влияют вовсе, и там, по комментарию самого движка, «надбавки за ночные считать не от чего». То есть `none` убивает вторую половину фразы владельца про другие средства влияния: не считаются ни ночные, ни вечерние, ни выходные, ни больничный, ни доплата до минимума. Умолчание `worked_hours` (стоит в пресете Сербии) даёт ровно описанное: полная норма часов = ровно оклад, отпуск оплачен полностью, праздники с коэффициентом, больничный по своему проценту. **Настоящая дыра лежит не в правилах, а в полноте табеля:** полная выплата держится на том, что в табеле стоят все часы месяца, а полноту никто не проверяет — не проставили тип часов, и окладник получает меньше контракта при успешном на вид расчёте (issue #236, головная задача блока окладов) | owner |
| D071 | 2026-09-07 | Продукт называется **MAXIMUS**. Переименование ограничено видимым слоем: шапка, заголовки вкладок, печатные формы, демо, материалы для людей. Имя репозитория, пакеты, схема базы, переменные окружения и адреса не трогаются; слово `P&L` как предметный термин (отчёт о прибылях и убытках) остаётся везде, где означает отчёт, а не продукт | Владелец: «пока полностью под 0 не переименовываем, только визуальные части при контакте с пользователем. MAXIMUS — ПНЛ 🏛️, DECIMUS — Аудиты, MERIDIUS — Чек-листы». Разбивка по местам и открытые вопросы (что остаётся от «Dodo», роль знака 🏛️) — issue #238 | owner |
| D072 | 2026-09-17 | **Видимое имя продукта — MAXIMUS.** В шапке жирное `MAXIMUS` и приглушённое `P&L` рядом как пояснение; заголовок вкладки — `MAXIMUS · Периоды`. 🏛️ из реплики владельца брендом **не** является, favicon остаётся отдельным решением | Ответы владельца 17.09.2026 на три вопроса, висевшие в issue #238 с 07.09. Про эмодзи дословно: «картинку не надо, она хер знает как попалась» — значит из неё не делается ни favicon, ни аватар. `P&L` в шапке сохраняется приглушённым по той же причине, по какой вопрос задавался: человек, впервые открывший экран, из одного слова MAXIMUS не поймёт, что перед ним ПНЛ. Порядок `MAXIMUS · Периоды` выбран владельцем против моей рекомендации (`Периоды · MAXIMUS`) — цена известна и принята: в узкой вкладке у всех разделов видно одно и то же слово | owner |
| D073 | 2026-09-17 | **Ребрендинг продукта и чистка хвостов Forge→FURCA делаются одним планом**, а не двумя заходами. Исключение — путь сторожа CI: он ждёт решения по `GarroV/furca#134` | Ответ владельца 17.09.2026 на вопрос об объёме. Оба ренейма трогают одни и те же `README.md` и `docs/`, второй проход по тем же файлам дороже. Сторож выделен потому, что `tools/ci_watch.py` держится за `~/.claude/forge/ci-watch`, и сегодня он **прав** — каталог там; профиль стройки при этом уже уехал в `~/.claude/furca/`. Чинить путь раньше, чем ядро решит, куда переезжает остаток, значит сломать работающее | owner |
| D074 | 2026-09-18 | **Спорные развилки бухгалтерии не выбираются — поддерживаются все варианты, выбор живёт настройкой в админке КОНКРЕТНОГО пространства партнёра.** Касается вопросов к бухгалтеру: Q001 (база доплаты до минимума), Q002 (поводы для доплаты), Q003 (топли оброк по дням или по часам), Q004 (проценты по типам отсутствия), Q005 (признак схемы с минимальной базой взносов), Q007 (двойной учёт топли оброка в P&L), Q009 (статья налогов с зарплаты), Q017 (обязательства или только оплаченное), Q018 (авансы и платёж на несколько счетов), Q019 (регистр трат мимо официального контура), Q024 (надбавки за вечер и выходные), Q029 (рамка ставок и производных), Q030 (булевы признаки правил). Уровень настройки — пространство партнёра, поверх пресета страны; страна поставляет значение по умолчанию, партнёр меняет отличия | Решение владельца 18.09.2026, дословно: «делай максимальное покрытие. Учитывая, что мы строим сервис для разных стран, то в разных странах могут быть разные требования, разные подходы, разные решения. Поэтому все спорные вопросы, которые могут быть с бухгалтером, делай все варианты с настройкой в админке этого конкретного пространства для конкретного партнёра. Давай примем такое решение, потому что мы сейчас подгоним всё под Сербию, а потом всё равно придётся переделывать под другие страны. Поэтому тут давай, пожалуйста, заложим сразу вот такую вот махинацию. Я понимаю, что это будет более ресурсозатратным в теории, но тем не менее». Снимает зависимость стройки от ответов бухгалтера Сербии: они перестают быть блокером и становятся выбором значения по умолчанию в пресете страны | owner |
| D075 | 2026-09-18 | **Вход остаётся паролем; пользователи заводятся в админке внутри пространства партнёра — почта вводится, пароль генерируется. Авторизация через Google откладывается на будущее.** Из того, что пользователь создаётся внутри пространства, следует и вторая половина Q026: членство остаётся ОДНИМ, выбора партнёра в шапке сейчас нет — эталон модуля 10 в этой части опережает продукт | Решение владельца 18.09.2026, дословно: «давай сделаем пока колхозный вариант: в админке где создаем пространства для партнеров, внутри этих пространств создаем и пользователей. там же вводим почту и генерируем пароль. в будущем прикрутим авторизацию через гугл». Снимает блокировку с T202: почтовый канал не нужен, хранение паролей остаётся, множественное членство не строится. Вывод про одно членство подтверждён владельцем в тот же день («ок») | owner |
| D076 | 2026-09-18 | **Неразнесённая накладная попадает в P&L, а не выпадает из него.** Расхождение с эталоном модуля 15 («до разнесения накладная в P&L не попадает») разрешено в пользу нынешнего поведения продукта: сумма без статьи видна в отчёте числом и стоит в инбоксе. Настройкой пространства НЕ делается — в отличие от развилок D074 здесь у владельца есть один ответ | Решение владельца 18.09.2026, дословно: «накладная падает в прочее если ее не разнесли. учет первостепенен. корректность уже на совести менеджмента». То есть полнота денег важнее чистоты отчёта: трата, исчезнувшая из P&L молча, опаснее шумной строки, а следить за разбором — работа менеджмента, а не продукта. Название служебной строки остаётся «Не разобрано», а не «Прочее» — подтверждено владельцем в тот же день («ок»): «Прочее» звучит как законное место, где трата может остаться навсегда | owner |
| D077 | 2026-09-18 | **Отчёт движения наличных (Q008) откладывается; приоритет — модель учёта окладов и ведение этой части (очередь 5 продуктовой карты).** Остаток кассы и отчёт по кассам в работу сейчас не берутся ни в каком объёме, включая минимальный на один день | Решение владельца 18.09.2026, дословно: «пока хороним идею на потом. нам крайне важно запустить именно модель учета окладов и ведения этой части». Проверено перед оценкой: функции остатка кассы в коде нет — есть касса `Till`, чеки и факты с видом `transfer`, исключённые из P&L, то есть данные для отчёта на месте, а сам отчёт не начат. Отложено не из-за дороговизны (2–3 дня), а потому что очередь 5 важнее | owner |
| D078 | 2026-09-18 | **Переименование идёт на всех слоях, а не только в видимом.** Отменяет границу D071 и D072 («имя репозитория, пакеты, схема базы, переменные окружения и адреса не трогаются»): техническое имя теперь `maximus` везде — репозиторий `GarroV/maximus`, пакет, compose-проект, имена баз, CI, каталоги перевода. Конвенция взята у соседних продуктов: `decimus`, `meridius` — значит `maximus`, а не `dodo_maximus_service`. Хроника в журналах блоков (имена копий и стендов прошлых прогонов вида `dodo_pnl_service-debt`) не переписывается: это запись о том, что было, а не действующая инструкция | Решение владельца 18.09.2026, дословно: «переименовывай вообще все». Записано задним числом 19.09.2026: решение было исполнено коммитом `fe83f79` и названо только в его сообщении, а в этом файле продолжали стоять D071 и D072 с прямо противоположной границей. Следующая сессия прочитала бы их как действующие | owner |
| D079 | 2026-09-21 | **Публичность репозитория `GarroV/maximus` и беклог в нём остаются как есть.** Вопрос был поднят потому, что глобальное правило владельца требует приватного беклога, а репозиторий проекта — публичный, и в его issues уже уехали адрес стенда и порты | Владелец на прямой вопрос «удалить issue, перевести репозиторий в приватные или оставить» ответил дословно: «похер». Записано, чтобы следующая сессия не открыла тот же вопрос заново и не перевела репозиторий в приватные по общему правилу: здесь оно осознанно не применяется. Границу это не отменяет — находка по безопасности по-прежнему идёт приватным advisory, а не задачей | owner |
