# Блок: timesheets

## Назначение

Ввод часов — то место, где продукт либо экономит бухгалтеру время, либо не нужен ему
вовсе. Два равноправных пути: сетка в интерфейсе и загрузка привычной таблицы.
Выделен отдельно от расчёта, потому что часы живут своей жизнью: их вводят, правят и
закрывают по точкам до того, как считается зарплата.

## API-контракт

```python
timesheet_for(employee_id: UUID, period: date) -> Timesheet
    # собирает из подневного хранения то, что принимает движок:
    # hours по типам, insured_hours, norm_hours

import_partner_table(file, tenant_id: UUID, period: date) -> ImportResult
    # ImportResult: matched: int, created: int, updated: int, unchanged: int,
    #               unmatched_rows: list[UnmatchedRow]  — некому записать,
    #               skipped_rows: list[UnmatchedRow]    — есть кому, но некуда,
    #               warnings: list[Note], findings: list[Finding]
    # идемпотентно: повторная загрузка того же файла ничего не меняет
```

```
GET  /timesheets/<uuid:period_id>/         сетка «сотрудник × тип часа»
POST /timesheets/<uuid:period_id>/cell/    обновление одной ячейки (htmx-фрагмент)
POST /timesheets/<uuid:period_id>/piece/   сдельная величина строки (T075)
POST /timesheets/<uuid:period_id>/import/  загрузка таблицы партнёра (T020)
POST /timesheets/<uuid:period_id>/close/   закрыть часы точки за месяц (T022)
POST /timesheets/<uuid:period_id>/reopen/  открыть часы точки заново (T022)
```

Закрытие и открытие — разные адреса, а не значение поля в общей форме: два
действия с разными последствиями не должны отличаться содержимым скрытого поля.

Период адресуется id, а не датой: так же, как во всём остальном интерфейсе
(`/periods/<uuid>/`).

**Отчёт загрузки разводит два повода не попасть в табель** (T122):
`unmatched_rows` — человека нет в справочнике партнёра, `skipped_rows` — человек
найден, но строку записывать было некуда (часы точки закрыты, условий найма на
месяц нет). Один счётчик на оба врал про класс события.

**Подозрительные числа — одно правило на оба пути ввода** (`timesheets/suspicion.py`,
T118). Часов больше нормы, строка без часов, отрицательные часы, часы у
уволенного до начала месяца. Правило зовут и загрузка файла, и сетка; текст
подсказки собирается в одном месте, а вызывающий добавляет своё: импорт — лист
файла, сетка — саму строку. Подсказка **не мешает сохранить**: ни одно из этих
чисел не ошибка само по себе.

**Хранение подневное**, даже когда вводится итог за месяц: иначе разнесение по точкам
при переводе сотрудника посреди месяца потребует миграции данных. Ввод итогом
раскладывается по рабочим дням календаря.

## Зависимости

Блоки: `web` (каркас и компоненты), `db` (таблицы табелей и календарей).

Внешнее: openpyxl (уже используется существующим импортёром).

## Definition of Done блока

- [x] Сетка вводится с клавиатуры без мыши: перемещение, ввод, сохранение
- [x] Изменение ячейки сохраняется без перезагрузки страницы и не теряется при
      случайном уходе со страницы
- [ ] Загрузка эталонной таблицы июня 2026 даёт корректный табель на 30 сотрудников
- [ ] Повторная загрузка того же файла ничего не меняет — тест на идемпотентность
- [ ] Испорченный файл (лишний лист, переименованная колонка, текст вместо числа)
      не молчит: строки попадают в `unmatched_rows`, пользователь видит отчёт
- [ ] Подсказки о подозрительном: часов больше нормы, сотрудник без часов,
      отрицательное значение, часы у уволенного
- [x] Закрытие часов по точке отдельно от других точек
- [x] Тест: управляющий не видит и не может править часы чужой точки

## Журнал

### 2026-08-07 — T019, сетка ввода часов

**Что строится.** Экран табеля: строки — сотрудники периода, колонки — типы часов
из пресета страны. Ячейка сохраняется сразу, без кнопки «сохранить всё».
Импорт (T020), отчёт проверок (T021) и закрытие по точке (T022) — третья очередь,
здесь их нет.

**Решение 1: подневная таблица — детализация к строке табеля, а не замена ей.**
D011 требует подневного хранения. Прямолинейный вариант — переделать `timesheets`
в подневную таблицу — сломал бы вход движка: `payrun.calc.collect_cases` читает
`timesheets.hours` как готовый месячный итог, а приёмка второй очереди снята на
конкретных числах. Поэтому заведена `timesheet_days`, ссылающаяся на строку
`timesheets`:

- `timesheets` остаётся тем, что читает движок: месячный итог по типам часов плюс
  поля, которых у дня нет и быть не может — `insured_hours`, `norm_hours`,
  `deduction`, `cash_payout`, `manual_correction`;
- `timesheet_days` — из чего этот итог сложился: дата, тип часа, часы.

Инвариант: `timesheets.hours[тип]` равно сумме дней этого типа. Держится
единственной точкой записи (`timesheets.store`), не формой и не триггером.

Почему это выполняет D011, а не имитирует его. Смысл требования — не переносить
данные, когда сотрудника переводят между точками посреди месяца. Точка стоит на
строке `timesheets` (её ключ — `tenant + employee + period + unit`), то есть
человек на двух точках это две строки. Переезд без подневных данных потребовал бы
угадать, какие часы куда; с ними строка режется по датам механически. Дублировать
точку ещё и на дне не стали: два места для одного факта разъезжаются.

**Решение 2: месячное число раскладывается поровну по рабочим дням.** Партнёр
вводит итог за месяц (D011) — какие именно дни за ним стоят, продукт не знает и
знать не может. Раскладка ровная, по рабочим дням производственного календаря
страны (`calendars.holidays` минус выходные); календаря на месяц нет — пятидневка.
Копейки часа распределяются по остатку: сумма дней **точно** равна введённому
числу, иначе итог поехал бы от самого факта хранения. Каждый день помечен
`source='spread'` — когда придут настоящие подневные данные из Dodo IS, ровную
раскладку будет видно и можно будет отличить от факта.

**Решение 3: строка переходит на подневное хранение целиком и один раз.** Строки
сида и импорта содержат месячные итоги без дней. При первой же правке любой ячейки
строки раскладываются **все** её типы часов, а не только правленый: иначе строка
осталась бы наполовину подневной, и «сумма дней равна итогу» перестало бы быть
проверяемым утверждением. Раскладка сумму сохраняет, поэтому расчёт после неё даёт
те же числа — на это есть тест.

**Решение 4: `insured_hours` сетка не трогает.** Это база для взносов, она не
равна сумме часов (у схем с половиной ставки — своя). По какому признаку сотрудник
попадает в такую схему — открытый вопрос Q005. Пересчитывать её от введённых часов
значило бы придумать правило за бухгалтера и получить правдоподобно неверный
расчёт. Поле остаётся тем, чем его заполнил импорт; в сетке показано отдельной
колонкой только для чтения, чтобы расхождение было видно, а не пряталось.

**Решение 5: грид не подключается.** D017 разрешает Tabulator, но велит начинать с
самого дешёвого. Дешевле некуда: обычные `<input>` в обычной таблице. Tab-порядок,
фокус, доступность и ввод с клавиатуры у браузера уже есть — грид их сначала
отнимает, потом возвращает своим кодом за 400 КБ. Стрелки между ячейками — 40 строк
своего JS. Что из Tabulator реально понадобится (вставка из буфера обмена,
диапазоны) — вопрос импорта, то есть T020, и решать его заранее нечем.

**Решение 6: htmx 2.0.10, файлом в пакете.** Пин версии обязателен (D017): в npm
уже лежит `4.0.0-beta6`, и CDN без пина однажды подменил бы библиотеку молча.
Файл лежит в `src/timesheets/static/` — то есть внутри пакета, а не в корневом
`static/`: в образ копируется только `src/`, и статика в корне до контейнера бы не
доехала. Контрольная сумма файла записана рядом в шапке.

**Решение 7: «не теряется при уходе со страницы» — два пути, не один.** Обычный —
`hx-trigger="change"`: значение уходит на сервер при уходе фокуса с ячейки, то есть
при любом переходе к следующей. Но если человек закрывает вкладку прямо из
поля, браузер вправе оборвать незавершённый запрос. Поэтому ячейки, ответ по
которым ещё не пришёл, досылаются на `pagehide` через `navigator.sendBeacon` —
запрос, который браузер обязан доставить после ухода со страницы. Без второго пути
критерий задачи выполнялся бы «обычно», а обнаруживалось бы это потерянными часами.

**Решение 8: сетка показывает только свои точки.** Управляющий с непустым
`unit_ids` видит и правит строки только своих точек — и на странице, и на записи
ячейки. Это не T022 (закрытие часов по точке) и не T044 (фильтр в ведомости):
это отказ строить редактируемый экран с заведомо открытой дырой. Полное
разграничение по точкам в базе — задача T022.

**Отклонение от контракта блока.** В контракте маршрут записан как
`/timesheets/{period}`; фактически `/timesheets/<uuid:period_id>/`, потому что
периодами весь остальной интерфейс адресуется по id (`/periods/<uuid>/`), а не по
дате. Контракт выше поправлен.

### 2026-08-07 — что фактически проверено

**Тесты.** `229 passed, 8 skipped` на полном наборе (пропуски — сверка с
настоящей таблицей партнёра, её в репозитории нет по D028). Из них 27 новых в
`tests/test_timesheets.py`. Тесты доступа к `timesheet_days` гоняются ролью
`app_user`: изоляция тенантов, пустота без контекста, отказ на запись в чужого
тенанта, каскадное удаление дней вместе со строкой табеля.

**Суммы не сдвинулись.** На чистом сиде расчёт июня даёт 107 компонентов на
1 951 806,13 у директора и 44 на 464 752,41 у бухгалтера — ровно ориентир
задания. После перевода **всех** 35 строк на подневное хранение (1540 дней)
пересчёт даёт те же 107 на 1 951 806,13. Это же свойство закреплено тестом
`test_calculation_survives_materialization`, чтобы оно не держалось на удаче.

**Живой браузер, 17 проверок из 17.** Headless Chrome по протоколу отладки,
настоящие события клавиатуры (`Input.dispatchKeyEvent`), сценарий —
`smoke.mjs`, зависимостей нет, `WebSocket` встроен в Node.

| проверка | результат |
|---|---|
| вход логином и паролем, ссылка на табель со страницы периода | есть |
| htmx поднялся из файла | версия 2.0.10 |
| сетка построена | 35 строк, 210 ячеек |
| набор с клавиатуры, Enter → строка ниже | фокус перешёл, тот же тип часа |
| ячейка сохранена без перезагрузки, значение приведено к виду базы | `123.50` |
| итог строки пересчитан на месте | 203,50 |
| стрелки вправо и вверх | колонка и строка сменились |
| ушли со страницы и вернулись | `123.50` на месте |
| закрыли страницу прямо из поля | `77.25` доехало (sendBeacon) |
| текст вместо числа | видимый отказ: «„восемь“ — это не число часов» |
| управляющий | 17 строк, единственная точка NS1 |
| консоль браузера | чиста; после намеренной ошибки — только ожидаемый 422 |

**Чего проверить не удалось.** Ничего из заявленного: все пункты закрыты
фактически. Не проверялось то, чего в задаче нет, — импорт (T020), отчёт
проверок (T021), закрытие по точке (T022).

### 2026-08-07 — что осталось незакрытым и почему

* **Кто правил ячейку — не пишется.** След есть только у ручной правки
  бухгалтера (`manual_correction`), у обычного ввода часов автора нет.
  Придумывать под это колонки на ходу — расширение скоупа; но назвать пробел
  надо: когда табель начнут закрывать (T022) и утверждать (T023), «кто поставил
  эти 176» станет вопросом, на который система не ответит.
* **Разграничение по точкам живёт в представлении, не в базе.** Экран чужие
  строки не показывает и не принимает, но политика `timesheet_days` и
  `timesheets` точку не смотрит: прямой запрос ролью `app_user` под управляющим
  видит все точки тенанта. Это ровно то, что должна закрыть T022; на текущее
  поведение стоит тест, который обязан упасть, когда T022 будет сделана.
* **Ровная раскладка — не факт, а следствие ввода одним числом.** Дни помечены
  `source='spread'`, но продукт нигде не показывает, что даты за ними условные.
  Когда появится подневный источник (Dodo IS), это придётся объяснить на экране.

### 2026-08-07 — найдено по ходу: рабочие копии делят один docker-стек

`docker-compose.yml` жёстко задаёт `name: dodo-pnl`. Имя проекта Compose при этом
одно на машину, а не на каталог, — значит **все рабочие копии репозитория
работают с одними и теми же контейнерами и томами**. Проверено фактически, а не
предположено: посреди этой работы параллельный блок поднял стек из своей копии
(порт приложения сменился с 8039 на 8036), а затем снёс его вместе с томом —
моя база с сидом исчезла в середине смоука.

Последствия шире неудобства: `docker compose down -v` из любой копии уничтожает
базу соседа, а `up` молча меняет ему порты. При параллельной стройке это не
редкая случайность, а поведение по умолчанию.

Обход, которым доделана эта работа: задавать имя проекта своим —
`COMPOSE_PROJECT_NAME=dodo-pnl-timesheets docker compose up -d`. Починка — либо
убрать `name` из файла (тогда Compose возьмёт имя каталога и копии разойдутся
сами), либо параметризовать его переменной с умолчанием. Сам файл не трогал: он
общий, а править общее из блока — ровно тот способ терять чужие изменения,
против которого стройка и разведена по копиям.

### 2026-08-07 — починки после сверки со спекой (T061, T062, T066)

Свежий взгляд прошёл продукт как пользователь и нашёл три вещи в этом блоке.
Первая — серьёзная: ввёл 8,5 праздничных часов, «Итого» стало 96,50, а «База
взносов» осталась 88,00, и период посчитался без единого предупреждения.

**Решение 9: база взносов идёт за часами там, где она за ними шла, а где не
шла — расчёт останавливается.** Это оба пути задания сразу, и вот почему ни
один по отдельности не годится.

Пересчитывать базу всегда нельзя. В таблице бухгалтерии это **отдельная
колонка** (`UKUPNO SATI ZA OBRACUN DOPRINOSA`), а не сумма часов, и по какому
признаку человек попадает в схему с минимальной базой — открытый вопрос Q005.
Считать её от введённых часов у всех значило бы придумать правило за
бухгалтера. Решение 4 прошлого захода в этой части остаётся в силе.

Но и оставить как было нельзя: `insured_hours` — вход движка наравне с часами
(`engine.gross_up` и `engine.contributions` читают её в трёх методах), и
устаревшая база даёт правдоподобно неверный расчёт без падения. Конституция,
принцип 1.

Поэтому правило узкое и проверяемое: **сохраняется связь, которая была.** Перед
записью ячейки сравнивается, равнялась ли база сумме часов тех типов, что
пресет помечает `insured: true`. Равнялась — значит база шла за часами, и после
правки она идёт за ними дальше. Не равнялась — значит она задана независимо, и
трогать её нечем: строка помечается в сетке, а `payrun.calc.check_insured_base`
отказывается считать период, называя людей поимённо.

Два следствия, которые стоит знать дальше:

* Флаг `insured` у типов часов был объявлен в пресете с самого начала и не
  читался никем. Теперь у него есть потребитель — `payroll.insured_base`.
  Правило «какие часы входят в базу» осталось в пресете, а не переехало в
  интерфейс.
* Проверка при расчёте смотрит только те схемы, которые базу читают
  (`payroll.uses_insured_hours`). У курьеров (`direct`) и временных работ
  (`temporary`) её нет ни в одной формуле — блокировать их из-за неё было бы
  отказом на пустом месте. Проверено тестом.

Чего это **не** решает: если у партнёра база и правда должна отличаться от
отработанного (контрактные застрахованные часы), человек упрётся в отказ и не
сможет его снять из интерфейса. Снять его правильно нельзя, пока нет ответа на
Q005: пришлось бы либо разрешить любое число (тогда проверка бессмысленна),
либо завести пометку «подтверждено бухгалтером», то есть колонку в схеме и
миграцию — выдумывать её под неизвестное правило рано. На данных сида и июня
2026 расхождений нет ни у кого, так что сегодня отказ не срабатывает ни разу.

**Решение 10: табель отказывает так же, как страница периода.** Колонки сетки —
это типы часов страны, то есть те же правила, на которых стоит расчёт. Нет
правил на месяц — `PayrunRefused` вылетал наружу из представления, и при
`DJANGO_DEBUG=0` человек видел «Server Error (500)». Теперь тот же отказ
показывается плашкой с той же командой (`python manage.py load_presets`) и тем
же кодом ответа 409, что на странице периода. Проверено на поднятом стенде с
`DJANGO_DEBUG=0`.

**Решение 11: объяснение отказа живёт у ячейки, а значение возвращается из
базы.** Три отдельные вещи, и каждая была своим дефектом:

* Подсказка была `<p>` внизу документа. При 35 строках низ страницы за экраном
  (сверка намерила `top=1571` при высоте окна 900), то есть видимый отказ был
  видимым только на бумаге. Теперь это фиксированный блок, который скрипт
  ставит у самой ячейки; в окне 1440×900 он оказывается в 22 px от неё.
* Пометка ячейки была бледной заливкой, неотличимой от соседних. Отказ —
  единственное состояние, ради которого стоит нарушить правило «полоска, а не
  заливка»: рамка, цвет текста и жирное начертание. Ячейку видно в столбике.
* В поле оставался введённый мусор. Теперь сервер и при отказе возвращает
  заголовком то, что **осталось в базе**, и поле возвращается к нему. Правило
  экрана одно и то же в обе стороны: показано ровно то, что подтвердила база.

### Что T020–T022 стоит знать про эту модель

* Импорт (T020) должен писать **через `timesheets.store`**, а не прямым
  `update` месячного итога: иначе итог разойдётся с днями, и инвариант,
  на котором стоит вся конструкция, перестанет действовать. Функция
  `set_cell` для этого годится как есть, `materialize` — тоже.
* Идемпотентность импорта модель не ломает: запись одного и того же числа
  дважды даёт те же дни (тест `test_repeated_save_changes_nothing`).
* Закрытие часов по точке (T022) режется по строке `timesheets` — точка стоит
  на ней, а не на дне. Разнесение часов при переводе внутри месяца делается
  разрезанием дней по дате на две строки; переносить данные не нужно.

### 2026-08-07 — что фактически проверено по починкам

**Тесты.** `262 passed, 8 skipped` на полном наборе (пропуски прежние — сверка
с настоящей таблицей партнёра, её в репозитории нет по D028). Девять новых в
`tests/test_timesheets.py`. Главный из них проверен на порчу: с отключённым
пересчётом базы он падает ровно тем числом, что нашла сверка, — «88,00 вместо
96,50».

**Суммы не сдвинулись.** На чистом сиде расчёт июня даёт 107 компонентов на
1 951 806,13 и 44 на 464 752,41 в официальном регистре — ровно ориентир
задания.

**Живой браузер, 37 проверок из 37.** Headless Chrome по протоколу отладки,
окно задано ровно 1440×900 (`Emulation.setDeviceMetricsOverride`: у headless
высота окна и высота страницы разные, и проверка «видно без прокрутки» на 813
точках доказывала бы не то).

| проверка | результат |
|---|---|
| прежний смоук табеля целиком (`smoke_timesheets.mjs`) | 17 из 17, без изменений |
| ввели 8,5 праздничных: «Итого» и «База взносов» | 88 → 96,50 обе, без перезагрузки |
| после возврата на страницу | те же два числа |
| период после правки часов | считается |
| база рассинхронизирована мимо экрана (SQL) | строка помечена, видно «по часам 176,00» |
| «Посчитать период» на рассинхронизированной базе | отказ с именем человека |
| правил на месяц нет, `DJANGO_DEBUG=0` | 409 и плашка с командой, не «Server Error» |
| мусор в первой строке: объяснение | видно, в 22 px от ячейки, `top=303` |
| ячейка после отказа | рамка `rgb(154,52,18)`, начертание 600, `aria-invalid` |
| значение после отказа | вернулось прежнее `12.00`, в базе тоже `12.00` |
| консоль браузера | чиста; после намеренной ошибки — только ожидаемый 422 |

**Чего проверить не удалось.** Случая, ради которого отказ и сделан: данных, где
база взносов законно отличается от отработанного, ни в сиде, ни в июне 2026 нет.
Расхождение приходилось создавать руками через SQL. Пока Q005 без ответа,
проверить можно только механику отказа, а не его правоту.

### 2026-08-07 — T073: отказ на записи ячейки вместо ошибки сервера

**Что было.** `grid` объясняет отсутствие правил с T062, `cell` — нет:
`PayrunRefused` вылетал из представления наружу. Проверено фактически, а не по
коду: тест, написанный до правки, получил не 500 даже, а необработанное
исключение — то есть при `DJANGO_DEBUG=0` человек, набирающий часы, увидел бы
голое «Server Error».

Сценарий узкий (правила перестают действовать между открытием страницы и
досылкой ячейки), но именно в нём человек уже работает: страница открыта, часы
набраны, и ответ приходит на его ввод.

Правка:

| что | зачем |
|---|---|
| `views._refused(status, text, row, hour_type)` | один способ отказать на записи ячейки: код ответа, текст для человека и значение **из базы** заголовком. Заведён, чтобы новая причина отказа не завела себе третий способ объясняться |
| `except PayrunRefused` в `cell` | тот же текст и тот же код 409, что на странице табеля |
| `grid.js` | 409 показывается тем же механизмом, что мусорное значение (T066) и отказ по правам (T064) — плашкой у ячейки, а не третьим способом |

Решение, принятое по ходу: **хвост сообщения у 409 свой.** У 422 и 403 сказано
«не принято, в ячейке прежнее значение» — это правда: до базы ввод не дошёл. У
409 правдой это быть не обязано: отказ приходит и от проверки правил перед
записью (значение прежнее), и — теоретически, если правила пропадут внутри
одного запроса — от пересборки сетки после удачной записи (значение новое).
Поэтому сказано то, что верно всегда: в ячейке то, что сейчас в базе, — сервер
прислал это значение заголовком.

Тест — `test_cell_explains_missing_rules_instead_of_500` в
`tests/test_timesheets.py`: правила сдвигаются на 2030 год, POST ячейки обязан
дать 409 с той же командой `load_presets`, вернуть заголовком значение из базы
и ничего в ней не изменить. Проверен порчей: с отключённой веткой падает
исходным `PayrunRefused`.

Прогон целиком: **303 passed, 8 skipped** (пропуски прежние — сверка с
настоящей таблицей партнёра, файла в репозитории нет, D028). `ruff check .`
чист.

**Проверка в живом браузере** (тот же стенд и тот же сценарий,
`tools/smoke_permissions.mjs`): под директором ячейка сохранена обычным путём —
клик мышью, набор с клавиатуры, Enter, — затем правила сдвинуты на 2030 год и
тот же ввод повторён. Плашка появилась **в 22 px от ячейки**, внутри окна, с
текстом «в базе нет правил расчёта … python manage.py load_presets»; в поле
вернулось `12.00` — то, что в базе; ячейка помечена `aria-invalid`. Страница
табеля целиком в том же состоянии объясняет то же самое. После возврата правил
ячейка снова пишется.

**То же самое при выключенной отладке** (`DJANGO_DEBUG=0`, приложение
перезапущено, запросы обычным `curl` с настоящей сессией): `POST …/cell/`
отвечает `409 Conflict`, `Content-Type: text/plain`, `X-Cell-Value: 12.00` и тем
же текстом; `GET` страницы табеля — тоже 409. Строки «Server Error» нет ни в
ответе, ни на странице. Это и была цена дефекта: при выключенной отладке других
слов человек не увидел бы.

Сюда же относится половина T072 (блок `web`, запись в его журнале): роль без
права `timesheet.edit` получает сетку **на чтение** — числа те же, полей ввода
нет, htmx и `grid.js` на такую страницу не грузятся вовсе. Серверная проверка
в `cell` остаётся на месте: интерфейс её не заменяет.

### 2026-08-08 — третья очередь: T020, T021, T022 (в работе)

**Стенд.** Своя копия `…-ts3`, ветка `feat/ts3`, порты 8049/5457, имя
compose-проекта `dodo-pnl-ts3`. Базовый прогон до правок — зелёный.

**После вливания master** (принятые блоки интерфейса и db, миграция
`0023_totals_by_role`) базовый прогон — **309 passed, 8 skipped**. Это точка
отсчёта: всё, что ниже, сравнивается с ней.

**План третьей очереди** (записан до кода, чтобы обрыв связи не унёс замысел):

* T020 — `src/timesheets/importer.py` поверх `payroll.importers.plata_xlsx`.
  Сопоставление по `employees.external_id` (сид кладёт туда имя из таблицы),
  точка берётся из условий найма, а не из имени листа: соответствие
  «лист → точка» приблизительное и живёт только в сиде. Запись — через
  `timesheets.store`, чтобы не разошёлся инвариант «итог равен сумме дней».
  Идемпотентность строгая: совпало — **не пишем вовсе**, иначе `_write_days`
  пересоздаст дни с новыми `id` и `created_at`, и «ничего не изменилось» стало
  бы неправдой при равных числах.
* T021 — разбор возвращает не только строки, но и находки: чужие листы,
  ненайденные колонки, пропущенные строки, часы больше нормы, сотрудник без
  часов, часы у уволенного. Отчёт показывается на экране табеля.
* T022 — миграции `0030` (видимость дней по точке) и `0031` (закрытие часов
  по точке). Номера с 0030 потому, что в master последняя `0023`, а номера с
  0040 занимает параллельный блок payrun. Закрытие — таблицей `unit_closures`
  и ограничивающими политиками на запись, а не проверкой в Python.

### 2026-08-08 — T022: замысел до кода

**Стенд.** Копия `…-ts4`, ветка `feat/ts4`, порты 8052/5460, имя compose-проекта
`dodo-pnl-ts4`. Базовый прогон до правок — зелёный, 8 пропусков (сверка с
настоящей таблицей партнёра, файла в репозитории нет, D028).

Предыдущая попытка T022 отброшена целиком: код был написан, но ни разу не
запущен. Замысел ниже — переоценённый, а не унаследованный.

**Что закрывается.** Две дыры и одно новое действие:

1. *Дни чужой точки видны.* На `timesheets` политика по точкам стоит (T044), на
   `timesheet_days` — нет: у дня своей точки не бывает, а через родителя условие
   не проверялось. Прямой запрос ролью `app_user` под управляющим отдаёт дни всех
   точек тенанта. На это стоит тест, который сегодня утверждает дыру и обязан
   быть переписан (`test_manager_cannot_read_days_of_another_unit`).
2. *Закрытия часов нет вовсе.*
3. Право `unit.close` в ролях есть с самого начала и ничего не решает
   (миграция `0022` называет это прямо: политика без экрана — догадка). Теперь у
   него появляется экран, значит появляется и политика.

**Форма.** Миграции `0030` (видимость дней) и `0031` (закрытие). Номера с 0030
— диапазон `0040+` занят блоком payrun, два листа в графе Django ломают слияние.

**Закрытие — строка в таблице, а не колонка статуса у точки.** Точка живёт
годами, закрытие относится к паре «точка + месяц»: колонка на `units` не смогла
бы выразить «июнь закрыт, июль открыт». Отсюда `unit_closures` с ключом
`tenant + unit + period`.

**Открытие обратно — тем же правом и без причины.** Причина обязательна при
откате утверждённого периода (T025): там переписывается уже выданная ведомость.
Здесь закрытие — операционная отсечка «я закончил вводить», и человек, нажавший
её по ошибке, не должен ждать бухгалтера. Действующим закрытием считается строка
с пустым `reopened_at`, а не удалённая строка: история закрытий — единственный
ответ на вопрос «когда точка была готова», и удаление стирало бы его.

**Запрет — политиками, а не проверкой в Python.** Та же логика, что у видимости
точек (T044) и прав роли (T064): приложение объясняет словами, база гарантирует.
Форма политик взята у `0022`: `for insert/update with check` — отказ громкий;
тихого «изменено 0 строк» здесь допустить нельзя, иначе гонка (точку закрыли
между открытием страницы и досылкой ячейки) выглядела бы как удачная запись.

**Как это сочетается с заморозкой утверждённого периода (T023).** Проверено по
коду `0041`, а не предположено: заморозка стоит на `payruns`, `payslips`,
`payslip_totals`, `pay_components` и табеля **не трогает** вовсе (правка входных
данных задним числом — T026). Пересечения по таблицам нет, поэтому один и тот же
отказ двумя разными словами не объясняется: на пути записи в табель запрет ровно
один — закрытие точки, а на пути записи в расчёт — ровно один, утверждение.
Разница в механизме намеренная и объяснимая: утверждение — юридическая
заморозка, её обязан соблюдать и суперпользователь, поэтому триггер; закрытие —
операционная отсечка внутри партнёра, её соблюдают пользователи приложения,
поэтому политика (обслуживание и сид обязаны иметь возможность писать).

**Порядок работ:** тесты (ролью `app_user` и через экран) → `0030` → `0031` →
приложение и экран → смоук в живом браузере.

### 2026-08-08 — T022: как сделано и что решено по ходу

**Решение 12: закрытие — своя таблица, а не колонка статуса.** `unit_closures`
из прошлого замысла названа `timesheet_closures`: у `units` уже есть `closed_at`
в смысле «пиццерия закрылась совсем», и два «закрыто» рядом читались бы как одно.
Ключ — «тенант + точка + месяц», действующее закрытие это строка с пустым
`reopened_at`, частичный уникальный индекс держит «одно действующее на точку и
месяц». Открытие заново не удаляет строку, а помечает её: закрывать и открывать
можно много раз, и «когда точка была готова» — вопрос, на который система обязана
отвечать.

**Решение 13: запрет — политиками, заморозка расчёта — триггерами, и это не
противоречие.** Разобрано по коду `0041`, а не предположено: заморозка
утверждённого расчёта стоит на `payruns`, `payslips`, `payslip_totals`,
`pay_components` и табеля **не трогает вовсе** (правка входных данных задним
числом — T026). Пересечения по таблицам нет, поэтому один и тот же отказ двумя
словами не объясняется: на пути записи в табель запрет ровно один — закрытие
точки; на пути записи в расчёт — ровно один, утверждение.

Разница в механизме намеренная. Утверждение — заморозка уже выданной ведомости,
её обязан соблюдать и суперпользователь, поэтому триггер. Закрытие часов —
операционная отсечка внутри партнёра: её соблюдают пользователи приложения, а
обслуживание (сид, восстановление из дампа) обязано мочь писать. Поэтому
политики, там же, где вся остальная изоляция (D014, T044, T064).

**Решение 14: закрыты обе таблицы табеля, и запрет читается через родителя.**
Месячный итог собирается из дней (`store`), поэтому закрыть `timesheets` и
оставить `timesheet_days` значило бы запереть парадный вход, оставив чёрный.
У дня своей точки нет — она у строки-родителя; правило живёт в одной функции
`timesheet_row_closed()`, а не повторяется условием на каждой таблице.

Обе функции — `security definer`. Иначе политика запрета читала бы закрытия
глазами того, кого она ограничивает, и невидимое закрытие означало бы «не
закрыто»: запрет снимался бы сам собой у того, кто его не видит.

**Решение 15: у права `unit.close` наконец появился потребитель.** Оно стояло в
ролях с самого начала и ничего не решало — `0022` называет это прямо: политика
без экрана есть догадка. Теперь есть экран, поэтому есть и политика. Право не
подразумевается из других: у директора сида его нет, и его не пускает ни
интерфейс (кнопки нет, сказано почему), ни база.

Побочное следствие, которое стоит знать: **закрыть точку сегодня может только
управляющий**. Так устроен сид ролей, и это соответствует спеке («управляющий
вводит и закрывает часы по своей точке»), но директор, желающий закрыть точку за
уехавшего управляющего, этого не сможет. Права ролей ведёт блок db/auth — менять
их из этого блока значило бы править чужое; названо здесь, чтобы не потерялось.

**Решение 16: загрузка таблицы отказывает построчно, а не падает.** Транзакция у
импорта одна на файл, и одна закрытая точка уносила бы вместе с собой все
остальные листы, а человек прочитал бы «файл не удалось прочитать как книгу
Excel» — неправду о причине. Теперь строки закрытой точки попадают в отчёт
поимённо, остальное загружается.

**Граница, названная честно.** Политика правки — `with check` без `using`
(форма из `0022`, чтобы отказ был громким, а не тихим «изменено 0 строк»).
Следствие: `update`, **переносящий** строку табеля из закрытой точки в открытую,
политикой не ловится. Сегодня такого пути нет ни в интерфейсе, ни в импорте —
точка строки берётся из условий найма и не меняется; разнесение при переводе
внутри месяца делается разрезанием дней, и запрет для него надо писать вместе с
ним, а не угадывать сейчас. Тихий отказ здесь был бы хуже: точку могут закрыть
между открытием страницы и досылкой ячейки, и «изменено 0 строк» приложение
приняло бы за удачную запись.

**Строки без точки не закрываются никогда.** `unit_id` обнуляется при удалении
пиццерии, такая строка не принадлежит ни одной точке: закрыть её нечем, а прятать
от правки значило бы потерять часы молча.

### 2026-08-08 — T022: что фактически проверено

**Тесты.** `407 passed, 8 skipped` на полном наборе (пропуски прежние — сверка с
настоящей таблицей партнёра, файла в репозитории нет, D028). Из них 22 новых:
21 в `tests/test_unit_closing.py` и один в `tests/test_timesheet_import.py`.
Все проверки запретов идут **ролью `app_user`** — владелец схемы в этой базе
суперпользователь, и без переключения роли они были бы зелёными всегда.

Тест `test_manager_cannot_read_days_of_another_unit`, который с T019 утверждал
дыру как факт и должен был упасть при T022, — упал и переписан.

| проверка (ролью `app_user`) | результат |
|---|---|
| правка часов закрытой точки | отказ, значение в базе прежнее |
| новая строка табеля в закрытой точке | отказ |
| подневная запись в закрытую точку | отказ |
| закрыли одну точку — соседняя пишется | пишется |
| закрыт июнь — июль той же точки пишется | пишется |
| открыли заново — часы снова пишутся | пишутся |
| управляющий закрывает свою точку | проходит |
| управляющий закрывает чужую | отказ |
| директор (нет `unit.close`) закрывает | отказ |
| управляющий видит закрытия | только своей точки |
| управляющий снимает чужое закрытие | 0 строк, `reopened_at` пуст |
| закрытия другого тенанта | не видны |
| дни чужой точки: чтение и запись | не видит, не пишет |

**Живой браузер, 29 проверок из 29.** Headless Chrome по протоколу отладки,
окно 1440×900, сценарий `tools/smoke_closing.mjs`; закрытие и открытие —
настоящими нажатиями кнопок, ввод часов — мышью и клавиатурой.

| проверка | результат |
|---|---|
| управляющий видит панель точек | только NS1 |
| нажал «Закрыть часы точки» | «часы закрыты», полей ввода 0 из 102 |
| запись ячейки мимо экрана (fetch с настоящей сессией) | 409, «Часы точки NS1 за Июнь 2026 закрыты», значение из базы заголовком |
| директор правит соседнюю точку при закрытой NS1 | `101.00` записано |
| строки закрытой точки на общем экране | без полей ввода |
| у директора (нет права) кнопки закрытия | нет, объяснение есть |
| закрытие директором мимо экрана | 403 с тем же объяснением |
| «Открыть заново» | поля вернулись, `7.00` записано |
| консоль браузера | чиста |
| числа ведомости: директор / бухгалтер / управляющий | 60 и 1 951 806,13 / 33 и 464 752,41 / 24 и 891 373,32 |
| норма часов у всех трёх ролей | 176,00 |

**Найдено по ходу и заведено issue #61: две одновременные досылки одной ячейки
роняют запись 500-й.** Смоук поймал `duplicate key ... timesheet_days_uniq` в
`store.materialize`. Причина не в закрытии: `grid.js` досылает несохранённую
ячейку на `pagehide`, а переходов подряд бывает два — уходят две одинаковые
досылки, обе видят «дней нет», вторая падает на уникальном ключе. Записи в
`set_cell` не хватает блокировки строки-родителя. Дефект старше T022 и
воспроизводится на любой ячейке; чинить его здесь — расширение скоупа.

**Найдено по ходу: `seed_dev` падает на базе, где период хоть раз утверждали** —
журнал переходов (T023) не даёт Django удалять свои записи, потому что каскад он
исполняет в Python. Уже заведено соседним блоком (issue #60) и там же починено;
здесь обошёл пересозданием базы.

**Чего проверить не удалось.** Ничего из заявленного: все пункты закрыты
фактически. Не проверялось то, чего в задаче нет, — построчная блокировка
(T027), утверждение и откат периода (T025).

### T075 — курьеры: часы и сдельная величина как два равноправных пути

Основание — D032, ответ владельца на Q011. Чем в Сербии платят курьерам,
бухгалтер не ответил, поэтому поддержаны оба способа, а выбор сделан **правилом**
`groups.<код>.work_measure`, а не веткой в коде.

**Состояние на входе.** Задачу вёл предыдущий агент, остановленный исчерпанной
квотой. Три коммита: движок (`1b0c385`), порядок импортов (`5dee0fb`) и
спасённые диспетчером правки схемы, расчёта и экрана (`1f609d9`) — последние **ни
разу не запускались**. Первым делом влит master и прогнан весь набор:

| прогон | результат |
|---|---|
| до вливания master, `feat/couriers` как спасли | 739 зелёных, 4 красных, 8 пропущено |
| красные | все четыре — `tests/test_i18n.py`: семь новых строк экрана без перевода и `.mo` не собран |

То есть спасённый код оказался рабочим, а незакрытыми остались хвосты. Их два, и
оба найдены прогоном, а не чтением.

**Дыра, найденная прогоном: `check_measures` была написана, но не вызвана.**
Функция отказа по неизвестной мере работы стояла в `payrun/calc.py` мёртвым
кодом: `compute` вызывал `check_schemes` и `check_insured_base`, а её — нет.
Последствие не косметическое: опечатка в `work_measure` (партнёр переопределяет
это правило руками) доходила до движка и поднимала `ValueError` посреди счёта,
то есть человек, который правило и переопределил, получал «Server Error» вместо
объяснения. Тест написан до правки, красный получен именно на `ValueError`,
после правки — зелёный.

**Правки движка** (их две, обе от предыдущего агента, обе объясняю здесь, потому
что на движке стоит сходимость с бухгалтерией):

| правка | почему допустима |
|---|---|
| `work_measure(group)` и ветка `measure == HOURS` в `calculate` | у группы без правила мера — часы, то есть прежняя ветка целиком. Умолчание проверено отдельным тестом: без `work_measure` расчёт побайтово прежний |
| поле `Timesheet.piece_value` в движке | у почасовой группы не читается вовсе — на это тоже отдельный тест |

Главный тест проекта (`test_serbia_june_2026.py`) и регрессия движка — зелёные,
контрольные числа не сдвинулись.

**Тесты, 22 из 22 зелёных** (`tests/test_piecework.py`; 20 от предыдущего агента,
2 добавлены здесь). Проверка порчей сделана по каждому добавленному:

| проверка | порча | результат порчи |
|---|---|---|
| опечатка в мере — отказ с именем человека, а не 500 | до правки `calc.py` | красный: `ValueError: неизвестная мера работы: per_delivery` |
| закрытая точка не пускает сдельную величину **в базе**, ролью `app_user` | убрать закрытие из подготовки | красный |
| то же **с экрана**: 409, код точки, число из базы заголовком | снять `refuse_if_closed` из вида `piece` | красный, и заодно доказано, что гарантию держит база: `new row violates row-level security policy "hours_open_update"` |

**Что закрывает требования задачи:**

| требование | чем закрыто |
|---|---|
| сдельная величина вводится в табель | колонка `timesheets.piece_value`, адрес `POST …/piece/`, поле в сетке у сдельных строк |
| оба способа дают разные и объяснимые числа | 168 ч × 420 = 70 560, 120 доставок × 420 = 50 400, фиксированная выплата = сама величина; в следе видно количество, ставку, `pay_per_unit` и путь правила |
| переключение способа не ломает закрытый период | правило с датой начала действия: утверждённый июнь при переключении с июля остаётся тем же до копейки, и пересчёт «а что было бы сегодня» даёт то же самое. Обратная проверка — переключение с июня июнь меняет — тоже есть, иначе первый тест доказывал бы, что переключатель ни на что не влияет |

**Прогон после правок: 746 зелёных, 8 пропущено, ни одного красного.**

**Живой браузер, настоящими кликами** (headless Chrome, стенд 8066, вход
директором логином и паролем). Оба способа введены и посчитаны с экрана:

| проверка | результат |
|---|---|
| колонка сдельной величины при почасовых курьерах | её нет вовсе — ни одного поля `piece` |
| включили `deliveries` переопределением правила | колонка появилась, заголовок переведён — «Piecework» |
| подпись способа у ячейки | «Доставки» — из правил страны, не зашита |
| у 33 почасовых строк | прочерк с объяснением, а не пустая ячейка |
| ввод 120 в ячейку курьера (клик, набор, Tab) | ячейка помечена сохранённой, в базе `piece_value = 120.00` |
| расчёт месяца кнопкой | 50 400,00 = 120 × 420, видно в колонке «Доставки» ведомости |
| след расчёта числа | `quantity 120.00`, `rate 420.00`, `pay_per_unit True`, правило — переопределение партнёра |
| переключили правило на `fixed_amount`, обновили табель | подпись сменилась на «Фиксированная выплата» без единой правки кода |
| ввод 45000 и пересчёт | 45 000,00 — сама величина; в следе `rate = None`, ставка не выдумана |
| два способа на одних и тех же 168 часах | 50 400,00 против 45 000,00 — разные и объяснимые |
| консоль браузера | чиста |

**Смоуки** (стенд 8066, свой Chrome на порту 9339):

| смоук | результат |
|---|---|
| `smoke_timesheets` | 17 из 17 |
| `smoke_timesheets_review` | 15 из 15 |
| `smoke_reports_sheet` | 54 из 54 — контрольные числа на месте: директор 1 951 806,13, бухгалтер 464 752,41, управляющий 891 373,32, норма 176,00 |
| `smoke_reports_trace` | 119 из 119 |
| `smoke_closing` | падает — протух до этой задачи, чинит соседний блок |

**Найдено по ходу и заведено issue #72: след сдельного начисления называет цену
единицы «ставкой за час»,** а при фиксированной выплате печатает «rate per hour
None». Подпись входов следа живёт в `src/web/views.py` — чужой блок, правка туда
была бы вторжением в параллельную работу, поэтому issue, а не патч.

**Чего проверить не удалось.** Ничего из заявленного. Не проверялось то, чего в
задаче нет: как сдельная величина поедет из Dodo IS (там она придёт со своими
датами и будет своей таблицей) и как её показывать в отчёте расхождений.

**Что осталось открытым вопросом владельца и здесь не решалось:** Q011 — чем
на самом деле платят курьерам в Сербии. Задача ровно про то, чтобы ответа не
ждать; когда он придёт, меняется одна строка правил, а не код.

## Статус

done — T123 принята 2026-08-14 (ветка `feat/debt` давно влита и удалена; искать код по ней
бесполезно, он в `master`). Статус висел `in_progress` месяц и разошёлся с графом —
выправлено диспетчером 2026-09-25. Принято ранее: T118, T122 —
2026-08-13; T075 — 2026-08-12; T019, T061, T062, T066, T073 — 2026-08-07;
T020, T021, T022 — 2026-08-08.

### 2026-08-14 — T123: две одновременные досылки одной ячейки роняли табель (issue #61)

Работал блок-агент сам, без исполнителя: правка идёт в единственную точку записи
табеля и трогает транзакции — механической по готовому контракту она не бывает.
Стенд `dodo-pnl-debt` (8085/5495), свой Chrome на порту 9345, копия
`dodo_pnl_service-debt`.

**Что было не так.** `set_cell` и `store_row` устроены как «прочитать состояние
строки — переписать дни заново», и делали это без блокировки. Две записи в одну
строку, начатые одновременно, обе видели «дней у строки нет», обе раскладывали
дни, и вторая падала на `timesheet_days_uniq`. Человек получал «Server Error» на
обычном вводе часов, а досылка (`sendBeacon`) ответа не читает — то есть часть
ввода терялась молча. Это не экзотика ввода: `pagehide` срабатывает и дважды
подряд (страница входа, затем форма входа), и с одной ячейки уходили две
одинаковые досылки сразу.

**Сначала тест, и он покраснел ровно тем, что описано в issue.**
`test_two_writers_of_one_cell_go_one_after_another` — настоящая гонка на **двух
соединениях**: первый писатель замирает внутри своей транзакции (пауза
подсунута вместо `store.spread`, то есть после чтения состояния и до записи
дней), в это окно пускается второй. До починки:
`IntegrityError: duplicate key value violates unique constraint "timesheet_days_uniq"`.
Два последовательных вызова такого не показывают вовсе — `test_repeated_save_changes_nothing`
проходил и на сломанном коде, поэтому проверка обязана была быть двухпоточной.

**Починка — блокировка строки, а не «поймать и простить».** Появился `lock_row`:
`refresh_from_db(from_queryset=Timesheet.objects.select_for_update())`. Строка
берётся под блокировку **после** проверок значения (отказ по значению не должен
ждать чужой транзакции) и **до** чтения `hours` — перечитывание здесь
обязательно: после ожидания на блокировке в объекте лежал бы снимок, сделанный
до чужой записи, и решение «база для взносов шла за часами» принималось бы по
данным, которых в базе уже нет. Та же блокировка добавлена в `store_row`:
загрузка таблицы пишет в те же дни и может идти одновременно с вводом с экрана.
`ON CONFLICT` сознательно не выбран: он спас бы от падения, но оставил бы гонку
двух раскладок, то есть итог строки мог собраться из половин двух правок.

**Вторая половина — клиентская, одноразовая досылка.** `grid.js` помечает
ячейку отправленным значением (`dataset.beaconValue`) и второй раз то же самое
не шлёт. Пометка — именно значение, а не флаг: успел набрать другое — уедет.

**Проверено фактически.** Живой браузер, смоук `smoke_timesheets` на стенде —
25 из 25, включая две новые проверки: «второй уход со страницы не шлёт ту же
ячейку заново» (досылок 1) и «после двойного ухода часы записаны». Порча:
пометка убрана из `grid.js` прямо в контейнере — смоук покраснел «досылок: 2»,
файл возвращён и сверен с репозиторием. Порча серверной половины показана самим
порядком работы: тест писался до починки и падал дубликатом ключа.

Граница честно: браузер двух **одновременных** досылок не сделает, поэтому
смоук доказывает только одноразовость, а серверную гонку доказывает тест на двух
соединениях. Подменять одно другим нельзя.

**Находка по дороге, из-за которой чуть не починили исправное (issue #105).**
После проверки порчей смоук снова покраснел — «досылок: 2» — на коде, где
починка уже стояла. Причина оказалась не в продукте: Chrome держал в своём кэше
`grid.js`, отданный ему во время порчи, и выполнял **вчерашний** файл на свежей
странице. Спрошено у самого браузера:

```
из кэша: beaconValue НЕТ, длина 10909
свежий:  beaconValue ЕСТЬ, длина 11017
```

`curl` того же адреса всё это время отдавал правильный файл. Проверка, зависящая
от кэша, врёт в обе стороны: красным на исправном коде (что и случилось) и
зелёным на сломанном (что случилось бы молча). Починено в `tools/cdp.mjs` —
`Network.setCacheDisabled` рядом с очисткой cookie и по тому же доводу:
состояние браузера приводится к известному на входе, а не просьбой к каждому
смоуку прибраться на выходе.

### 2026-08-13 — T122: отчёт импорта врал про класс события, README — про число тестов

**Отчёт импорта.** «Сопоставлено сотрудникам 16 · Не сопоставлено 16», и тут же
список этих шестнадцати с фамилиями и причиной «часы точки NS1 за этот месяц
закрыты». Их сопоставили — иначе откуда фамилии; не загрузили по другой
причине. Счётчик человек читает раньше списка и уходит искать расхождение
справочника, которого нет.

Поводов не попасть в табель ровно два, и теперь у них два счётчика и два
раздела: **не сопоставлено** — человека нет в справочнике партнёра, о ком
строка неизвестно; **сопоставлено, но не загружено** — человек найден и назван,
а строку записывать было некуда (часы точки закрыты; условий найма на месяц
нет). `ImportResult.skipped_rows` — отдельный список, а не поле внутри строки:
на экране это два раздела с разными заголовками и разными следующими шагами.

**README обещал 655 зелёных тестов** при фактических тысяче с лишним — отстал
в полтора раза. Число взято из настоящего прогона, а не вписано на глаз, и
сторожится проверкой `tests/test_readme_numbers.py`: она сравнивает обещание с
количеством собранных тестов (`pytest --collect-only`, доли секунды). Иначе оно
устареет снова тем же способом, каким устарело.

**У этого сторожа есть цена, и её надо знать:** любой, кто добавит или уберёт
тест, получит красный, пока не поправит одну строку в README. Это осознанно —
именно отсутствие такого красного и позволило числу разъехаться в полтора раза,
— но при сведении параллельных веток он краснеет предсказуемо, и правится
единственной цифрой. Текст отказа это и говорит.

> **Отменено 17.09.2026 (D129, issue #239).** Сторож снят вместе с обещанием
> числа в README: цена, честно названная выше, и оказалась единственным, что он
> производил стабильно — краснел на каждый добавленный тест и чинился правкой
> цифры. Молчаливый пропуск ловит `.github/scripts/check_skips.py`, и делает это
> строже: он называет пропущенное поимённо, а не сверяет итог. Запись оставлена
> как летопись того захода — механизма, который она описывает, больше нет.

Заодно убрано второе непроверяемое число: «`PAYROLL_FIXTURE=… pytest` # 363
зелёных». Проверить его отсюда нечем (файла с настоящими данными нет), а
писать наугад — то же самое, из-за чего задача и появилась. Вместо числа
сказано проверяемое: с этим файлом пропущенных не остаётся.

**Проверено.** `tests/test_timesheet_import.py` — две проверки: строки закрытой
точки лежат в `skipped_rows`, а счётчик «Не сопоставлено» на самой странице
отчёта равен нулю; `tests/test_readme_numbers.py` — обещание README сходится с
фактом. Живьём на стенде `dodo-pnl-ux` (8082): закрыл часы NS1, загрузил
`plata-sample.xlsx` — отчёт читается «Сопоставлено, но не загружено 16 · Не
сопоставлено 0», шестнадцать фамилий стоят под своим заголовком. Стенд после
проверки возвращён сидом.

### 2026-08-13 — T118: подсказка о подозрительном была только у загрузки файла

**Что было.** Спека даёт must-стори «хочу видеть подсказку о подозрительных
числах (часов больше нормы, сотрудник без часов, отрицательное значение), чтобы
ловить опечатки до расчёта». Проверки были написаны и работали — но в
`importer._check_data`, то есть только на пути «загрузили файл». На сетке, которую
D011 делает равноправным и главным путём ввода, не было ничего: 400 часов при
норме 88 сохранялись молча, без цвета, подписи и подсказки.

**Что сделано.** Правило вынесено в `timesheets/suspicion.py` и стало общим.
Импортёр теперь только раскладывает подсказку по своим местам (лист файла, ключ
сотрудника), сетка — по своим: пометка у итога строки с подсказкой мыши и
сводка фразами над таблицей. Сводка и пометка приезжают **вместе с ответом на
ячейку** (`hx-swap-oob`), а не после перезагрузки: подсказка, которую видно
только после обновления страницы, доходит до человека тогда, когда он уже не
помнит, что вводил.

Три решения по дороге, каждое меняло бы поведение, если решить иначе:

1. **Тексты сделаны нейтральными к пути ввода.** Были «в файле нет ни одного
   часа» и «а в файле %(total)s ч» — на сетке никакого файла нет. Стало «ни
   одного часа за этот месяц» и «а часов за этот месяц %(total)s». Иначе
   пришлось бы держать два набора фраз, то есть ровно две копии правила.
2. **Числа форматируются как вся сетка рядом** (`web.format.hours`): было
   `400.00` посреди столбца из `400,00` — на сербском экране это читалось бы
   как число из другого языка.
3. **Сдельной строке отсутствие часов не предъявляется** (D032): её деньги
   считает не время, и упрёк «нет ни одного часа» отправил бы человека искать
   ошибку там, где всё правильно. Остальные подсказки у неё остаются —
   отрицательные часы это опечатка при любом способе оплаты.

**Проверено.** `tests/test_timesheet_hints.py`, семь проверок: подсказка на
странице и в ответе на ячейку, сохранению не мешает, отрицательные и пустые
строки помечены, сдельная — нет, и главное — **дословное совпадение текста**
на сетке и в отчёте импорта на одних и тех же числах.

Порча, обе откачены: (1) выключил проверку «больше нормы» — покраснели три
проверки, включая проверку импорта; (2) убрал сводку из ответа на ячейку —
покраснела проверка «подсказка приходит без перезагрузки». Вторая порча заодно
показала, что первая редакция теста была тупой: она проходила через
`title=` у ячейки. Тест усилен — теперь он требует фразу целиком в сводке.

Смоук в живом браузере (стенд `dodo-pnl-ux`, 8082): `smoke_timesheets` **23 из
23** (три проверки добавлены: подсказка появляется без перезагрузки на числах
самой строки, переживает перезагрузку, снимается вместе с возвратом значения),
`smoke_timesheets_review` 15/15, `smoke_i18n` 123/123 — новых русских строк на
английском и сербском экранах нет.

### 2026-08-13 — T091: способ оплаты курьеров включается из продукта

D032 («работа меряется и часами, и сдельно, выбор — настройкой группы») был
сделан в движке и не доведён до продукта: правило `groups.<код>.work_measure`
переключалось только руками в базе, поля в форме группы не было, экрана правил
не существовало (T090). То есть второй способ оплаты в продукте был, а включить
его было нечем.

**Табель при этом не был сломан.** Колонка сдельной величины показывается,
только когда на экране есть хоть одна сдельная строка, — и это правильное
решение, а не пропуск: пустая колонка на каждом табеле мира приглашала бы
вводить в неё там, где её никто не спрашивает. В сиде курьерам написано
`work_measure: hours`, поэтому колонки и не было. Обещанный CHANGELOG прочерк у
почасовых групп появляется вместе с колонкой; проверено фактически, в браузере:
после переключения курьеров на «Доставки» в табеле июня стоит колонка «Сдельно»,
у курьеров — величина с подписью способа, у 33 почасовых строк — прочерк с
объяснением «Работа этой группы меряется часами».

**Где живёт переключатель.** Поле «Чем меряется работа» добавлено в форму группы
(`web/directory_views.py`), рядом — дата «Способ действует с». Способ —
свойство группы в глазах человека («курьерам платим за доставки»), и искать его
в списке из ста тридцати правил он не станет. Хранится он при этом правилом, а
не колонкой справочника: правило версионируется по дате, и смена способа не
переписывает уже посчитанный месяц.

Оба экрана — форма группы и экран правил — пишут **одной и той же** функцией
`web/rules.save_override`. Второй путь записи разъехался бы с первым на первой
же правке, и правка на одном экране перестала бы быть видна на другом.

**Право спрашивается своё — `rules.manage`, а не `directory.manage`:** это
правка правила, и партнёр вправе развести ведение справочников и ведение правил
по разным людям. У кого права нет — читает значение и объяснение, а не видит
пропавшее поле (T072). У новой группы полей нет вовсе, и об этом сказано
словами: правило адресуется кодом группы, а код ещё не сохранён.

**Дата не спрашивается, если способ не меняли.** Требовать дату у того, кто
правил название группы, значило бы отказывать на пустом месте. Пустая дата при
настоящей смене — первый день, который не задевает утверждённый месяц: «сегодня»
могло бы лежать внутри закрытого месяца, и человек получал бы отказ, ничего не
сделав неправильно.

**Смена способа задним числом отклоняется** тем же отказом и той же функцией,
что любая другая правка правила. Проверено тестом и порчей.

Способ работы показан и в списке групп отдельной колонкой: у одного партнёра
рядом живут почасовая кухня и сдельные курьеры, и «чем меряют работу» — первое,
что спрашивают у справочника групп.

Задача сделана блок-агентом целиком: механики здесь нет, есть выбор места
хранения и того, каким правом это закрыто.

## T100 (вторая половина) — режется ли табель по регистрам. Позиция подтверждена (2026-08-13)

Вопрос пришёл вместе с шестой утечкой регистров: сверка выдавала поимённый
состав скрытого регистра (разбор — в журнале блока `reports`), а табель
`/timesheets/<period>/` называет тех же людей всем ролям. Это записано осознанной
позицией в журнале блока `db` («табель по регистрам не режется и резаться не
должен») и висит открытым issue #57 с 07.08. Требовалось решить обе половины
вместе: либо табель тоже режется, либо позиция подтверждается — и тогда сверка
обязана не выдавать имён другим способом.

**Факт до разговора.** На стенде ролью `app_user`: у директора 35 строк, у
бухгалтера (`official`) те же 35, включая «Курир Ана» и «Курир Марко» — обе
строки ведомости `{internal}`; у управляющего 17 строк своей точки, включая
«Курир Ана». Норма 176,00 у всех. То есть табель действительно называет всем
ровно тот состав, который сверка выдавала списком.

**Решение: позиция подтверждается. Регистр учёта режет деньги, а не факт
работы.** Доводы по убыванию силы — не «так исторически», а почему иначе хуже.

1. **Разрезав табель, вектор не закроешь — он переедет.** Справочник сотрудников
   режется по точкам, а не по регистрам (T057): бухгалтер видит всех 35 человек.
   Табель на 31 строку рядом со справочником на 35 — это тот же поимённый
   список, полученный вычитанием. Чтобы закрыть по-настоящему, резать надо
   справочник; а его резать нельзя — тогда перестанет читаться закрытый период
   (ведомость и табель на месте, а имени к ним нет) и нельзя будет завести
   человека, у которого регистра ещё нет, потому что нет условий найма.
2. **У табеля нет своего регистра, и взять его неоткуда честно.** Регистр
   приходит от группы в условиях найма, версионированных по датам, а человек
   бывает смешанным: часть начислений официальная, часть нет. Часы неделимы —
   строку пришлось бы относить к регистру целиком, то есть **выдумать** ей
   принадлежность, которой в данных нет. Это ровно та «придуманная бухгалтерия»,
   от которой отказались в T050.
3. **Итоги сетки считаются по показанным строкам (D023).** Разные роли получили
   бы разные суммы часов и разные нормы по одному и тому же документу — при том
   что 176,00 контрольное число приёмки. Спорить о том, чей табель настоящий,
   партнёр будет уже на живых данных.
4. **Часы вносит тот, кто ведёт смену.** Управляющий не видит внутреннего
   регистра; порежь табель — и вносить часы курьеру станет некому, кроме
   директора. Расчёт при этом не отказал бы, а посчитал по пустому табелю.

Проще говоря: табель отвечает на вопрос «кто и сколько работал», и ответ на него
одинаков для всех, кому виден сам человек. Регистр отвечает на другой вопрос —
«из какого кармана и сколько ему заплатили», и вот он закрыт (T050, T065, T071,
T101 и теперь T100).

**Цена решения названа честно.** Роль, сопоставив табель со своей ведомостью,
по-прежнему может вывести имена: «часы есть, а в моей ведомости ни рубля». Это и
есть issue #57, он **остаётся открытым**, и способ закрыть в нём назван —
пересобрать ведомость так, чтобы она не присоединялась к `payslips`. Разница с
T100 не в чистоте, а в том, кто говорит: одно дело — человек сам сопоставил два
экрана, другое — продукт напечатал готовый список под заголовком. Второе
закрыто, первое стоит в очереди задачей уровня схемы.

**Граница позиции, найденная по дороге, — issue #85.** Премиса «в табеле нет
денег» верна не всегда: при `work_measure: fixed_amount` колонка
`timesheets.piece_value` хранит **саму сумму к начислению** (D032). Проверено
фактически: одно переопределение правила `groups.couriers.work_measure`, без
единой правки кода, — и `66 600,00` курьера внутреннего регистра стоят в табеле
бухгалтера в колонке «Сдельно». Эксперимент откачен, стенд возвращён сидом.

Сегодня путь не включён (пресет Сербии пишет `work_measure: hours` явно), и
чинить его в T100 нельзя: «спрятать колонку» меняет итоги показанной сетки, а
развилка продуктовая — считать ли `piece_value` работой или деньгами — и висит
на Q011, вопросе бухгалтеру. Поэтому issue, а не заплатка. Но знать надо: **в
тот день, когда партнёр переключит курьеров на фиксированную выплату, позиция
«табель по регистрам не режется» перестанет быть верной**, и её придётся
пересматривать вместе с местом хранения этой величины.

Кода в этой половине не написано ни строки — сознательно. Написана позиция,
названа её цена и названа граница, за которой она ломается.

### 2026-08-14 — T143: у правки часов появился автор, база взносов задаётся с экрана

**Стенд.** Копия `…-fix3`, ветка `feat/fix3`, порты 8092/8102/5502, имя
compose-проекта `dodo-pnl-fix3`.

Две половины, и обе про первичный документ.

#### 1. Кто поставил эти часы (issue #52)

**Что было.** След правки в табеле был ровно один — ручная корректировка
бухгалтера: без `corrected_by` и причины база её не принимает
(`timesheets_correction_trace_check`, миграция `0007`). У самих часов следа не
было: `timesheet_days.source` отвечал «откуда» (`spread`, `manual`, `import`), но
не «кто». Вопрос «кто поставил 176» задают ровно тогда, когда числа разошлись —
при закрытии часов точки, при утверждении периода, при правке задним числом, — и
ответа не было. Восстановить его нельзя: данных просто нет.

**Схема — миграция `0238_timesheet_edit_trace`**, четыре колонки в двух таблицах,
и это не перестраховка:

| где | что отвечает |
|---|---|
| `timesheet_days.edited_by/edited_at` | след **ячейки**. Дни одного типа часов пишутся одной операцией (`store._write_days` сносит прежние и создаёт заново), поэтому пара «строка + тип часов» — это ровно то, что человек видит ячейкой сетки |
| `timesheets.edited_by/edited_at` | след **строки**. Здесь величины, у которых дня нет и быть не может: сдельная величина, база для взносов, норма, удержание, наличные |

Одной таблицей это не закрывается ни с какой стороны: след только на строке не
скажет, кто поставил ночные, если ночные и обычные ставили двое; след только на
днях не скажет ничего про сдельную величину. Отдельной таблицы истории правок
намеренно нет: она отвечает на «всю цепочку», а спрашивают «кто поставил то, что
стоит сейчас», и цепочка правок табеля — это тысячи строк на партнёра в месяц.

**Пусто — это ответ, а не пробел.** Раскладка прежнего месячного итога по дням
(`materialize`, `source = 'spread'`) автора не получает никогда: её никто не
вводил. Это главное решение половины и на него стоит отдельная проверка —
приписать автора всем дням разом было бы легко, и продукт начал бы отвечать «176
поставил директор» про часы, которых директор не касался. Ложный ответ хуже
отсутствующего: его не перепроверяют. По той же причине запись мимо продукта
(сид, обслуживание) остаётся без автора, а не падает.

**Оба пути записи.** Автор ставится в `store` — единственной точке записи, — и
потому доезжает и с сетки (`set_cell`, `set_piece`), и с загрузки таблицы
(`store_row`; `actor_id` туда уже приходил, для следа ручной корректировки).
Отдельная проверка на каждый путь: след, поставленный на одном, оставил бы
историю с дырами ровно там, где в табель попадает больше всего чисел.

**Показ.** Подсказка мыши у самой ячейки: «поставил Оперативный директор, 14
августа 2026 г. 14:31» либо «кто поставил, не записано». Имя достаётся
`app_user_display_name()` (миграция `0042`) — `select` на чужие строки `users`
роли не выдан, и открывать его ради одного поля нельзя. Чтение вынесено в
`timesheets/authorship.py`: два запроса на всю сетку (`distinct on` по паре
«строка + тип часов» и одно разрешение имён), а не по запросу на ячейку — их 210.

Под таблицей сказано словами, что значит пустой ответ: подсказка «не записано»
без объяснения читается как поломка, а не как честное «не знаем».

#### 2. База для взносов задаётся с экрана (issue #54)

**Что было.** По базе движок считает взносы и бруто. Связь «база идёт за часами»
`set_cell` бережёт только там, где она была, поэтому база, пришедшая мимо экрана
(загрузка, сид, правка в базе), может разойтись с часами — и расчёт периода на
этом останавливается (`payrun.calc.check_insured_base`). Снять такой отказ можно
было только правкой в самой базе данных: мимо права, мимо закрытой точки и мимо
следа правки.

**Что сделано.** Колонка «База взносов» стала ячейкой сетки с теми же правилами,
что у часов: своё право не заводится (это тот же табель — `timesheet.edit`),
закрытая точка не правится (409 словами, проверено живым запросом со страницы),
разбор ввода тот же (`parse_insured` = `parse_hours`), автор записывается.
Свой адрес `/timesheets/<id>/insured/`, а не значение скрытого поля у записи
часов, — по тому же доводу, что у сдельной величины (T075): род ячейки не должен
решаться разметкой.

**Чего эта половина не закрывает и не притворяется, что закрывает.** Расчёт
по-прежнему отказывается считать, если база не сходится с часами. То есть закрыт
случай «база отстала — поправьте её из продукта», а случай «база **законно**
отличается от отработанного» остался: чтобы отказ на нём перестал срабатывать,
нужно правило, а правила нет — это Q005 у бухгалтера. Заводить пометку
«подтверждено бухгалтером» под неизвестное правило значило бы решить вопрос
владельца экраном ввода. Сказано прямо и в шапке `set_insured`, и в проверках.

**Островок сетки.** Появилась одна отмена: подмена вне цели (`htmx:oobBeforeSwap`)
не трогает место, в котором стоит курсор. Она нужна ровно из-за этой половины:
ответ на правку часов подменяет заодно колонку базы той же строки, а Tab с
последней ячейки часов ведёт прямо в неё — человек успевал начать печатать
раньше, чем прилетал ответ на предыдущую ячейку. Проверено обеими сторонами: при
обычной работе (правка → уход из поля) подмена проходит и пометка расхождения
обновляется сразу, при курсоре внутри — отменяется.

#### Проверки

`tests/test_timesheet_authorship.py` — 14 новых: след на обоих путях записи,
раскладка без автора, запись без автора не падает, автор виден на экране, слова
о пустом ответе; база с экрана — запись, автор, четыре вида непринятого ввода,
право, поле только у того, кто правит.

**Порча, откачена.** Автор перестал доезжать до дней (`_write_days`) и до строки
(`_stamp` вернул правку как есть) — красными стали три проверки: правка ячейки,
загрузка, база для взносов. Остальные одиннадцать остались зелёными, и это
правильно: они про показ и про разбор ввода.

**Проверка в живом браузере** (Chrome по протоколу отладки, стенд `dodo-pnl-fix3`
на 8092, схема с нуля, сид 35 человек). Русская и английская страницы:

- правка ячейки «Больничный» мышью и клавиатурой → подсказка правленой ячейки
  «поставил Оперативный директор, 14 августа 2026 г. 14:31», соседняя нетронутая
  говорит «кто поставил, не записано»; в английской — `entered by …` и
  `who entered this is not recorded`;
- база для взносов набрана в поле (140,50 при 97,00 по часам) → в базе данных
  `insured_hours = 140.50`, `edited_by` не пуст, на перезагруженной странице
  пометка расхождения и объяснение числами;
- база возвращена к 97,00 обычным вводом → пометка расхождения ушла **сразу**,
  без перезагрузки;
- закрытая точка NS2: полей ввода нет ни у часов, ни у базы, а запрос со
  страницы отвечает `409` и словами «Часы точки NS2 за Июнь 2026 закрыты».

**Что осталось честно неполным.** Подсказка автора у только что правленной
**ячейки часов** обновляется после перезагрузки страницы, а не сразу (issue #113): ответ
подменяет итоги и колонку базы, а `title` самой ячейки живёт на `<td>`, внутри
которого стоит поле под курсором, — то есть подменять его нельзя ровно по тому
правилу, ради которого заведена отмена выше. Значение и след при этом записаны;
человек, который только что ввёл число, знает, кто он.

**Кто что делал.** Схема, код, показ, проверки, порча и смоук — блок-агентом.
Исполнителю `forge-executor` отдана раскладка переводов: четыре новые строки на
два языка с готовым контрактом (точные msgid и тексты переводов, запрет на
`makemessages` и на `fuzzy`) — механическая работа, где от блок-агента нужны
смысл формулировок и приёмка. Прогон вернул 37 зелёных проверок языка.

### 2026-08-16 — T156, автор базы взносов (находка Н6 сверки 8)

Сделано **самим блок-агентом**: задача про след первичного документа, и главное
решение в ней — какой ответ считать честным, когда автора нет. Контракта на это
не напишешь, поэтому исполнителю не отдавалось.

**Что было.** Правишь соседнюю ячейку часов — подсказка у базы взносов говорит
«поставил Бухгалтер», хотя базу он не трогал. Колонка показывала след **строки**
(`timesheets.edited_by`), а его ставит любая правка строки; база при этом
пересчиталась сама, потому что `set_cell` бережёт связь «база идёт за часами».

Тот же класс, что закрывала первая половина T143: вопрос «кто поставил это
число» задают ровно тогда, когда числа разошлись, и неверное имя хуже
отсутствующего — его не перепроверяют. База взносов не производная величина, а
самостоятельный вход расчёта (issue #54): она законно отличается от часов, и
спор о взносах решается именно этим вопросом.

**Что сделано.** Миграция `0241_insured_author` — две колонки
(`timesheets.insured_by`, `insured_at`) без значения по умолчанию, зависимость
от текущего листа `0240_vat`. Заполняет их только тот, кто задал базу сам:
`set_insured` с экрана и `store_row` при загрузке таблицы (там база — своя
колонка файла, а не пересчёт, и подпись под сеткой обещает имя за загрузку
тоже). Пересчёт в `set_cell` автора **снимает**: посчитанное человеку не
приписывается.

След строки при этом остался на месте и не тронут: строку и правда правил тот
человек, и на него же смотрит колонка сдельной величины.

**Пусто — ответ, и ответов два.** Новый тег `insured_author_note`: база сошлась
с часами и автора нет — «посчитано по часам табеля» (продукт знает, откуда
число, и молчать незачем); не сошлась и автора нет — «кто поставил, не
записано» (так выглядит число мимо продукта). Подпись под сеткой дополнена этой
разницей — текст обязан описывать то, что человек увидит.

**Проверки** — 6 новых в `tests/test_timesheet_authorship.py`: правка часов не
делает правившего автором базы; пересчитанная база говорит «по часам»; автор
появляется от правки самой ячейки и переживает правку часов рядом; база без
автора и без сходимости честно говорит «не записано»; база из файла несёт
принёсшего; **база, совпавшая с часами, теряет автора при пересчёте**.

**Порча — и она поймала дыру в проверках.** Снятие автора при пересчёте убрали:
все проверки остались зелёными. Причина — ни одна из них не проходила через
ветку `base_tracked_hours` при непустом авторе. Дописана шестая (последняя в
списке выше), с ней порча краснеет; порча откачена, `grep ПОРЧА` пуст.

**Замечено рядом и не чинится здесь:** колонка сдельной величины показывает тот
же след строки и болеет тем же — правка часов меняет «автора» сдельной
величины. Задача про базу взносов, расширять её самовольно нельзя; заведено
issue #115 с готовым рецептом починки.

**Переводы** — `tools/translations/t156-insured-author.json`, 2 строки на оба
языка (одна из них — переписанная подпись под сеткой, `makemessages` пометил её
`fuzzy` со старым текстом). `.mo` пересобран.
