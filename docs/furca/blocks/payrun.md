# Блок: payrun

## Назначение

Жизненный цикл периода расчёта: от черновика до утверждения, с откатом и правками
задним числом. Это то, что превращает «посчитать зарплату» в повторяемый месячный
процесс с ответственностью и следом.

Выделен отдельно, потому что состояние периода — самостоятельная сущность со своими
правилами перехода, и от неё зависит, что можно править, а что уже заморожено.

## API-контракт

```
POST /api/periods/{period}/calculate  запуск расчёта (фоновая задача)
GET  /api/periods/{period}            состояние: статус, кто считал, когда,
                                      какие точки закрыты, какие строки заморожены
POST /api/periods/{period}/approve    утверждение
POST /api/periods/{period}/reopen     откат; тело {reason: str} — обязательное поле
POST /api/units/{unit}/close          закрытие точки
POST /api/payslips/{id}/freeze        заморозка строки сотрудника
```

Что из этого есть сейчас (T042, T023, T024, T025, T026, T027): всё делается формами со страницы
периода, ответ — редирект на неё же, отказ приходит той же страницей с плашкой и
своим кодом ответа.

| маршрут | что делает | отказы |
|---|---|---|
| `POST /periods/<uuid>/calculate/` | расчёт: фоновой задачей при `PAYRUN_BACKGROUND=1`, синхронно при `0` или с полем `inline=1` | 403 — нет права считать или роль не видит затрагиваемые регистры; 409 — считать нечем, период утверждён либо расчёт уже идёт |
| `GET /periods/<uuid>/calculate/status/` | состояние расчёта в JSON для полосы хода работы | своих нет: чужое задание не видно, и ответ тот же, что для «расчёта не было» |
| `POST /periods/<uuid>/approve/` | утверждение | 403 — нет `period.approve`; 409 — цикл не пускает |
| `POST /periods/<uuid>/reopen/` | откат, поле `reason` | 403 — нет `period.reopen`; 400 — причина не написана; 409 — цикл не пускает |
| `POST /payslips/<uuid>/freeze/` | заморозка строки, поле `reason` | 403 — нет `payslip.freeze`; 400 — причина не написана; 409 — период утверждён; 404 — строка не видна роли |
| `POST /payslips/<uuid>/release/` | снятие заморозки | 403 — нет `payslip.freeze`; 404 — строка не видна роли |

`GET /periods/<uuid>/` показывает состояние расчёта (включая полосу хода работы
фоновой задачи), ведомость с колонкой «Спор» (заморозка строки, её причина и
снятие) и историю переходов с причиной и автором. Отдельного JSON-API из таблицы
выше нет — единственный ответ в JSON пока один, для полосы хода работы.
Закрытие точек (`POST /units/{unit}/close`) живёт в блоке `timesheets` (T022) и
делается с экрана табеля, а не отсюда.

Статусы периода: `draft` → `calculated` → `approved`. Плюс `reopened` как явный
результат отката (не молчаливый возврат в `draft` — по журналу должно быть видно,
что период открывали).

Правила, которые блок гарантирует:

- в одном периоде один черновик, не больше;
- утверждённый период не меняется молча: попытка записи в него отклоняется на уровне
  базы, а не только в интерфейсе;
- откат требует причины; причина и автор попадают в историю периода;
- **правка задним числом по умолчанию** не переписывает закрытый период: разница
  ложится помеченной строкой в текущий, со ссылкой на исходный, в тот же регистр
  учёта. Настройкой тенанта можно переключить на пересчёт — но только через явное
  переоткрытие, не тихо;
- блокировка построчная и по точкам: спорный сотрудник не держит закрытие остальных;
- модель согласования многоуровневая, интерфейс показывает один уровень.

## Зависимости

Блоки: `timesheets` (часы), `core` (расчёт и правила на дату).

Внешнее: `django-q2` (фоновые задачи на очереди в Postgres).

## Definition of Done блока

- [x] Расчёт периода на 30 сотрудниках запускается и завершается — синхронно
      (T042) и фоновой задачей через очередь (T024), с полосой хода работы на
      странице; проверено на поднятом стенде с рабочим процессом очереди
- [x] Фоновая задача выставляет контекст тенанта сама — тесты, что без контекста
      данных не видно, что задание чужого тенанта не видно **самой базе** и что
      расчёт идёт ролью `app_user`, а не владельцем таблиц (T024)
- [x] Тест на каждый переход статуса, включая запрещённые (T023)
- [x] Тест: запись в утверждённый период отклоняется базой (T023)
- [x] Откат без причины невозможен; с причиной — виден в истории с автором и
      временем (T025): запрет держит триггер `payruns_reason_guard`, история
      показана на странице периода
- [ ] Тест ретро-дельты: изменение ставки задним числом даёт помеченную строку
      в текущем периоде, закрытый период байт в байт прежний
- [x] Тест: закрытие точки не блокируется спорной строкой другой точки (T027):
      и своей точки тоже — заморожен результат расчёта, а не табель
- [x] Повторный запуск расчёта не плодит ведомости — идемпотентность (T042)

## Статус

done — 2026-09-25: **T220** (issue #236) — закрытие месяца называет окладника,
которому не проставили часы: сколько часов не хватает и во сколько денег это
обойдётся против контракта. Блокирующая находка: отложить можно с причиной,
пропустить молча нельзя. Задачу вёл диспетчер: волну оборвал лимит подписки,
красный тест блок-агента спасён и доведён. Подсчёт часов месяца жил в трёх
копиях — вынесен в `src/timesheets/totals.py`.

Ранее — 2026-08-08. Приняты T042, T023, T024, T025, T026, T027: цикл периода со
статусами и историей переходов, утверждение и откат с обязательной причиной,
расчёт фоновой задачей на очереди в самой базе с полосой хода работы, построчная
заморозка ведомости (спорный сотрудник не держит ни закрытие точки, ни
утверждение месяца, а пересчёт обходит его стороной) и перенос разницы задним
числом помеченной строкой в текущий период.

Итоговую запись журнала и эту строку статуса дописал **диспетчер**: агент T026
погиб на обрыве связи ровно на фразе «пишу итоговый раздел журнала блока и
обновляю контракт». Сама работа была закоммичена им и проверена приёмкой
целиком — дописаны только документы.

## Журнал работы

### 2026-08-07 — старт, T042

Прочитано фактически: этот контракт, конституция, спека, решения (D023, D025,
D028), журналы блоков `db` и `web`, `tasks.md` (строка T042), код
`src/payroll/engine.py`, `presets/serbia-2026.yaml`, `src/core/models.py`,
миграция `0004_rls`, `seed_dev.py`, весь `src/web/`, `tests/conftest.py`,
`tests/test_web.py`.

Что в этот заход **не** делается (третья очередь, не мой скоуп): статусы
периода и переходы, утверждение и откат, блокировки по строкам и точкам,
ретро-дельта, след расчёта (D025), фоновая задача через очередь. Расчёт
синхронный: 32 человека считаются мгновенно.

Найденное по коду, что определяет решения (до написания кода):

- **`payslips` не защищены политикой регистра.** Ledger-политика висит только на
  `pay_components` и `allocation_rules` (список `LEDGER_TABLES` в `0004_rls`), а
  `payslips.net/gross/tax/contributions` — суммы по **всем** регистрам сразу.
  Значит показывать их в ведомости нельзя: бухгалтер вычислил бы скрытый от него
  дополнительный регистр вычитанием. Ведомость строится **только из
  `pay_components`** — тогда «итог по видимому срезу» (D023) получается сам, а не
  маскируется на выводе.

  > Снято во второй очереди (T050): суммарные поля вынесены в отдельную таблицу
  > `payslip_totals` с ограничивающей политикой — итоги видны, только когда видны
  > все регистры строки. Ведомость по-прежнему строится из компонентов, но
  > запрет на показ сумм больше не держится одной дисциплиной в коде.
- **Регистр — свойство компонента, а не человека.** В пресете надбавка
  `meal_and_vacation_bonus` помечена `ledger: official`, а группа `kitchen` —
  `supplementary`.
  Один сотрудник кухни даёт строки в двух регистрах одновременно. Это ровно тот
  случай, ради которого в D023 выбрана одна ведомость с меткой. Отсюда строка
  ведомости — пара «сотрудник × регистр».
- **`insert … returning` проверяется политиками `select`.** Django получает
  первичный ключ через `RETURNING`, поэтому запись компонента в невидимый роли
  регистр упала бы ошибкой базы. Отсюда расчёт требует роли, видящей все
  регистры, которые он собирается записать, — и отказывает внятно, а не 500-й.

### 2026-08-07 — T042 сделан

Что появилось:

| Файл | О чём |
|---|---|
| `src/payrun/calc.py` | сбор входа из базы, вызов движка, запись расчёта; отказы до записи |
| `src/payrun/sheet.py` | сборка ведомости из компонентов выплаты (чистая функция + выборка) |
| `src/payrun/rules.py` | выбор пресета по стране тенанта и дате периода |
| `src/payrun/errors.py` | отказы с текстом для человека и кодом ответа |
| `src/web/views.py` | `period_calculate`, страница периода с ведомостью и плашками |
| `src/web/templates/web/period.html` | ведомость, кнопка расчёта, итоги |

Маршрут добавлен один: `POST /periods/<uuid>/calculate/` (имя `period-calculate`).
Схема базы, модели и миграции **не менялись** — всё легло в существующие
`payruns`, `payslips`, `pay_components`. Сид тоже не менялся: часы в нём есть
на все 32 человека и все четыре схемы расчёта.

Решения, принятые по ходу, и почему:

- **Ведомость строится только из `pay_components`.** Суммарные поля `payslips`
  (нето, бруто, взносы) политики видимости регистров не имеют — показ любого
  из них дал бы бухгалтеру скрытый регистр вычитанием. Как следствие, итог
  «по видимому срезу» (D023) не требует отдельной механики: он равен сумме
  показанных строк по построению.
- **Строка ведомости — пара «сотрудник × регистр».** У сотрудника кухни часы
  идут в дополнительном регистре, а надбавка за питание помечена в пресете
  официальным. Одна ведомость с меткой регистра — ровно то, что описано в
  обосновании D023 («один человек может иметь строки в двух регистрах»).
- **Регистр берётся из базы** (`employment_terms.ledger` → `employee_groups.ledger`),
  а не из пресета: на нём стоят политики доступа, и второй источник истины
  означал бы показ строки не тому человеку. Значения совпадают — сид заводит
  группы из того же пресета.
- **Пресет выбирается по стране и дате**, объявленным в самом YAML
  (`country`, `valid_from`), а не по таблице соответствия в коде. T011 заменит
  тело `select_preset`, а не его вызовы.
- **Считать может только роль, видящая все затрагиваемые регистры.** База и так
  отвергла бы запись, но ошибкой драйвера; проверка до записи даёт объяснение,
  а база остаётся страховкой (подтверждено порчей, см. ниже).
- **Сотрудник с табелем без условий найма — отказ, а не пропуск.** Месяц,
  посчитанный без одного человека, выглядит правильным. В отказ попадают
  внешние идентификаторы, чтобы было что искать.
- **Округление до копейки явное** (`ROUND_HALF_UP` в одном месте), а не «как
  сложится в `numeric(14,2)`».
- **Статус `payruns` остаётся `draft`.** Переходы черновик → посчитан →
  утверждён — задача T023, придумывать их правила здесь нельзя. Записывается
  только `calculated_at`.

Тесты — `tests/test_payrun.py`, 16 штук. Прогон всего набора:
**77 passed, 8 skipped** (пропуски прежние — сверка с настоящей таблицей, файла
нет и не должно быть). Покрытие 86% при пороге 80%; `src/payrun/` — 95–100%.

Ключевой тест задачи: расчёт, запущенный со страницы, сверяется с прямым
вызовом движка. Вход для сверки тест собирает **своим кодом прямо из SQL** —
если бы он звал `payrun.calc`, то проверял бы ошибку самим собой.

Проверка тестов порчей (конституция: проверка, которая не падает, — не проверка):

| Что сломано | Что покраснело |
|---|---|
| коэффициент ставки заменён на 1 | сверка с движком |
| прежние ведомости не сносятся перед записью | идемпотентность (`payslips_payrun_employee_uniq`) |
| убрана проверка регистров до записи | отказ бухгалтеру — вместо 403 приходит `new row violates row-level security policy "ledger_visibility"`, то есть база действительно последний контур |
| итог ведомости считается мимо строк | три теста итогов, включая чистый unit |
| регистр строки берётся у сотрудника, а не у компонента | две строки на человека и след дополнительного регистра |

Проверка в живом браузере (свой headless Chrome по протоколу отладки, клики
настоящими событиями мыши; сервер `127.0.0.1:8033` — порт проверен `lsof` до
старта; база `dodo_pnl_payrun_smoke` из `migrate` + `seed_dev`):

| Шаг | Что увидел |
|---|---|
| до расчёта | «Последний расчёт: не считался», «Расчёта за этот период ещё не было» |
| директор жмёт «Посчитать период» | плашка «Расчёт выполнен», 56 строк, регистры Официальный и Дополнительный |
| итоги директора | Официальный 463 474,00 + Дополнительный 1 306 393,72 = **1 769 867,72**, равно сумме 56 строк |
| бухгалтер, та же страница | **32 строки**, только Официальный, итог **463 474,00**, равно сумме его строк |
| бухгалтер жмёт «Посчитать» | 403 и плашка: регистр «Дополнительный» ему недоступен; в базе ничего не изменилось |
| ширины 1440 / 1280 / 768 | страница по горизонтали не едет; ведомость прокручивается внутри себя, колонка «Итого» прилипшая и видна всегда |

Ошибок в консоли, кроме ожидаемого 403 на запрещённый расчёт, нет.

Что стоит знать следующему:

- **Шаблоны кэшируются даже при `DEBUG=1`** (кэширующий загрузчик включён с
  Django 3.1, сбрасывает его автоперезагрузчик). С `runserver --noreload`
  правка шаблона не видна до перезапуска — на этом легко потерять час, решив,
  что правка не сработала.
- **`payslips` без политики регистра** — мина под будущие экраны: любой отчёт,
  показавший `net` или `gross`, раскроет скрытый регистр. Настоящее решение —
  ограничивающая политика на `payslips` или хранение сумм в разрезе регистра;
  сейчас спасает только дисциплина «ведомость собирается из компонентов».
- **`cash_payout` и `manual_correction` в табеле по-прежнему негде хранить**
  (колонок нет, см. журнал блока `db`). Движок их принимает; расчёт по сиду
  без них сходится сам с собой, но с таблицей бухгалтера разойдётся там, где
  она правит вручную.
- **Фикстуры живого Django переехали в `tests/conftest.py`**: настройки Django
  читаются один раз на процесс, поэтому база у веб-тестов общая. Тест, которому
  важны суммы, обязан сначала привести зарплатные таблицы в известное состояние
  (`wipe_payruns`) — иначе результат зависит от порядка модулей.
- В чистом SQL каскадов нет (`on delete` Django не пишет): сносить нужно
  `pay_components` → `payslips` → `payruns` явным порядком.

### 2026-08-08 — T023, решения до кода

Прочитано фактически: этот контракт, конституция, спека, план, решения, `tasks.md`
(строки T023–T027), `src/core/models.py`, миграции `0001_types`, `0004_rls`,
`0005_app_role`, `0009_payslip_ledgers`, `0021`, `0022_role_permissions`,
`0023_totals_by_role`, весь `src/payrun/`, `src/web/views.py`,
`src/web/permissions.py`, `tests/conftest.py`, `tests/test_payrun.py`.

**Где живёт цикл — на `payruns`, не на `periods`.** В схеме два независимых
статуса: `periods.status` (`open`/`review`/`closed`) — учётный месяц тенанта
целиком, и `payruns.status` (`draft`/`calculated`/`approved`/`paid`) — расчёт
зарплаты за этот месяц. Статусы из контракта (черновик → посчитан → утверждён)
— это второе. Вешать зарплатный цикл на `periods` нельзя: утверждение зарплаты
заморозило бы весь месяц, включая платежи поставщикам и выручку, которых в этом
периоде ещё даже нет. `periods` не тронут.

**Один черновик на период** уже гарантирован `payruns_tenant_period_uniq`
(уникальность пары «тенант + месяц»): расчёт за период вообще один, черновик тем
более. Отдельный частичный индекс `where status = 'draft'` не заводится — он
никогда бы не сработал, а мёртвое ограничение в схеме врёт следующему читателю
не меньше устаревшей доки. Тест на правило есть, он упирается в существующее
ограничение.

**Разрешённых переходов ровно четыре**, всё остальное отвергает база:

| откуда | куда | когда |
|---|---|---|
| — (создание) | `draft` | завели расчёт |
| `draft` | `calculated` | расчёт прошёл |
| `calculated` | `approved` | утвердили (T025) |
| `approved` | `reopened` | откат (T025) |
| `reopened` | `calculated` | пересчитали после отката |

Что из этого следует и почему именно так:

- **`reopened` не схлопывается в `draft`.** Иначе по текущему состоянию нельзя
  отличить «ещё не считали» от «утверждали и открыли обратно», а по контракту
  открытие периода обязано быть видно.
- **`reopened → approved` запрещён.** Открыли — значит, обязаны пересчитать
  прежде, чем утвердить снова. Иначе утверждение второй раз накрывало бы числа,
  посчитанные до правки входных данных, и расхождение уехало бы в закрытый
  период молча. Лишний шаг дешевле неверного утверждённого месяца.
- **Пересчёт — не переход.** Он меняет `calculated_at`, а статус оставляет
  прежним. Поэтому правило разделено надвое: статус не меняется — можно всё,
  кроме как в `approved`; статус меняется — только по таблице выше.
- **`paid` недостижим.** Значение в enum есть с самого начала, экрана выплаты
  нет ни в одной задаче очереди. Переход к нему база отвергает, а не пропускает
  «на будущее»: догадка о ненаписанном экране — это второй источник истины.

**Проверяет база, а не Python.** Три триггера:

1. `payrun_guard` на `payruns` — легальность перехода и запрет любой правки
   утверждённого расчёта, кроме самого отката. `delete` утверждённого тоже
   отклоняется: удаление — предельная форма записи.
2. `payrun_frozen_guard` на `payslips`, `payslip_totals`, `pay_components` —
   запись в утверждённый расчёт отклоняется на любом пути, включая чистый SQL
   мимо ORM. Это и есть требование задачи: проверка в Python — не контур,
   её обходит любой другой путь записи.
3. `payrun_log_transition` — журнал `payrun_transitions` пишется **триггером**,
   а не приложением. Тогда перехода без записи в журнале не бывает в принципе.

Триггеры выбраны сознательно вместо политик RLS: политики не действуют на
суперпользователя, а триггер действует на всех. Для «утверждённое не меняется»
это принципиально — иначе гарантия держалась бы на том, каким пользователем
подключились.

**Автор и причина отката ложатся сверху без переделки.** В журнале уже есть
`actor_id` (берётся из `app_user_id()`, того же контекста, на котором стоит вся
RLS) и `reason` (приезжает разовой настройкой транзакции
`app.transition_reason`, тем же механизмом, что `app.user_id`). T025 остаётся
дописать в триггер требование «`reason` обязателен при откате» и завести экран —
ни таблицу, ни модель это не трогает.

**Чего эта задача намеренно НЕ делает:**

- **не морозит `timesheets`.** Правка входных данных задним числом — предмет
  T026, и там она по умолчанию обязана давать помеченную дельту, а не отказ.
  Запретить её здесь значило бы решить T026 за неё, да ещё правкой таблицы,
  которую в этот же момент ведёт блок `timesheets`;
- **не заводит прав `period.approve` / `period.reopen` в политиках.** Сегодня
  запись в `payruns` закрыта правом `payrun.calculate` (миграция `0022`), то
  есть утвердить может тот, кто умеет считать. Это меньше, чем нужно, и это
  задача T025 — вместе с её экраном, как и требует шапка `0022` («политика,
  стоящая там, где никто не пишет, — не защита, а догадка»). **Записка для
  T025: не забыть, иначе право останется надписью;**
- **не трогает страницу периода.** Утверждения и отката в интерфейсе ещё нет,
  а расчёт со страницы работает как прежде — только теперь оставляет статус
  `calculated` вместо `draft`.

### 2026-08-08 — T025, решения до кода

Прочитано фактически: этот контракт, конституция, спека, решения, `tasks.md`
(строка T025), миграции `0010_users`, `0022_role_permissions`, `0040`, `0041`,
`src/payrun/lifecycle.py`, `src/web/views.py`, `src/web/permissions.py`,
`src/web/principal.py`, `src/web/dbcontext.py`, шаблон `web/period.html`,
`tests/conftest.py`, `tests/test_payrun_lifecycle.py`, `seed_dev.py`.

**Причину требует база, отдельным триггером.** `payrun_guard` из `0041` не
переписывается: чтобы дописать в неё одну строку, пришлось бы скопировать
шестьдесят строк вместе с их доводами в `0042`, и дальше два экземпляра одной
функции расходились бы молча — ровно то, против чего написан сам `0041`.
Поэтому правило живёт своим сторожем `payruns_reason_guard`. Разделение честное
и по смыслу: «куда можно перейти» и «что при этом обязан объяснить человек» —
разные вопросы.

**Какие переходы требуют причину — решает база, одна функция.**
`payrun_reason_required(payrun_status)` рядом с `payrun_next_statuses()`: у
приложения второго списка не заводится, форма спрашивает у базы, обязательна ли
причина. Сегодня причина обязательна ровно у отката.

**Права `period.approve` / `period.reopen` встают на место `payrun.calculate`,
а не поверх него.** Ограничивающая политика `payrun_calculate_update` на
`payruns` (миграция `0022`) требовала право считать на **любую** запись, включая
утверждение. Ещё одна ограничивающая политика сверху дала бы «утвердить может
тот, кто умеет считать **и** имеет право утверждать», то есть `period.approve`
так и не стал бы самостоятельным правом. Поэтому политика правки `payruns`
переписывается на разбор по тому, **чем строка становится**:

| строка после записи | требуемое право |
|---|---|
| `approved` | `period.approve` |
| `reopened` | `period.reopen` |
| прочее (расчёт, пересчёт) | `payrun.calculate` |

Право по новому статусу, а не по паре «откуда → куда», потому что `with check`
старой строки не видит. Этого достаточно: перейти в `approved` можно только
утверждением, а править утверждённую строку, оставляя статус, не даёт триггер.

**Приложение спрашивает у базы, куда можно перейти.** `payrun_next_statuses()`
объявлен единственным источником истины о цикле — значит, и «показывать ли
кнопку утверждения» берётся оттуда же, а не из списка в Python. Кнопки нет по
двум разным причинам, и они не смешиваются: цикл не позволяет (нечего утверждать)
или роль не имеет права (тогда на месте кнопки объяснение — T072).

**Имя автора в истории — функцией, а не политикой на `users`.** По `0010`
человек видит в `users` только свою строку, поэтому `actor_id` в журнале
интерфейсу нечем расшифровать, а «кто открывал» обязано быть видно словами.
Открывать `select` на чужие строки `users` нельзя: там же хэш пароля, почта и
признак активности. Поэтому `app_user_display_name(uuid)` — `security definer`,
отдаёт **только имя** и только тому, с кем у человека есть общий тенант.

**`payruns.approved_by` заполняется при утверждении** и после отката остаётся.
Так вышло не по недосмотру: триггер `0041` намеренно не даёт откату менять
ничего, кроме статуса, — иначе одним оператором можно было бы и открыть период,
и переписать его. Значит, колонка означает «кто утвердил в последний раз», и
это записано комментарием в схеме. Оставить её вечно пустой было бы хуже:
пустая колонка с таким именем врёт каждому следующему отчёту.

**Чего эта задача не делает:** не морозит табель и не считает ретро-дельту
(T026), не заводит построчных блокировок и закрытия точек (T027), не трогает
`periods.status` — учётный месяц живёт своей жизнью (решено в T023).

### 2026-08-08 — T025 сделан

Заход начался с прогона того, что осталось от предыдущего агента: миграция
`0042_payrun_approval` и `tests/test_payrun_approval.py` были написаны, но ни
разу не запускались. **Первый фактический прогон: сторона базы зелёная целиком**
(причина обязательна и для владельца таблиц, права разделены, имя автора видно
только коллегам), страничная сторона падала — кода не было.

Единственный настоящий дефект в спасённом:
`test_approval_requires_the_right_to_approve` падал **на подготовке**.
Управляющий заводил расчёт своими руками, а права `payrun.calculate` у него нет
— тест умирал на `insert`, не дойдя до проверяемого запрета. Расчёт теперь
доводит директор, управляющий только пробует утвердить. Миграция правки не
потребовала.

Что появилось со стороны приложения:

| Файл | О чём |
|---|---|
| `src/payrun/lifecycle.py` | `next_statuses`, `reason_required`, `approve`, `reopen`, `history`, названия статусов |
| `src/payrun/errors.py` | `ReasonRequired` — 400, а не 409 |
| `src/web/permissions.py` | `period.approve` и `period.reopen` названы словами |
| `src/web/views.py` | `period_approve`, `period_reopen` общей обвязкой `period_transition` |
| `src/web/urls.py` | два маршрута, оба только POST |
| `src/web/templates/web/period.html` | блок цикла и таблица истории |
| `tools/smoke_period_lifecycle.mjs` | браузерный смоук цикла |

Решения, принятые по ходу, и почему:

- **Куда можно перейти и нужна ли причина — приложение спрашивает у базы**
  (`payrun_next_statuses`, `payrun_reason_required`), а не помнит своим списком.
  Это прямое следствие решения T023 «единственный источник истины о цикле — в
  схеме»: «что предложить человеку» — такой же вопрос о цикле, как «что
  разрешить», и второй список разъехался бы с первым молча.
- **`ReasonRequired` — 400, а не 409.** «Не хватает поля в форме» лечится тем,
  что человек допишет причину, а не тем, что изменится состояние периода. По
  коду ответа снаружи должно быть видно, что делать.
- **Порядок в обработчике: право → цикл → причина.** Тот же, что у расчёта
  (T064): человек без права не должен сначала узнавать про состояние периода,
  до которого ему нет дела.
- **Переход пишется условием по прежнему статусу**
  (`filter(pk=…, status=payrun.status)`). Между открытием страницы и нажатием
  кнопки период мог утвердить кто-то другой; без условия второй оператор молча
  переписал бы чужой переход. Ноль изменённых строк — это «состояние уехало», и
  об этом говорится вслух.
- **Утверждённый период перестал предлагать кнопку расчёта.** Расчёт его
  отвергает база; предлагать кнопку значило бы обещать невозможное. Адрес
  расчёта остался рабочим и по-прежнему отвечает отказом со словами — интерфейс
  не контур доступа.
- **Кнопки нет по двум разным причинам, и они не смешиваются.** Цикл не пускает
  — о праве говорить нечего, объяснения нет. Право отсутствует — на месте кнопки
  тот же текст, которым ответит отказ (T072).
- **Состояние месяца и состояние расчёта — две разные строки в сводке.**
  `periods.status` не тронут (решено в T023), и на экране это видно, а не
  подразумевается.
- **Причина, написанная человеком, переживает отказ**: при 400 поле
  возвращается заполненным, иначе длинное объяснение пришлось бы набирать
  заново из-за опечатки.

Тесты — `tests/test_payrun_approval.py`, 19 штук. Полный прогон:
**404 passed, 8 skipped** (было 385 + 8 до задачи; пропуски прежние — сверка с
настоящей таблицей, файла нет и не должно быть).

Проверка тестов порчей:

| Что сломано | Что покраснело |
|---|---|
| убрана проверка причины в приложении | отказ на пустую причину приходит **ошибкой драйвера от триггера**, а не 400 — то есть база и есть настоящий контур |
| триггер причины ничего не требует | три теста базы, включая проверку на владельце таблиц |
| политика записи снова требует одно `payrun.calculate` | `period.reopen` перестаёт быть отдельным правом — бухгалтер открывает период |
| `app_user_display_name` не смотрит на общий тенант | имя автора достаётся постороннему |
| кнопка утверждения не смотрит на право | управляющий снова видит кнопку, которая даст 403 |
| кнопка утверждения не смотрит на цикл | кнопка появляется до расчёта и на открытом заново периоде |
| история не показывает причину | проверка истории |
| **история не показывает имени автора** | **ничего — дефект теста, см. ниже** |

**Порча, которая не покраснела, и что это значило.** Обнуление имени автора в
`lifecycle.history` не сломало ни одного теста: имя вошедшего стоит в шапке
**каждой** страницы, поэтому проверка «имя есть где-то в HTML» проходила и с
пустой историей. То же у слова «Утверждён» — оно же в сводке состояния. Тесты
теперь ищут имя, причину и статусы **внутри блока истории** (`history_block`),
и порча ловится. Ровно тот случай, ради которого проверка порчей и делается:
тест был зелёным не по той причине, по которой считался зелёным.

Проверка в живом браузере (`tools/smoke_period_lifecycle.mjs`, свой headless
Chrome по протоколу отладки на порту 9351, клики и ввод настоящими событиями;
приложение — свой compose-стенд `dodo-pnl-pr2` на 8051, база 5459):
**35 из 35 проверок прошли**, консоль чистая.

| Шаг | Что увидел |
|---|---|
| до расчёта | кнопки утверждения нет |
| директор считает нажатием | «Посчитан», 60 строк, итог 1 951 806,13, появилась кнопка утверждения |
| директор утверждает нажатием | «Утверждён», кнопка пересчёта пропала, появился откат, история назвала автора |
| управляющий | кнопки отката нет, вместо неё «Откат периода не входит в права вашей роли» |
| бухгалтер | то же самое (утверждать вправе, открывать — нет), история видна |
| откат с пустой причиной мимо формы | **400** и «требует причины»; период остался утверждённым |
| директор набирает причину и жмёт «Открыть заново» | «Открыт заново», утверждения не предлагает, расчёт снова предлагает, история назвала причину и автора, 4 перехода |
| бухгалтер / управляющий | 33 строки и 464 752,41 / 24 строки и 891 373,32, норма 176,00 у обоих |

Что стоит знать следующему:

- **Причина доезжает до триггера настройкой транзакции** (`app.transition_reason`),
  той же механикой, что `app.user_id`. Вне транзакции Postgres такую настройку
  молча игнорирует, поэтому `_set_reason` отказывается работать вне
  `atomic` — иначе откат падал бы «требует причины» при заполненном поле, и
  искать это пришлось бы долго.
- **Причина одноразовая** (триггер журнала гасит её после записи): пересчёт
  после отката не наследует чужое объяснение. На это есть тест.
- **`approved_by` означает «кто утвердил в последний раз»** и после отката не
  стирается: триггер `0041` намеренно не даёт откату менять ничего, кроме
  статуса. Записано комментарием в схеме.
- **Журналы блоков `db`, `auth`, `web` устарели**: они утверждают, что
  `period.approve` и `period.reopen` ничего не решают. С этой задачей решают —
  issue #59, чужие блоки, сам не правил.
- **Уборка тестовых данных теперь обязана объяснять откат** (`wipe_payruns` в
  `conftest`): требование причины держит триггер, а он действует и на
  суперпользователя.

**Дефект T023, найденный уборкой за смоуком (issue #60, починен здесь же).**
`seed_dev` падал на любой базе, где период хоть раз утверждали:

    журнал переходов периода только пополняется: DELETE отклонён

Сид сносит расчёты через ORM, а Django исполняет каскад **в Python** — то есть
отдельным оператором по `payrun_transitions`. Для сторожа `append_only` это
прямое удаление истории, а не каскад (`pg_trigger_depth()` равен единице), и он
честно отказывает. После первого же утверждения базу разработки и демо нельзя
было пересидировать.

Починка — миграция `0043_payrun_journal_cascade`: `PayrunTransition.payrun`
переведён на `on_delete=DO_NOTHING`, то есть Django в удаление не вмешивается, а
журнал сносит база настоящим внешним ключом (`on delete cascade` поставлен
руками ещё в `0041`) внутри удаления самого расчёта. Сторож не ослаблялся:
ослабить его значило бы разрешить чистить историю из приложения — ровно то,
ради запрета чего он написан.

Почему это пережило приёмку T023: `test_seed_is_idempotent` гоняет сид на базе
**без единого расчёта**, и в ней журнала нет вовсе. Дефект нашёлся не чтением
кода, а тем, что за смоуком убирали фактически. Оба недостающих теста заведены
(`test_the_journal_goes_away_with_its_payrun`,
`test_the_seed_runs_over_a_period_that_was_approved` — второй гоняет настоящую
команду в подпроцессе и до правки красный).

Смоук прогнан повторно уже с `0043` — снова 35 из 35, — и следом на том же
стенде сид поверх данных смоука: проходит, расчётов и журнала не остаётся.
Стенд после этого снесён целиком вместе с томом и сетью.

### 2026-08-08 — T027, разбор до кода: что здесь ещё не сделано

Прочитано фактически: этот контракт, `constitution.md`, `spec.md`,
`decisions.md`, `tasks.md` (строка T027), журнал блока `timesheets` целиком
(решения 12–16 про закрытие точки), `CLAUDE.md`, миграции `0022`, `0031`,
`0041`, `0042`, `0043`, `0044`, модули `src/payrun/calc.py`, `sheet.py`,
`lifecycle.py`, `src/timesheets/closing.py`, `src/web/views.py`,
`src/web/permissions.py`, `src/core/models.py`, `seed_dev.py`,
`tests/conftest.py`.

**Что уже сделано и переделывать нечего.** Закрытие **по точкам** целиком
закрыто соседним блоком (T022): таблица `timesheet_closures`, ограничивающие
политики на обе таблицы табеля, экран и смоук. Заморозка **периода целиком** —
T023/T025: триггеры на `payruns`, `payslips`, `payslip_totals`,
`pay_components`. Поэтому из формулировки задачи здесь остаётся ровно одно —
**построчная блокировка**: `POST /payslips/{id}/freeze` из контракта, которого
нет ни в одном виде.

**Чего именно не хватает, если смотреть по коду, а не по названию задачи.**
Сегодня спорный сотрудник держит месяц не «блокировкой», а тем, что пересчёт
пересобирает ведомость целиком: `calc._store` сносит все `payslips` расчёта и
пишет заново. Значит, любой пересчёт после правки часов переписывает и того
человека, по которому идёт спор, — «оставить его как есть, а остальное
пересчитать» сегодня невозможно ни через экран, ни через базу.

Отсюда три ответа на вопросы, которые задача обязана решить.

**1. Три запрета не должны объяснять один отказ тремя словами — и не будут.**
Разобрано по путям записи, а не по названиям:

| путь записи | сколько запретов | какой |
|---|---|---|
| `timesheets`, `timesheet_days` | один | закрытие точки (T022, политики) |
| `payruns` | один | утверждение периода (T023, триггер) |
| `payslips`, `payslip_totals`, `pay_components` | **два, вложенных** | утверждение периода (весь расчёт) → заморозка строки (одна строка) |
| `payslip_freezes` | один | право `payslip.freeze` + «период не утверждён» |

Два запрета на данных расчёта — не противоречие, а вложенность: заморозка
строки живёт **внутри неутверждённого периода**, а утверждение её поглощает.
Это закреплено механически, а не договорённостью:

- заморозить или снять заморозку в утверждённом периоде **отвергает база** той
  же функцией `payrun_is_frozen()`, на которой стоит заморозка периода;
- порядок сообщений задан порядком срабатывания сторожей: у одной таблицы
  `before`-триггеры Postgres вызывает в алфавитном порядке имён, поэтому
  сторож периода (`payslips_frozen`) отвечает раньше построчного
  (`payslips_row_frozen`). У утверждённого периода человек читает про период, а
  не про строку;
- в приложении тот же порядок написан явно, одним местом: период → строка.

Табеля построчная заморозка **не трогает вовсе**. Замороженная строка означает
«не пересчитывать этого человека», а не «не править его часы»: часы правятся,
просто в ведомость они попадут после снятия заморозки. Поэтому решение 13
блока `timesheets` («на пути записи в табель запрет ровно один») остаётся в
силе слово в слово.

**2. Что происходит с замороженной строкой при пересчёте: ничего.** И это
держится не на аккуратности `calc`, а на базе:

- `calc` **исключает** замороженных из пересчёта: их `payslips` не сносятся, а
  сами люди не считаются заново. Остальные пересчитываются как прежде;
- если бы `calc` этого не сделал, снос упёрся бы в триггер `payslip_frozen_guard`
  — то есть «заморозка не заморозка» не может случиться молча.

Механизм выбран триггером, а не политикой, сознательно, и решение 13 соседнего
блока этим не нарушается, а продолжается: **политиками закрывается ввод
данных** (операционная отсечка внутри партнёра, обслуживание обязано мочь
писать), **триггерами — уже посчитанные числа** (заморозку обязан соблюдать
любой путь записи, включая суперпользователя и чистый SQL). Построчная
заморозка — это заморозка посчитанных чисел, только уже одного человека,
поэтому она из второй семьи.

**3. Кто вправе и нужна ли причина.** Заводится право `payslip.freeze` — новое,
вместе со своим экраном, как требует шапка `0022` («политика, стоящая там, где
никто не пишет, — догадка»). Переиспользовать `period.approve` нельзя: отказ
показывает название действия, и человек, нажавший «Заморозить строку», прочитал
бы «Утверждение периода не входит в права вашей роли» — ровно тот случай, когда
один запрет объясняется чужими словами.

**Причина обязательна при заморозке и не требуется при снятии.** Заморозка —
это и есть запись спора: без объяснения «почему этот человек заморожен» строка
через месяц не читается никем. Держит требование сама схема (`not null` +
`check` на непустоту), а не приложение. Снятие причины не требует по тому же
доводу, по которому её не требует открытие точки (T022): снятие ничего не
переписывает, оно возвращает человека в общий порядок, и нажавший кнопку по
ошибке не должен ждать чужого разрешения. Откат утверждённого периода — другое
дело, там причина обязательна, потому что переписывается уже выданная ведомость.

**Чего эта задача не делает:** не считает ретро-дельту и не морозит табель
(T026), не трогает фоновый расчёт (T024) и очередь, не делает отчётов (T028+),
не меняет прав других ролей и не трогает `src/timesheets/`.

### 2026-08-08 — T027 сделан
### 2026-08-08 — T024, решения до кода

Прочитано фактически: этот контракт, `docs/furca/plan.md` (стек, контракты между
блоками, строка риска про контекст в фоновых задачах), конституция, спека,
решения, `tasks.md` (строка T024), `src/payrun/calc.py`, `src/payrun/lifecycle.py`,
`src/web/views.py`, `src/web/dbcontext.py`, `src/web/principal.py`,
`src/config/settings.py`, `src/core/db_types.py`, `src/core/fields.py`, миграции
`0004_rls`, `0005_app_role`, `0043`, `tests/conftest.py`, `docker-compose.yml`,
`.env.example`, таблицу переменных в `README.md`.

**Ресерч перед новой функциональностью** (правило «сначала смотрим, как это
решают другие»). Очередь выбрана не здесь — `django-q2` на самом Postgres
зафиксирован решением в `plan.md`, и переоткрывать вопрос нельзя. Проверялась
пригодность версии: `django-q2` 1.10.0 от 2026-05-01, поддержка Django 6.0
добавлена в 1.9.0 (PR #307 `django-q2/django-q2`), в классификаторах есть Python
3.13 и 3.14, зависимость объявлена как `django>=5.2`. Django 6.1 в списке
проверенных у них не назван, поэтому связка проверена фактически на этой машине,
а не по классификаторам. Своего кода очереди не пишем.

**Кто «запустил» расчёт с точки зрения базы.** Тот человек, который нажал
кнопку: его uuid едет в задаче, задача выставляет `set local role app_user` и
`app.user_id` через уже существующий `web.dbcontext.db_context` — он написан
ровно для кода без HTTP-запроса, второго такого не заводим. Из этого следует всё
остальное:

- политики RLS у задачи ровно те же, что были бы у запроса. Никакого
  «фон ходит владельцем таблиц» — это и есть названный в `plan.md` риск;
- **права и видимые регистры перечитываются в задаче из базы**, а не берутся из
  полезной нагрузки. Между постановкой в очередь и исполнением роль могли
  поменять, и расчёт обязан идти по правам на момент исполнения. Полезная
  нагрузка — заявление, проверка — RLS и `memberships`;
- задача, у автора которой права считать уже нет, заканчивается отказом с
  текстом, а не тихим расчётом «от имени системы».

**Что будет, если период утвердят, пока задача в очереди.** Побеждает
утверждение: оно уже случилось, а расчёт ещё нет. `refuse_if_approved` в начале
`calculate_period` отдаёт отказ словами, а если бы кто-то его убрал — запись
отвергнет триггер `payrun_frozen_guard` из T023, он действует на любой путь
записи, включая фоновый. Задача завершается состоянием «отказ» с той же
плашкой, что человек увидел бы синхронно. Заморозка T023 фону не переписывается
и никаких послаблений не получает.

**Прогресс пишется по второму соединению, и это не украшение.** Расчёт идёт
одной транзакцией (требование T042: повторный запуск не должен оставлять
полурасчёта), а незакоммиченная транзакция снаружи не видна — прогресс,
записанный внутри неё, появился бы на экране ровно тогда, когда он уже не нужен.
Автономных транзакций в Postgres нет, поэтому заводится второй алиас базы
(`progress`) на то же подключение со своими короткими транзакциями. Отсюда
железное правило: **основная транзакция расчёта не трогает `payrun_jobs`** —
иначе строка была бы заблокирована ею же, и канал прогресса встал бы намертво,
причём выглядело бы это как зависший расчёт.

**Идемпотентность держат две вещи в базе, а не дисциплина в коде:**

1. частичный уникальный индекс «один незавершённый расчёт на период»
   (`where status in ('queued','running')`) — второе нажатие кнопки не заводит
   второй задачи, а получает отказ словами;
2. перевод задачи `queued → running` условным `update` (compare-and-set): задача,
   не сумевшая перевести строку, не делает ничего.

Из второго бесплатно получается и защита от «взяли двумя рабочими процессами», и
безопасная передача расчёта в синхронный путь. Прежняя гарантия T042 (ведомости
пересобираются целиком, `payslips_payrun_employee_uniq`) остаётся последним
контуром и не трогается.

**Синхронный путь остаётся первым классом, подмены молча нет ни в одну сторону.**
`PAYRUN_BACKGROUND=0` — расчёт синхронный, и страница не обещает прогресса. При
включённом фоне, если задачу не взяли в работу дольше порога, страница **говорит
прямо**: похоже, рабочий процесс очереди не запущен, — и предлагает кнопку
«Посчитать прямо сейчас». Продукт не перестаёт считать, но человек знает, что
происходит.

Почему не «проверить живость кластера заранее»: у ORM-брокера `django-q2`
статистика кластера лежит в кэше Django, а кэш по умолчанию — в памяти процесса,
то есть веб не видит статистики рабочего процесса вовсе. Заводить общий кэш в
базе ради предсказания значило бы завести второй источник истины, который врёт в
обе стороны (кластер жив, а стата протухла — и наоборот). Наблюдаемый факт
«задачу не взяли за N секунд» честнее предсказания.

**Полезная нагрузка очереди подписана `SECRET_KEY`, но сериализуется pickle** —
это в `django-q2` намертво (`django_q/signing.py`), сменного сериализатора нет.
Отсюда правило, которое и без того следует из предыдущего: в задачу едут только
идентификаторы строками, и ни одно значение из неё не используется как право —
всё перечитывается из базы под политиками.

**Статус задачи — нативный enum**, как остальные словари домена: база обязана
отвергать чужое значение сама. Тип добавляется в `ENUM_TYPES` драйвера — иначе
psycopg отдал бы массив таких значений строкой целиком (см. шапку `db_types`).

**Чего эта задача не делает:** не трогает ретро-дельту (T026), построчные
блокировки и закрытие точек (T027), отчёты (T028–T032) и оформление (T016–T018);
не заводит расписаний и вторых очередей — задача в очереди ровно одна.

### 2026-08-08 — T024 сделан

Задачу вели трое: двое предыдущих агентов погибли на обрыве связи, третий довёл.
Код и тесты фонового расчёта были написаны до меня и описаны выше; здесь только
то, что закрывалось последним, и что из этого стоит знать следующему.

Что появилось:

| Файл | О чём |
|---|---|
| `src/core/migrations/0050_payslip_freezing.py` | таблица заморозок, сторожа чисел, сторож самой заморозки, политики права |
| `src/core/models.py` | модель `PayslipFreeze` |
| `src/payrun/freezing.py` | состояние заморозок, отказы словами, заморозка и снятие |
| `src/payrun/calc.py` | пересчёт обходит замороженные строки стороной |
| `src/payrun/sheet.py` | метка заморозки и её причина едут вместе со строкой |
| `src/web/views.py`, `urls.py` | `payslip_freeze`, `payslip_release` общей обвязкой `payslip_action` |
| `src/web/permissions.py` | право `payslip.freeze` названо словами |
| `src/web/templates/web/period.html` | колонка «Спор»: метка, причина, заморозка и снятие |
| `src/core/management/commands/seed_dev.py` | право `payslip.freeze` у директора и бухгалтера; уборка снимает заморозки |
| `tools/smoke_freezing.mjs` | браузерный смоук заморозки |

Решения, принятые по ходу, и почему:

- **Заморозка — своя таблица, а не колонка у `payslips`.** Ключ — «тенант +
  строка ведомости», действующая заморозка это строка с пустым `released_at`,
  частичный уникальный индекс держит «одна действующая на строку». Снятие не
  удаляет запись, а помечает её: «из-за чего морозили и кто» — единственный
  ответ на вопрос о споре, и стирать его нельзя. Спор может вернуться, поэтому
  морозить и снимать можно много раз, и история копится. Ровно та же форма, что
  у закрытия точки (решение 12 соседнего блока) — и по той же причине.
- **Пересчёт обходит замороженных стороной, а не пересчитывает и откатывает.**
  `calc._store` исключает их из сноса и из вставки. Пропуск сделан явно, но
  **гарантией не является**: снос упёрся бы в триггер `payslips_row_frozen`.
  Проверено порчей — с убранным пропуском пересчёт падает отказом базы, а не
  переписывает числа молча.
- **Триггеры, а не политики, и решение 13 блока `timesheets` этим не нарушено, а
  продолжено.** Там сказано: ввод данных закрывается политиками (операционная
  отсечка, обслуживание обязано мочь писать), уже посчитанные числа — триггерами
  (гарантия обязана действовать на любой путь записи). Построчная заморозка —
  про посчитанные числа, только одного человека, поэтому она из второй семьи.
  Заморозка, которую снимает подключение другим пользователем, заморозкой не
  является.
- **Порядок отказов задан именами триггеров, и это записано в схеме.**
  `before`-сторожей одной таблицы Postgres вызывает в алфавитном порядке, поэтому
  `payslips_frozen` (период) отвечает раньше `payslips_row_frozen` (строка).
  У утверждённого периода человек читает про период — «строка заморожена» ничего
  не объясняет там, где заморожено всё. В приложении тот же порядок написан
  явно, одним местом (`_refuse_if_period_approved` перед проверкой строки).
  Порча подтвердила: переименование сторожа так, чтобы он вставал первым, красит
  тест — то есть порядок держится не на удаче.
- **Новая заморозка в утверждённом периоде отвергается, снятие — разрешено.**
  Отвергает та же функция `payrun_is_frozen()`, на которой стоит заморозка
  периода: второго правила о том, «что такое утверждённый расчёт», не заводится.
  Снятие разрешено намеренно: оно ничего не переписывает (числа держит заморозка
  периода), а иначе строка застряла бы в закрытом месяце навсегда и обслуживание
  не смогло бы убрать за собой — ровно тот дефект, что был у сида в issue #60.
- **Причина обязательна при заморозке и не нужна при снятии.** Требование держит
  ограничение схемы (`payslip_freezes_reason_not_blank`), а не форма: порча
  подтвердила — без проверки в приложении отказ приходит `CheckViolation` от
  базы. Снятие причины не требует по тому же доводу, по которому её не требует
  открытие точки: оно возвращает человека в общий порядок, а нажавший кнопку по
  ошибке не должен ждать чужого разрешения.
- **`payslip.freeze` — отдельное право, а не следствие «умею утверждать».** Отказ
  показывает название действия, и на «Заморозить строку» человек прочитал бы
  «Утверждение периода не входит в права вашей роли» — один запрет чужими
  словами. В сиде право выдано директору и бухгалтеру: месяц ведут они. **Это
  правка ролей сида, то есть чужого блока (db/auth), на две строки** — без неё
  политика стояла бы там, где никто не пишет, а экран нельзя было бы проверить
  ни тестом, ни смоуком.
- **Заморозка видна тому, кому видна её строка ведомости.** Политика — `exists`
  по самим `payslips`, а не копия правил точки и сотрудника: разъехавшиеся копии
  одного правила и есть тот способ, которым доступ ломается незаметно (D014).
  Управляющий видит заморозки только своей точки; чужая строка для него не
  существует вовсе — на её адрес приходит 404, а не 403.
- **Метка стоит у каждой строки человека, а действие — один раз.** У сотрудника
  строк бывает две, по одной на регистр; живая половина ведомости вводила бы в
  заблуждение, а две одинаковые кнопки об одном и том же читаются как разные.
- **Заморозка не трогает табель.** Замороженная строка означает «не
  пересчитывать этого человека», а не «не править его часы»: правка входных
  данных задним числом — T026. Поэтому на пути записи в часы запрет по-прежнему
  ровно один — закрытие точки, и решение 13 остаётся в силе слово в слово.
- **Уборка снимает заморозки, а не обходит их.** И `seed_dev._wipe`, и
  `wipe_payruns` в тестах теперь помечают заморозки снятыми перед сносом
  расчётов. Иначе сид падал бы на чужом споре — тот же класс дефекта, что
  issue #60, и он был бы заведён этой задачей, если бы не был закрыт сразу.

Тесты — `tests/test_payslip_freezing.py`, 32 штуки. Полный прогон:
**460 passed, 8 skipped** (было 428 + 8 до задачи; пропуски прежние — сверка с
настоящей таблицей партнёра, файла в репозитории нет, D028). Покрытие 97% при
пороге 80%. Все проверки запретов идут ролью `app_user`; отдельным тестом
показано, что числа замороженной строки не отдаёт менять и **владельцу таблиц**.

Проверка тестов порчей:

| Что сломано | Что покраснело |
|---|---|
| пересчёт не обходит замороженных | расчёт падает **отказом триггера** (`строка ведомости заморожена: DELETE в «payslip_totals»`) — то есть база и есть настоящий контур |
| сторож `payslips_row_frozen` убран из миграции | замороженную строку удаляют — две проверки, включая владельца таблиц |
| сторож строки переименован так, чтобы срабатывать первым | у утверждённого периода отказ начинает говорить про строку, а не про период |
| проверка причины убрана из приложения | вместо 400 приходит `CheckViolation` от базы |
| проверка права убрана из обработчика | вместо 403 приходит ошибка политики (500) |
| страница не смотрит на право | управляющий снова видит форму, которая даст 403 |
| `sheet` не спрашивает заморозки | метка и причина пропадают с экрана |

Проверка в живом браузере (`tools/smoke_freezing.mjs`, свой headless Chrome по
протоколу отладки на порту 9354, окно 1440×900, клики и ввод настоящими
событиями; приложение — свой compose-стенд `dodo-pnl-pr4` на 8054, база 5462):
**44 из 44 проверок прошли**, консоль чистая.

| Шаг | Что увидел |
|---|---|
| директор считает период | 60 строк, итог 1 951 806,13, замороженных нет, заморозка предложена на 35 строках |
| набирает причину и жмёт «Заморозить» | помечены обе строки человека (официальный и дополнительный регистры), причина видна, предложено снятие |
| ставки удвоены в базе, директор жмёт «Посчитать период» | у замороженного 1 500,00 и 32 648,00 — те же; у остальных новые; итог стал 3 814 587,72; строк по-прежнему 60 |
| директор утверждает период | «Утверждён» — спорная строка месяц не удержала; форм заморозки не осталось |
| заморозка в утверждённом периоде мимо экрана | **409** и «Период утверждён…», про строку — ни слова |
| откат с причиной, затем «Снять заморозку» | метки нет; пересчёт даёт человеку новые числа (32 648,00 → 65 296,00) |
| ставки возвращены, пересчёт | снова 60 строк и **1 951 806,13**, норма 176,00 |
| заморозка без причины мимо экрана | **400** и «требует причины» |
| управляющий | форм заморозки нет, объяснение есть; заморозка строки **его** точки мимо экрана — **403** |
| бухгалтер / управляющий | заморозка предложена бухгалтеру; 33 строки и 464 752,41 / 24 строки и 891 373,32 |

Что стоит знать следующему:

- **Адрес строки ведомости живёт до первого пересчёта.** `calc` пересобирает
  `payslips`, поэтому у незамороженного человека `payslip_id` после пересчёта
  другой. У замороженного — тот же самый, в этом и смысл. Экран берёт адреса из
  свежей ведомости, а вот внешняя ссылка на строку между пересчётами
  протухает — учесть, когда появится JSON-API из контракта.
- **Заморозка переживает откат периода.** Она независима от цикла: открыли
  месяц заново — замороженный по-прежнему не пересчитывается, пока заморозку не
  снимут. Так и задумано: спор не заканчивается оттого, что месяц открыли.
- **Приложение не даёт заморозить дважды** (повторное нажатие возвращает ту же
  заморозку вместо ошибки уникального индекса), но частичный индекс всё равно
  стоит — на путь мимо экрана.
- **Порядок имён триггеров — часть контракта схемы.** Новый сторож на
  `payslips`, `payslip_totals` или `pay_components` обязан вставать в
  алфавитном порядке так, чтобы более общий запрет отвечал раньше частного.
  Записано комментарием в `0050`.
- **`docker compose` не подхватывает правку шаблонов**: у стенда нет тома с
  исходниками, образ собран. Правка разметки без `--build` не видна, и на этом
  легко потерять полчаса, решив, что селектор смоука сломан.

**Дефект T023/T025, найденный уборкой за смоуком (issue #62, починен здесь же).**
`seed_dev` падал на базе, где расчёт остался **утверждённым**:

    период утверждён: DELETE в «pay_components» отклонён

Сторож заморозки расчёта действует и на владельца схемы, а сид сносит данные
своими руками. Прошлый тест (`test_the_seed_runs_over_a_period_that_was_approved`)
случай не ловил: он открывает период **перед** сидом, то есть проверяет
«утверждали и открыли», а не «утверждён прямо сейчас» — имя обещало больше, чем
тест делал. Починка — уборка сама открывает такой период заново, с причиной, тем
же способом, что `wipe_payruns` в тестах. Сторожа не ослаблялись. Недостающий
тест заведён (`test_the_seed_runs_over_a_period_that_is_still_approved`, до
правки красный, гоняет настоящую команду подпроцессом).

### 2026-08-08 — T024, решения до кода

Прочитано фактически: этот контракт, `docs/furca/plan.md` (стек, контракты между
блоками, строка риска про контекст в фоновых задачах), конституция, спека,
решения, `tasks.md` (строка T024), `src/payrun/calc.py`, `src/payrun/lifecycle.py`,
`src/web/views.py`, `src/web/dbcontext.py`, `src/web/principal.py`,
`src/config/settings.py`, `src/core/db_types.py`, `src/core/fields.py`, миграции
`0004_rls`, `0005_app_role`, `0043`, `tests/conftest.py`, `docker-compose.yml`,
`.env.example`, таблицу переменных в `README.md`.

**Ресерч перед новой функциональностью** (правило «сначала смотрим, как это
решают другие»). Очередь выбрана не здесь — `django-q2` на самом Postgres
зафиксирован решением в `plan.md`, и переоткрывать вопрос нельзя. Проверялась
пригодность версии: `django-q2` 1.10.0 от 2026-05-01, поддержка Django 6.0
добавлена в 1.9.0 (PR #307 `django-q2/django-q2`), в классификаторах есть Python
3.13 и 3.14, зависимость объявлена как `django>=5.2`. Django 6.1 в списке
проверенных у них не назван, поэтому связка проверена фактически на этой машине,
а не по классификаторам. Своего кода очереди не пишем.

**Кто «запустил» расчёт с точки зрения базы.** Тот человек, который нажал
кнопку: его uuid едет в задаче, задача выставляет `set local role app_user` и
`app.user_id` через уже существующий `web.dbcontext.db_context` — он написан
ровно для кода без HTTP-запроса, второго такого не заводим. Из этого следует всё
остальное:

- политики RLS у задачи ровно те же, что были бы у запроса. Никакого
  «фон ходит владельцем таблиц» — это и есть названный в `plan.md` риск;
- **права и видимые регистры перечитываются в задаче из базы**, а не берутся из
  полезной нагрузки. Между постановкой в очередь и исполнением роль могли
  поменять, и расчёт обязан идти по правам на момент исполнения. Полезная
  нагрузка — заявление, проверка — RLS и `memberships`;
- задача, у автора которой права считать уже нет, заканчивается отказом с
  текстом, а не тихим расчётом «от имени системы».

**Что будет, если период утвердят, пока задача в очереди.** Побеждает
утверждение: оно уже случилось, а расчёт ещё нет. `refuse_if_approved` в начале
`calculate_period` отдаёт отказ словами, а если бы кто-то его убрал — запись
отвергнет триггер `payrun_frozen_guard` из T023, он действует на любой путь
записи, включая фоновый. Задача завершается состоянием «отказ» с той же
плашкой, что человек увидел бы синхронно. Заморозка T023 фону не переписывается
и никаких послаблений не получает.

**Прогресс пишется по второму соединению, и это не украшение.** Расчёт идёт
одной транзакцией (требование T042: повторный запуск не должен оставлять
полурасчёта), а незакоммиченная транзакция снаружи не видна — прогресс,
записанный внутри неё, появился бы на экране ровно тогда, когда он уже не нужен.
Автономных транзакций в Postgres нет, поэтому заводится второй алиас базы
(`progress`) на то же подключение со своими короткими транзакциями. Отсюда
железное правило: **основная транзакция расчёта не трогает `payrun_jobs`** —
иначе строка была бы заблокирована ею же, и канал прогресса встал бы намертво,
причём выглядело бы это как зависший расчёт.

**Идемпотентность держат две вещи в базе, а не дисциплина в коде:**

1. частичный уникальный индекс «один незавершённый расчёт на период»
   (`where status in ('queued','running')`) — второе нажатие кнопки не заводит
   второй задачи, а получает отказ словами;
2. перевод задачи `queued → running` условным `update` (compare-and-set): задача,
   не сумевшая перевести строку, не делает ничего.

Из второго бесплатно получается и защита от «взяли двумя рабочими процессами», и
безопасная передача расчёта в синхронный путь. Прежняя гарантия T042 (ведомости
пересобираются целиком, `payslips_payrun_employee_uniq`) остаётся последним
контуром и не трогается.

**Синхронный путь остаётся первым классом, подмены молча нет ни в одну сторону.**
`PAYRUN_BACKGROUND=0` — расчёт синхронный, и страница не обещает прогресса. При
включённом фоне, если задачу не взяли в работу дольше порога, страница **говорит
прямо**: похоже, рабочий процесс очереди не запущен, — и предлагает кнопку
«Посчитать прямо сейчас». Продукт не перестаёт считать, но человек знает, что
происходит.

Почему не «проверить живость кластера заранее»: у ORM-брокера `django-q2`
статистика кластера лежит в кэше Django, а кэш по умолчанию — в памяти процесса,
то есть веб не видит статистики рабочего процесса вовсе. Заводить общий кэш в
базе ради предсказания значило бы завести второй источник истины, который врёт в
обе стороны (кластер жив, а стата протухла — и наоборот). Наблюдаемый факт
«задачу не взяли за N секунд» честнее предсказания.

**Полезная нагрузка очереди подписана `SECRET_KEY`, но сериализуется pickle** —
это в `django-q2` намертво (`django_q/signing.py`), сменного сериализатора нет.
Отсюда правило, которое и без того следует из предыдущего: в задачу едут только
идентификаторы строками, и ни одно значение из неё не используется как право —
всё перечитывается из базы под политиками.

**Статус задачи — нативный enum**, как остальные словари домена: база обязана
отвергать чужое значение сама. Тип добавляется в `ENUM_TYPES` драйвера — иначе
psycopg отдал бы массив таких значений строкой целиком (см. шапку `db_types`).

**Чего эта задача не делает:** не трогает ретро-дельту (T026), построчные
блокировки и закрытие точек (T027), отчёты (T028–T032) и оформление (T016–T018);
не заводит расписаний и вторых очередей — задача в очереди ровно одна.

### 2026-08-08 — T024 сделан

Задачу вели трое: двое предыдущих агентов погибли на обрыве связи, третий довёл.
Код и тесты фонового расчёта были написаны до меня и описаны выше; здесь только
то, что закрывалось последним, и что из этого стоит знать следующему.

Что появилось:

| Файл | О чём |
|---|---|
| `src/payrun/jobs.py` | постановка, захват, исполнение задачи, канал хода работы |
| `src/core/migrations/0046_payrun_jobs.py` | таблица заданий, изоляция, уникальный индекс |
| `src/core/migrations/0047_queue_privileges.py` | права роли приложения на таблицы очереди |
| `docker/queue_healthcheck.py` | живость очереди для Docker |
| `tools/smoke_payrun_background.mjs` | смоук в браузере на живом стенде |
| `src/web/templates/web/period.html` | полоса хода работы и опрос состояния |

**Проверка тестов порчей: 20 сломанных гарантий, три не покраснели.** Разбор
каждой — не отчёт о процессе, а описание того, где такие дыры заводятся:

1. **Изоляция заданий не была проверена ни на одном уровне.** Политику
   `tenant_isolation` на `payrun_jobs` можно было ослабить до `using (true)` —
   тесты оставались зелёными, потому что чужое задание отсекал фильтр по тенанту
   в выборке. И наоборот: фильтр убирался без единой красной проверки, потому что
   строку прятала политика. **Два контура прикрывали друг друга, и ровно поэтому
   ни один из них не был проверен** — это общий случай, а не частный: там, где
   защиты две, тест «снаружи ничего не видно» не различает, какая из них
   работает, и обе могут оказаться сломанными по очереди. Добавлен тест, который
   спрашивает саму базу под ролью `app_user`.
2. **Поломка внутри расчёта могла не дойти до очереди.** `raise` после записи
   отказа заменялся на `return`, и ни один тест не краснел: человек-то видел
   правду на странице, а вот `django-q` пометила бы задачу выполненной. Тест на
   двух получателей правды сразу.
3. **Ранняя проверка статуса в `run_job` не держала ничего.** Захват ниже и так
   условный, так что её удаление не меняло поведения — никакая порча его и не
   могла поймать. Убрана: код, чьё удаление не ловится ничем, обещает гарантию,
   которой не даёт.

Что осталось непокрасневшим осознанно: фильтр по тенанту в `last_job`. Он
избыточен — гарантию держит политика, и теперь она проверена прямо. Убирать не
стал: слой в приложении рядом с политикой базы — намеренная дублирующая защита,
а не забытый код.

**Дефект, найденный только запуском стенда.** `docker-compose.yml` ссылался на
`docker/queue_healthcheck.py`, которого в репозитории не было, а `Dockerfile`
копировал проверки пофайлово. Контейнер очереди был бы нездоров всегда. Проверка
написана и спрашивает **не «жив ли процесс»**, а «разбираются ли задачи»:
надзиратель `qcluster` бывает жив и работу при этом не берёт, и проверка на
существование процесса была бы зелёной всё это время. Копирование в образе
переведено на каталог целиком — пофайловый список ровно так и протухает.

**Обещание под кнопкой отстало от кода.** Подпись гласила «Считается сразу» —
неправда ровно в тот момент, когда человек решает, ждать ему на странице или
уйти. Текст жил в шаблоне строкой и пережил появление очереди незамеченным.
Теперь он зависит от режима, и на это есть тест.

**Смоук на живом стенде** (`tools/smoke_payrun_background.mjs`, свой headless
Chrome, настоящие клики; стенд `dodo-pnl-pr3`, приложение 8053, база 5461, все
три контейнера healthy). Очередь смоук гасит и поднимает сам — иначе фазу
«задачу никто не взял» не воспроизвести: 35 человек считаются быстрее, чем
успевает моргнуть полоса.

| Шаг | Что увидел |
|---|---|
| очередь погашена, директор жмёт «Посчитать период» | «Расчёт принят и считается в фоне», полоса «Идёт расчёт периода», этап «В очереди», ведомости нет |
| задание в базе | `queued`, фоновое |
| рабочий процесс поднят, страницу не трогаем | страница **сама** показала ведомость: 60 строк, 1 951 806,13, норма 176,00 |
| кто считал | задание `done`, фоновое; очередь отметила задачу выполненной |
| бухгалтер / управляющий | 33 и 464 752,41 / 24 и 891 373,32, норма 176,00 |
| очередь погашена, расчёт запущен снова | через порог страница сама сказала: «похоже, рабочий процесс очереди не запущен», и предложила «Посчитать прямо сейчас» |
| нажатие этой кнопки | 60 строк, 1 951 806,13, полосы нет; **второго задания не завелось**, прежнее доведено синхронным |
| рабочий процесс поднят обратно | опоздавшую задачу забрал и **ничего не пересчитал** — ведомость та же |

**Чего смоук не показал, и это надо знать честно:** полоса ни разу не успела
показать промежуточные проценты — 35 человек считаются быстрее одного цикла
опроса (0,7 с), поэтому глазами видно «В очереди» и сразу готовую ведомость. Что
отметки о ходе работы действительно видны снаружи **до конца расчёта**, держит
тест `test_progress_is_visible_before_the_calculation_commits`: он смотрит третьим
соединением, пока транзакция расчёта ещё открыта. На настоящем объёме страна
побольше Сербии — там это станет видно и глазами.

Прогон всего набора: **456 passed, 8 skipped** (пропуски прежние — сверка с
настоящей таблицей, файла нет и не должно быть). `makemigrations --check
--dry-run` — `No changes detected`; схема накатана с нуля (стенд поднимался после
`down -v`, и каждый тест схемы создаёт свою базу заново).

### 2026-08-08 — T026, разбор до кода

Прочитано фактически: этот контракт, `constitution.md`, `spec.md` (сценарий
правки задним числом), `decisions.md` (D014, D020, D021, D023), `tasks.md`
(строка T026), `CLAUDE.md`, миграции `0004_rls`, `0009`, `0011`, `0020`, `0021`,
`0022`, `0023`, `0041`, `0042`, `0050`, модули `src/payrun/calc.py`, `sheet.py`,
`lifecycle.py`, `freezing.py`, `src/core/models.py`.

**Первым делом убраны маркеры конфликта, закоммиченные в master** при сведении
`feat/payrun3` и `feat/payrun4`: `CHANGELOG.md`, этот журнал и — что хуже —
`src/payrun/calc.py`. В коде они уцелели незамеченными только потому, что попали
внутрь строки документации модуля: Python их не видит, тесты зелёные, а читатель
получает документ, в котором два варианта правды подряд. Разделы T027 и T024
восстановлены каждый целиком.

#### 1. Что считается «правкой задним числом» и где её ловить

**Не на пути записи, а на пересчёте.** Правка задним числом — это не событие
записи, а **расхождение**: утверждённый месяц хранит одни числа, а сегодняшние
данные дали бы другие. Определение одно, и оно покрывает все три названных
случая сразу — часы, правило расчёта, условия найма, — потому что все три
являются входами `calculate_period`.

Ловить на записи нельзя, и это не вопрос удобства:

- сторожей понадобилось бы столько, сколько входов: `timesheets`,
  `timesheet_days`, `employment_terms`, `rule_presets`, `rule_overrides`,
  календарь. Каждый новый вход обязан не забыть про свой — то есть гарантия
  держалась бы на дисциплине, ровно на том, от чего уходит D014;
- **изменение правила вообще не является записью «в закрытый период».** Ставка
  правится в версионированной таблице, и какие месяцы задеты, из самой записи не
  видно: это зависит от `valid_from`, от того, у кого какие условия найма, и от
  того, какие месяцы вообще утверждены;
- одна правка правила задевает разом всех, и «событие правки» у неё одно на
  тридцать человек — раскладывать его по людям всё равно пришлось бы расчётом.

Отсюда **теневой расчёт**: месяц считается заново в памяти, ничего не записывая,
и результат сравнивается с хранимым. Закрытый период при этом не может
измениться физически — сторож `payrun_frozen_guard` (T023) отвергает любую
запись в утверждённый расчёт, включая владельца таблиц. Тест сверяет снимок
закрытого месяца до и после байт в байт, а не полагается на аккуратность кода.

#### 2. Как дельта попадает в текущий период, если он уже посчитан или утверждён

**Дельта — это вход периода-получателя, а не строка, приписанная к готовой
ведомости.** Иначе первый же пересчёт получателя её снесёт: `calc._store` сносит
ведомости расчёта и пишет заново — модель «удалить и пересобрать» в принципе
несовместима с «часть строк переживает пересчёт». Поэтому:

- перенос хранится своей таблицей `retro_adjustments` — там же, по смыслу, где
  живут часы: это вход;
- расчёт получателя **материализует** его в `pay_components` с пометкой
  `retro_source_period`;
- пока получатель не пересчитан после переноса, страница говорит это прямо
  плашкой, а не показывает ведомость, в которой дельты нет. Поведение то же, что
  у часов: внёс — пересчитай. Молчаливое расхождение экрана с данными было бы
  хуже лишнего нажатия.

**Куда именно — первый после источника месяц, чей расчёт не утверждён**
(месяц без расчёта считается неутверждённым). Утверждённый принять дельту не
может, и не должен: ведомость по нему уже на руках. Правило детерминированное,
месяц назван на экране словами — ничего не угадывается и не выбирается молча.

**Накопление, чтобы повторный перенос не задваивал первый:**

    дельта = свежий расчёт − хранимый расчёт − уже перенесённое (неотменённое)

Отсюда бесплатно получается проверка: сразу после переноса расхождение равно
нулю, и повторное нажатие переносить нечему.

#### 3. Как дельта видна человеку

Строка ведомости — пара «сотрудник × регистр», колонка — код компонента. Дельта
по `hours.regular` слилась бы с обычной колонкой того же кода **и стала бы
невидимой** — бухгалтер увидел бы поехавшую сумму без единого следа причины.

Поэтому ключ строки расширяется до «сотрудник × регистр × месяц-источник»:
перенос — **отдельная строка** с пометкой «Перерасчёт за июнь 2026». Суммы при
этом стоят в своих обычных колонках, то есть видно не только «есть дельта», но и
**какой компонент** изменился. Пометка едет колонкой `pay_components`, а не
знанием одного экрана: её получает любой потребитель компонентов, включая
будущий P&L.

#### 4. Источник откатили и пересчитали после того, как дельта уже ушла

Двойного счёта не бывает по двум правилам, и оба держит база:

- **пересчёт источника отменяет перенос.** Триггер на `payruns`: изменился
  `calculated_at` — все неотменённые переносы этого месяца помечаются
  отменёнными. Не приложением: иначе «отмена без пересчёта» держалась бы на том,
  что каждый путь записи о ней помнит. Правка `retro_adjustments` руками
  запрещена политикой `only_from_trigger` (`pg_trigger_depth() > 0`) — тот же
  приём, которым закрыт журнал переходов в `0041`;
- **источник нельзя открыть заново, пока его дельта лежит в утверждённом
  периоде.** Она уже выплачена, и пересчёт источника означал бы заплатить
  дважды. Отказ словами даёт приложение, гарантию — сторож на `payruns`. Это не
  отмена обратимости из D021: пока получатель не утверждён, откат свободен.

Отменённый, но ещё материализованный перенос виден у получателя той же плашкой,
что в п. 2: экран сравнивает материализованное с живыми переносами и говорит,
когда они разошлись. Один механизм на оба направления — и «перенесли, но не
пересчитали», и «отменили, но не пересчитали».

#### 5. В каком регистре живёт дельта

Дельта считается **покомпонентно**, а регистр — свойство компонента, а не
человека (это уже установлено в T042). Поэтому «в тот же регистр» получается по
построению: правка, задевшая надбавку в официальном и часы в дополнительном,
даёт две строки переноса в двух регистрах, и они не складываются ни на каком
шаге. Удерживает это не код, а политика `ledger_visibility` на
`retro_adjustments` — такая же, как на `pay_components`.

Отсюда же следует, **кто вправе переносить**: только роль, видящая все
затронутые регистры. Проверка та же, что у расчёта (`check_ledgers`), и по той
же причине: база отвергла бы `insert ... returning` в невидимый регистр, но
ошибкой драйвера, из которой человеку ничего не понятно.

#### Настройка тенанта

`tenants.retro_mode` — нативный enum `retro_mode` (`delta` | `recalculate`),
умолчание `delta`. Не переменная окружения и не глобальный флаг: партнёры ведут
учёт по-разному, а страновая и партнёрская специфика в этом проекте живёт в
конфигурации (D020, принципы проекта).

Что она решает: **какой путь продукт предлагает при расхождении**. `delta` —
кнопку переноса. `recalculate` — открыть период заново с причиной (T025) и
пересчитать; перенос при этом отвергается словами, иначе настройка не решала бы
ничего.

Чего она **не** решает: право откатить утверждённый период. Оно есть всегда
(D021) — настройка, отбирающая обратимость, была бы не настройкой учёта, а
запретом.

#### Что решено ещё, и почему именно так

- **Право `retro.post` — своё, а не «умею считать».** Отказ показывает название
  действия: на «Перенести разницу» человек прочитал бы «Расчёт периода не входит
  в права вашей роли» — один запрет чужими словами (тот же довод, что у
  `payslip.freeze` в T027).
- **Итоги строки (`payslip_totals`) дельта не трогает, и это осознанно.**
  `payslip_totals` описывает расчёт **этого** месяца, а налог и взносы июньской
  правки относятся к июньской декларации, а не к июльской. Сложить их в июльские
  итоги значило бы испортить ровно то, по чему бухгалтер отчитывается. Ведомость
  и строки P&L в этом продукте и так собираются из компонентов, а не из итогов
  (решение T042), — значит, дельта попадает туда, где её ждут. **Граница для
  блока отчётов: отчёт, считающий месяц по `payslip_totals`, дельту не увидит.**
- **Сотрудник, у которого в получателе нет своего расчёта** (уволился, нет
  табеля), строку ведомости всё равно получает — иначе перенос было бы некуда
  положить. Итоги у неё нулевые, и это правда: своего июля у человека нет, есть
  только перенос за июнь.

**Чего эта задача не делает:** не трогает `src/timesheets/` (блок принят), не
делает отчётов (T028–T032) и оформления (T016–T018), не заводит следа расчёта
(D025, T013), не решает вопросов владельца из `questions.md`.


### 2026-08-13 — T082: что делает норма часов и чего она не делает

Блок долгов. Наблюдение соседнего агента: норму часов подняли **всем**
сотрудникам, строка табеля изменилась, а итог расчёта не сдвинулся ни на
копейку. Выглядит как молчаливо игнорируемый вход — худший вид дефекта: число
вводят, а оно никуда не идёт.

**Разбор.** Норма строки табеля читается движком ровно в одном месте — там, где
правило схемы прямо говорит брать часы из неё: `worked_hours_source:
half_of_norm` (полставка **без введённых часов**, `engine.calculate`). Во всех
остальных схемах деньги считаются из введённых часов, а пропорции надбавок и
необлагаемого минимума — из константы страны `constants.reference_norm_hours`.
То есть вход не игнорируется: у него узкая и объявленная роль. Проверено
перебором всех пяти схем страны, а не на одной.

**Почему наблюдение и выглядело как дефект:** у людей, на которых смотрели,
часы в табеле введены, а значит норма не участвует ни в одной формуле их
расчёта. Версия про ручную правку, перекрывающую эффект, не подтвердилась —
дело не в ней.

**Закреплено тестами** (`tests/test_norm_hours.py`, 10 проверок) — обе половины
сразу: где норма не двигает ничего и где двигает. Без второй половины первая
доказывала бы, что вход мёртв. Проверка порчей: пропорция надбавки, переведённая
на норму табеля, роняет пять тестов; снятое условие `half_of_norm` — один, и
именно тот, который про источник часов.

Наблюдение воспроизведено и на живых данных сида: норма поднята всем строкам
июня, период пересчитан — контрольное число приёмки 1 951 806,13 не сдвинулось.

**Что осталось вопросом бухгалтеру, а не решено здесь** (issue #74): пропорции
считаются от константы 176, а не от нормы месяца из производственного календаря.
В июне 2026 они совпадают, поэтому разницы не видно ни на одном числе, и приёмка
на июне её не поймает никогда. В месяце с другой нормой они разойдутся молча.
Календарь при этом виден на странице периода и правится в справочнике, то есть
**выглядит** входом расчёта, не будучи им; это закреплено тестом как факт, а не
как одобрение. Вопрос соседний с Q003 и требует таблицы бухгалтерии за второй
месяц.

### 2026-08-13 — T056 на стороне расчёта

След пишется тем же расчётом и той же транзакцией, что и суммы
(`payrun.steps.store_steps` из `calc._store`). Разбор и устройство — в журнале
блока `reports`; здесь важно одно: записи следа отдельным проходом нет и быть не
должно, иначе объяснение разъедется с числами именно на закрытом месяце.

### 2026-08-13 — T094: месяц закрывается сам вместе с утверждением

**Что было.** Утверждение ставило `payruns.status = approved`, а
`periods.status` оставался `open` навсегда: пути, который его меняет, в продукте
не было ни одного. Страница периода читалась как «Состояние: Открыт / Расчёт
зарплаты: Утверждён», список месяцев — «Июнь 2026 · Открыт». Демо при этом
проставляло `closed` руками, то есть показывало состояние, до которого продукт
своими силами не доходил.

**Решение: закрывается автоматически, отдельного действия нет.** Разбиралось
так. Кнопка «закрыть период» стала бы четвёртым шагом месяца, а онбординг
(T077) обещает три и заканчивается утверждением. Ни одного условия сверх уже
проверенных при утверждении у неё нет — то есть это ручка, за которой ничего не
стоит, а список продолжал бы врать у каждого, кто её не нажал. Сегодня в
продукте месяц закрывает только зарплата: других данных за период он не
собирает. Когда появятся (расходы, выписка, P&L), закрытие станет
самостоятельным решением и получит своё действие; выдумывать его сейчас — та же
догадка о ненаписанном экране, от которой отказалась `0041` про `paid`.

**«Закрыт» у месяца с откаченным расчётом — снова «открыт».** Это состояние, а
не история: иначе по списку нельзя отличить законченный месяц от того, который
прямо сейчас пересчитывают. История «утверждали и открыли обратно» и так есть в
`payrun_transitions` вместе с причиной и автором.

**Держит триггер, а не `lifecycle.approve()`** — `0190_period_closing`. Правило
«месяц закрыт тогда и только тогда, когда расчёт утверждён» — утверждение о
данных; записанное в приложении, оно обходилось бы сидом, обслуживанием и любым
чистым SQL. Соответствие статусов вынесено в функцию
`period_status_for_payrun()`: второго списка в Python нет, как и у переходов.
Прямая правка `periods.status` запрещена сторожем `periods_status_guard`
(глубина триггера ≤ 1 = оператор снаружи) — хранимая копия чужого состояния без
такого запрета остаётся вторым источником истины. Запрет только на `update`
статуса: заведение месяца — дело администрирования, сид заводит месяцы до того,
как появится первый расчёт.

**Доливка в той же миграции.** Без неё правда в списке началась бы со
следующего утверждения, а уже утверждённые месяцы остались бы открытыми
навсегда: из `approved` цикл ведёт только в откат. Доливка идёт до сторожа —
это единственный оператор, которому позволено ставить состояние снаружи.

**Гарантии T023 и T025 целы** и закреплены отдельно: запись в утверждённый
расчёт по-прежнему отвергает заморозка, откат по-прежнему требует причины, а
отказ отката оставляет месяц закрытым (иначе состояния разъехались бы на
отказе).

**Закреплено** `tests/test_period_closing.py`, 15 проверок: база (ролью
`app_user` и владельцем таблиц), доливка на схеме, накатанной до T094, и экран —
список месяцев и страница периода после настоящих утверждения и отката.
Проверка порчей: `reopened → closed` в соответствии статусов роняет 2 теста,
снятый сторож — 2, убранная доливка — 1 (и именно тот, который про старые базы).

Задача сделана мной, не исполнителем: цена ошибки — состояние месяца в базе.

## Что осталось следующим очередям

- T011/T012 — правила из базы вместо YAML; `select_preset` меняется телом.
- T026 — ретро-дельта: правка задним числом даёт помеченную строку в текущем
  периоде. Заморозка строки её не заменяет: заморозка держит числа как есть, а
  ретро-дельта переносит разницу вперёд.
- T024 — фоновая задача расчёта; сегодня расчёт синхронный.
- T013 (D025) — след расчёта: сейчас видно только результат.
- Разнесение зарплаты по точкам при переводе в середине месяца: точка берётся
  из табеля, второй табель на того же человека в другой точке схема допускает,
  но ведомость покажет его двумя строками одного регистра — проверить, когда
  появится ввод часов.
