# Блок hr — кадры: человек, условия его работы и точки, на которые ложатся его деньги

Текущая задача — **T221** ([issue #194](https://github.com/GarroV/maximus/issues/194),
решение **D055**): затраты человека разносятся по точкам, к которым он привязан.
Одна точка — целиком на неё, несколько — делятся, ни одной — это сеть.

Рабочая копия: `~/Documents/workbench/worktrees/maximus-hr`, ветка `feat/hr-unit-split`.
Стенд: `docker compose -p maximus-hr`, порты 8110 (приложение), 8111 (Postgres),
8112 (демо), 8113 (healthcheck).

Прежние задачи блока — T164 (заведение человека, схема расчёта и мера работы
выбором из списка), T197 (деление ФОТ по точкам), T210 (экран привязки к точкам) —
приняты и закрыты. Как и почему они сделаны, лежит в `git log` и не пересказывается
здесь.

## API-контракт

<!-- Снят с кода 02.09.2026: адреса — из `docs/routes.md` (собирается резолвером
Django). -->

**Что блок отдаёт — справочник людей и условий их работы:**

| Адрес | Что это |
|---|---|
| `/directory/employees/` | люди: список, карточка, заведение, увольнение, точки человека |
| `/directory/groups/` | группы расчёта и их условия |
| `/directory/groups/raise/` | массовое изменение ставок |
| `/directory/positions/` | должности (вилка ставки) |
| `/directory/units/`, `/directory/legal-entities/` | точки и юрлица |
| `/directory/calendar/` | производственный календарь по месяцам |

**Что блок потребляет:**

- правила страны и уровни переопределения из `core.rules` — схема расчёта и мера
  работы выбираются **из списка, известного правилам**, а не вводятся текстом;
- версионирование по датам из `core` (D020) — условия найма и привязка к точкам
  не правятся на месте, а заводят новую версию;
- контекст пользователя из `web` — права `rules.manage` и `directory.manage`
  (последнее после D059 есть и у оперативного директора, и у бухгалтера);
- механизм разнесения фактов по точкам (`allocation_plan`, `allocate_fact`) — им
  разносится ФОТ сетевого человека; второй такой механизм не пишется.

**Что блок отдаёт наружу движку:** через таблицы условий найма —
`employee_id → (схема расчёта, мера работы, ставка, коэффициент, группа, точки с
долями)` на дату. Движок `src/payroll/` читает это и не знает про экраны;
обратной зависимости нет и заводить её нельзя.

**Инвариант:** изменение условия задним числом не пересчитывает закрытый период
молча — разница ложится помеченной дельтой в текущий (D020).

## Definition of Done (T221)

- [x] У человека набор точек вместо одной, версионированный по датам (T197, T210)
- [x] Затраты делятся между его точками; доля у строки переопределяет, пусто — поровну
- [x] Управляющий видит своего человека, даже если тот привязан и к чужой точке —
      проверено ролью `app_user`, включая запись и отсутствие расширения чужого доступа
- [ ] Пустой набор означает сеть **на деле**: ФОТ офиса разносится по точкам, а не
      висит `pending`
- [ ] Табель и ведомость показывают человека с несколькими точками

## Где стоим

**Сделано, закоммичено и запушено** (`0d78988`):

- `0269_employee_visible_by_units` — `app_employee_is_visible` спрашивает и
  привязку к точкам, тем же условием `app_unit_is_visible`. Дыра была в одну
  сторону: часть ФОТ ложится в затраты точки, а её управляющий человека в
  справочнике не находит;
- `tests/test_employee_units_visibility.py` — 7 проверок ролью `app_user`.

**Найдено и подтверждено прогоном, ещё не починено.** ФОТ сетевого человека
**висит неразнесённым навсегда**. Проверка
`test_the_office_payroll_does_not_hang_undistributed` (дописана в
`tests/test_payroll_across_units.py`, сейчас красная) даёт 3 строки на
−46 548,55 в состоянии `pending`. Причина: `allocation_rule_for` ищет правило по
контрагенту **или** по статье расхода, а у зарплатного факта нет ни того, ни
другого — правило не находится никогда, план пуст, факт ждёт человека. Соседняя
`test_a_person_without_units_...` этого не ловит: её условие
`unit_id is null or allocation = 'allocated'` зеленеет и на висящей строке.

## Что дальше

1. **Разнести ФОТ сети правилом**, переиспользуя существующий механизм.
   Разобранные варианты и почему отвергнуты:
   - *синтетическое правило в `allocation_plan`* — у ребёнка разнесения схема
     требует либо `allocation_rule_id`, либо `allocated_by` (см. комментарий у
     `Fact.allocated_by`); правило без строки не проходит;
   - *статья расхода для зарплаты* — `expense_items` запрещает статью без
     поверхностей, то есть она полезла бы в форму внесения расхода;
   - *строки правил в каждом пути заведения тенанта* (`seed_dev`, `demo/seed`,
     `spaces.create_space`) — хрупко: четвёртый путь молча останется без правила.
   Остаётся третий ключ правила — **строка P&L** (`allocation_rules` с пустыми
   контрагентом и статьёй) и одно место, где строка заводится. Экрана для этой
   настройки в продукте нет; где ей жить — «у партнёра или у группы» — открытый
   пункт самого issue #194, и решать его за владельца нельзя.
2. **Табель и ведомость показывают набор точек** человека, а не одну.
3. Живая проверка экранов на 8110 со снимками, полный прогон, уборка стенда.

## Чем проверено

- `pytest tests/test_employee_units_visibility.py` — 7 passed (98 с);
- отрицательный прогон той же проверки со сломанным условием — 3 failed, 4 passed,
  то есть проверка ловит именно снятое правило;
- `pytest tests/test_payroll_across_units.py -k office` — 1 failed: дефект
  подтверждён фактом, а не чтением кода.

## Открытые вопросы блока

- Где живёт настройка способа деления (у партнёра или у группы) — пункт issue #194,
  ответ за владельцем. Умолчание «поровну» названо им прямо и сомнению не подлежит.
- Заведённому с экрана человеку негде взять часы: строка табеля создаётся только
  загрузкой таблицы за месяц ([issue #152](https://github.com/GarroV/maximus/issues/152)).
